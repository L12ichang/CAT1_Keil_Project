# 中科MQTT协议开发进度分析与实现计划

> 基于「重构开发方案v2.0.md」与「中科协议.docx」交叉复核
> 目标工程：`MDK-ARM-8008000`
> 统一服务器：`47.120.15.220:1883`
> CRC校验：统一使用 **CRC16-Modbus (0xA001)** ✅ 已修正
> 最后更新：2026-05-25

---

## 一、总体进度：约 96%（2026-05-25 修复后复核）

```
已完成   ████████████████████████████████████░  96%
未完成   █░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░    4%
```

> 2026-05-25 修复后复核结论：当前主工程 `MDK-ARM-8008000/project.uvprojx` 已接入 `mqtt_zk_protocol.c`、`zk_property.c`、`zk_alarm.c`、`zk_runtime_stats.c`、`zk_work_plan.c`、`zk_sunriset.c` 等核心模块，登录、心跳、属性读写、基础控制、告警配置、计划存储、OTA入口等主链路具备基础可用性。本次已修复 `Plan DO="sr"` 协议入口/响应格式，以及 Type=2 日出日落计划 `timetp=1 + dTime[]` 时段型支持。按当前确认边界：不要求 Type=3 光感计划、不要求双回路、不要求 10006 灯杆倾斜；剩余主要缺口为 10005 关灯异常采集源和实机/Keil构建验证。

### 进度变化说明（2026-05-22 本轮新增）

本轮新增了 **日出日落计算 (zk_sunriset)、Type=2 日出日落计划、告警占位完善** 三项能力。2026-05-25 修复后确认：NOAA 日出日落算法已接入 `SV="plan"` 的 `DO="sr"` 查询，响应格式为协议要求的 `DT.sr` 数组；Type=2 已按协议区分 `timetp=0` 时刻型 `time[]` 与 `timetp=1` 时段型 `dTime[]`；关灯异常(10005)仍只有状态表占位，灯杆倾斜(10006)因无传感器不纳入当前范围。

```
进度增量明细:
  日出日落计算 (zk_sunriset)                      +0.4%
  Type=2 日出日落计划协议修正                    +0.4%
  告警状态表完善(10005/10006 占位)                +0.1%
  ─────────────────────────────────────────
  Plan P1协议缺口已修复，当前总体约96%
```

---

## 1.1 2026-05-25 代码复核结论

### 复核范围

- 协议依据：`中科协议.docx`，重点核对 MQTT 主题、SV/CT、属性、控制、告警、工作计划、上报、校时、OTA。
- 代码依据：`MDK-ARM-8008000/project.uvprojx` 确认主工程已编译引用中科协议相关源文件；重点核对 `Core/Src/LampProtocolLib/mqtt_zk_protocol.c`、`Json_Protocol.c`、`zk_property.c`、`zk_alarm.c`、`zk_work_plan.c`、`zk_sunriset.c`、`ota.c`、`TcpClient.c`、`main.c`。
- 验证结果：
  - `bash tests/run_mqtt_protocol_tests.sh`：通过 48 项基础 MQTT/登录/路由测试。
  - `python3 tools/check_keil_app_image.py --project MDK-ARM-8008000/project.uvprojx --skip-freshness`：工程引用和 Flash 合同检查通过，但 `MDK-ARM-8008000/out/cat1.sct/.map/.bin/.hex` 缺失，烧录前必须重新用 Keil 构建。
  - `python3 -m unittest tests.test_zk_work_plan_contract tests.test_zk_control_contract tests.test_zk_property_flash_persistence tests.test_zk_publish_backpressure -v`：通过 24 项测试，已覆盖 Plan `DO="sr"`、Type=2 `dTime[]`、控制、属性Flash持久化、发送退避等契约。

### 复核结论

| 模块 | 结论 | 说明 |
|------|------|------|
| MQTT连接、主题、登录、心跳 | 基本完成 | 服务器、ClientID/Username、CRC16-Modbus 密码、主题订阅、登录/心跳状态机均已接入主工程。 |
| 属性读写 | 基本完成 | 常用属性组和 Flash 持久化已实现；当前项目按单回路边界验收，不再把双回路数组作为本阶段缺口。 |
| 控制命令 | 基本完成 | `cnCtrl` 支持当前单回路 `cns=1` 的调光/开关/last恢复；双回路不属于当前范围。 |
| 上报 | 基本完成 | L/H/C/B/A 主路径存在；当前硬件无倾角传感器，周期上报不要求携带 `Angle`。 |
| 告警配置与告警上报 | 部分完成 | `alam` 配置读写和 10000/10001/10002/10003/10004/10007/10008/10009 已接入；10005关灯异常只有状态表占位，无采集源；10006灯杆倾斜因无传感器不纳入当前范围。 |
| 工作计划 `SV="plan"` | 基本完成 | Type=1、基础存储、查询、删除、执行引擎存在；`DO="sr"` 已按 `SV="plan"` 返回 `DT.sr` 数组；Type=2 已支持 `timetp=0/time[]` 与 `timetp=1/dTime[]`。Type=3光感计划不属于当前范围。 |
| OTA | 基本入口完成 | 可接收 URL 并触发下载，上报 CT=P/E；但进度仍为固定节点，Keil产物缺失，实机烧录前需重新构建验证。 |

---

## 二、已完成项

### 2.1 基础架构（第一阶段）

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 数据结构定义 | `mqtt_zk_protocol.h` | ✅ 95% | `zk_mqtt_config_t`、`zk_device_config_t`、`zk_message_header_t`。SV/CT枚举、10种告警ID枚举均已定义。新增svrIp/svrPort/uPeriod/hPeriod/tPeriod/commMain/commSub/commSAuto/spreadOffset/spreadWindow/spreadInterval/dimTp字段 |
| CRC密码生成 | `mqtt_zk_protocol.c` | ✅ 100% | **CRC16-Modbus(0xA001)**，调用`crc16_modbus_get()` |
| MQTT连接参数 | `NbDriver.c` | ✅ 100% | 全部切换到ZK函数，IMEI认证，plt2dev+pcp2dev双主题订阅 |
| 栈大小 | `MDK-ARM-8008000/startup_stm32f103xe.s:33` | ✅ 100% | `Stack_Size EQU 0x800` (2KB) |

### 2.2 协议处理层（第二阶段）

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| JSON消息处理 | `mqtt_zk_protocol.c` | ✅ 95% | 消息头解析、登录/心跳/周期/变化/告警报文构建、属性读写、控制命令、校时请求、OTA入口、**告警配置读写**均已实现 |
| 协议入口路由 | `Json_Protocol.c` | ✅ 100% | **已收口为中科JSON统一分发入口**，通过`zk_dispatch_message()`路由prop/ctrl/rqst/ota/plan/alam全部SV类型 |
| cJSON双池内存管理 | `mqtt_zk_protocol.c` | ✅ 100% | RX池4KB + TX池4KB，8字节对齐，线性分配器，TX耗尽日志告警 |

```
                      MQTT 收包 (app_mqtt_rx)
                              │
                              ▼
                    ┌──────────────────┐
                    │  cJSON_Parse()    │
                    │  解析 JSON 字符串  │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ 消息头解析+设备SN  │
                    │  校验(IMEI匹配)    │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ 登录ACK/心跳ACK   │
                    │  快速路径拦截      │
                    └────────┬─────────┘
                             │ 非ACK消息
                             ▼
               ┌─────────────────────────┐
               │   zk_dispatch_message()  │
               │   按 SV+CT 联合路由       │
               └────────┬────────────────┘
                        │
        ┌───────┬───────┼───────┬───────┬───────┬───────┐
        ▼       ▼       ▼       ▼       ▼       ▼       ▼
      prop    prop    ctrl    plan    rqst     ota    alam
      (R)     (W)     (W)    (R/W)    (R)     (W)    (R/W)
```

### 2.3 上报机制（第三阶段）

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 登录上报(L) | `mqtt_zk_protocol.c` | ✅ 100% | DevInfo+MdlInfo+Gis+Dim+Sense，与协议要求一致 |
| 心跳上报(H) | `mqtt_zk_protocol.c` | ✅ 100% | 60秒间隔（可配置），无DT节点，与协议要求一致 |
| 周期上报(C) | `mqtt_zk_protocol.c` | ✅ 100% | 已上报RunTm+LightTm+RunSts+EleInfo+PerSts+Signal，默认300秒间隔。**RunTm/LightTm改用`zk_runtime_stats`独立模块统计，Signal.rsrp改用AT+QENG真实值**。当前硬件无倾角传感器，`Angle`不纳入周期上报验收 |
| 变化上报(B) | `mqtt_zk_protocol.c` | ✅ 100% | 亮度/开关变化时触发，通过`zk_notify_state_changed()`设置标志 |
| 告警上报(A) | `zk_alarm.c` | ✅ 95% | **已抽离为独立模块**。8种告警已接入硬件采集源（电压/电流/漏电/温度/失电），带pending/reported/one-shot状态机；灯杆倾斜(10006)/关灯异常(10005)未接入采集源 |

```
上报时间线 (设备在线期间):

  Login    H(60s)   H(120s)  H(180s)  H(240s)  C(300s)  H(360s) ...
  ──●────────●────────●────────●────────●────────●────────●─────▶
    │                                      │
    L                                    C(周期上报)
  (登录)                              RunTm+LightTm+RunSts
                                      +EleInfo+PerSts+Signal
                                      (当前无倾角传感器，不上报Angle)
  
  变化上报(B): 亮度/开关变化时立即触发 (非周期性)
  告警上报(A): 异常发生时立即触发 (带状态机防重复)
```

### 2.4 属性读写

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 属性读取框架 | `zk_property.c` | ✅ 95% | **已抽离为独立模块(1271行)**。支持DevInfo/MdlInfo/Gis/Dim/Sense/RunSts/EleInfo/PerSts/Signal/RTC/Svr/Comm/Spread共13组。**新增Factory属性组**。**新增RunTm/LightTm/Angle**（复用运行时统计及倾角构建函数） |
| 属性写入框架 | `zk_property.c` | ✅ 100% | 支持Gis/Dim/Sense/Svr/RTC/**Factory**写入，含范围校验和错误码返回；写入值已通过Flash双副本持久化，重启不丢失 |
| 属性Flash持久化 | `zk_property.c` | ✅ 100% | **已迁移至`zk_property.c`**，双副本+序列号方案，main+backup互为冗余，magic=0x5A4B5052 |

```
属性读取分发表 (zk_add_property_to_dt):

  items[] ──▶ 遍历每个属性名 ──▶ strcmp 匹配:
  ┌──────────┬──────────┬──────────┬──────────┬──────────┐
  │ DevInfo  │ MdlInfo  │   Gis    │   Dim    │  Sense   │
  │   ✅     │   ✅     │   ✅     │   ✅     │   ✅     │
  ├──────────┼──────────┼──────────┼──────────┼──────────┤
  │ RunSts   │ EleInfo  │ PerSts   │ Signal   │   RTC    │
  │   ✅     │   ✅     │   ✅     │   ✅     │   ✅     │
  ├──────────┼──────────┼──────────┼──────────┼──────────┤
  │   Svr    │  Comm    │  Spread  │ Factory  │  RunTm   │
  │   ✅     │   ✅     │   ✅     │   ✅     │   ✅ *   │
  ├──────────┴──────────┴──────────┼──────────┼──────────┤
  │        LightTm     Angle       │  (其他)   │          │
  │          ✅ *         ✅ *     │  ER=1    │          │
  └────────────────────────────────┴──────────┴──────────┘

  * RunTm/LightTm 共享运行时统计组（同时返回两者）
  * Angle 返回 x/y/z=0（无倾角传感器硬件）

  🆕 新增 Factory 属性组 (MID/SET_OUTCUR/HWMAX_OUTCUR/...)
  🆕 Signal 已改用 AT+QENG 真实 RSRP 值
  🆕 EleInfo.e 已上报当前电源故障位图 (不再固定填0)
```

### 2.5 控制命令

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| cnCtrl调光 | `mqtt_zk_protocol.c` | ✅ 100% | 当前项目按单回路验收，支持cns=1、last持续时间、亮度/开关控制、多item批量处理；双回路不属于当前范围 |
| cnCtrl恢复机制 | `mqtt_zk_protocol.c` | ✅ 100% | last分钟数到期后自动恢复原亮度，`zk_control_restore_process()`定时器驱动 |
| reboot重启 | `mqtt_zk_protocol.c` | ✅ 100% | 先响应ER=0，延迟500ms后NVIC_SystemReset() |
| restore恢复 | `mqtt_zk_protocol.c` | ✅ 100% | 支持0/1/5/6四种恢复类型（0/1/6恢复出厂配置到Flash，5清零电能），2/3/4返回不支持 |
| patrol巡检 | `mqtt_zk_protocol.c` | ✅ 100% | 响应ER=0后延迟上报周期数据；当前硬件无倾角传感器，巡检不要求携带Angle |

### 2.6 校时功能

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 主动校时请求 | `mqtt_zk_protocol.c` | ✅ 100% | SV="rqst", CT="R", DT.DO="TmCali"，默认3600秒周期 |
| 校时响应处理 | `mqtt_zk_protocol.c` | ✅ 100% | 解析TmCali响应，覆盖RTC |
| 服务器时间校准 | `mqtt_zk_protocol.c` | ✅ 100% | 登录响应中解析time字段，相差>30秒时覆盖RTC |

### 2.7 OTA升级

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| OTA指令接收 | `mqtt_zk_protocol.c` | ✅ 100% | SV="ota", CT="W", DT.url解析，提取文件名，触发HTTP下载 |
| OTA进度上报 | `mqtt_zk_protocol.c`、`ota.c` | ✅ 90% | CT="P"上报API，30/50/90/100进度节点；支持pending队列延迟发送 |
| OTA错误上报 | `mqtt_zk_protocol.c`、`ota.c` | ✅ 90% | CT="E"上报API，支持pending队列和挂起重试 |
| OTA URL提取 | `mqtt_zk_protocol.c` | ✅ 100% | `zk_get_ota_url()`供ota.c获取中科下发URL |

### 2.8 工作计划 (SV="plan")

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| Plan写入/更新 | `zk_work_plan.c` | ✅ 100% | `zk_handle_plan_message()`处理CT="W"，`zk_plan_upsert()`支持新增/覆盖/禁用 |
| Plan禁用 (en=0) | `zk_work_plan.c` | ✅ 100% | 保留全部配置仅翻转en位 |
| Plan删除 (del) | `zk_work_plan.c` | ✅ 100% | CT="W" + DT.del=[1,3,5] 数组格式 |
| Plan Flash持久化 | `zk_work_plan.c` | ✅ 100% | 双副本+magic=0x5A4B504C，plan存储区0x08006000/0x08007800，最多8个计划 |
| Plan执行引擎 | `zk_work_plan.c` | ✅ 95% | Type=1(周日期)基础执行可用；Type=2已按协议支持`timetp=0/time[]`和`timetp=1/dTime[]`，执行时按日落+setoffset作为时段起点累加dTime；仍需实机验证RTC/经纬度/调光输出 |
| Type=2 日出日落计划 | `zk_work_plan.c` | ✅ 90% | 已接入setoffset/riseoffset和zk_sunriset计算；协议示例中的`timetp=0`时刻型`time[]`、`timetp=1`时段型`dTime[]`均可解析、保存和查询回显 |
| 主循环集成 | `main.c` | ✅ 100% | `zk_work_plan_init()`初始化，`zk_work_plan_process()`每循环调用 |
| 有效计划查询 (DO="nid") | `zk_work_plan.c` | ✅ 100% | 返回所有valid=1且en=1的计划ID数组 |
| 计划内容查询 (id) | `zk_work_plan.c` | ✅ 100% | 返回指定id的完整计划JSON (含jobs/actions)，en=0时仅返回{id, en} |
| 当前运行查询 (DT.now) | `zk_work_plan.c` | ✅ 100% | DT.now=cns (Number), 支持通道过滤, 返回完整plan JSON |
| 日出日落查询 (DO="sr") | `zk_sunriset.c` / `zk_work_plan.c` | ✅ 100% | 已按协议接入`SV="plan"` + `CT="R"` + `DT.DO="sr"`；响应为`DT.sr=["HH:MM","HH:MM"]`数组，`SV="rqst"`下不再处理该命令 |

### 2.9 告警处理

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 硬件采集源接入 | `zk_alarm.c` | ⚠️ 90% | **已抽离为独立模块(247行)**。过压/欠压/过流/欠流/开灯异常(10004)/漏电/设备故障/设备失电均已接入。关灯异常(10005)仅加入状态表占位，未接入采集源；灯杆倾斜(10006)因无传感器不纳入当前范围 |
| 告警状态机 | `zk_alarm.c` | ✅ 100% | pending/pending_status/reported/one_shot四状态跟踪，防重复上报。状态表从8路扩展至10路 |
| 告警阈值动态计算 | `zk_alarm.c` | ✅ 100% | 过流阈值按额定电流150%计算，欠流按80%，温度阈值按NTC读数 |
| **告警配置读写 (SV="alam")** | `mqtt_zk_protocol.c` | ✅ 100% | **新增实现**。CT=R 读取单路/多路告警阈值配置；CT=W 写入告警阈值。支持 almValue/recValue/almEn 字段，可选字段不发送则保持原值。**阈值持久化到Flash**（通过`zk_device_config_save_alam()`） |
| **告警阈值可配置化** | `zk_alarm.c` | ✅ 100% | **阈值优先级**：平台 alam 配置值(非0) > 出厂默认值。过流/欠流/设备故障的默认值使用原有的动态计算逻辑 |

```
告警配置读写流程 (SV="alam"):

  CT="R" (读取告警阈值)
    DT.alms = [10000, 10007]
      → 遍历数组，查 zk_device_config_get() 获取 almValue/recValue/almEn
      → 构建响应 DT.alms = [{almId:10000, almValue:3200, recValue:2640, almEn:1}, ...]
      → 返回 ER=0

  CT="W" (写入告警阈值)
    DT.alms = [{almId:10007, almValue:50, recValue:30, almEn:1}]
      → 解析各字段（almValue/recValue/almEn 可选，不出现则保持原值）
      → zk_device_config_save_alam() 写Flash双副本
      → Flash写入成功 → 更新RAM配置 → 告警检测立即使用新阈值
      → 返回 ER=0
```

### 2.10 运行时统计 (新增模块)

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 运行时长累加 | `zk_runtime_stats.c` | ✅ 100% | **新增独立模块(259行)**，秒级累加运行/亮灯时间，从Flash加载历史数据 |
| 运行时统计初始化 | `main.c:164` | ✅ 100% | `zk_runtime_stats_init()`在main初始化时调用 |
| 周期处理 | `main.c:195` | ✅ 100% | `zk_runtime_counter_process()`每main loop调用 |
| 访问接口 | `zk_runtime_stats.h` | ✅ 100% | 提供get_boot_run_seconds / get_boot_light_seconds / get_total_run_seconds / get_total_light_seconds |

### 2.11 Signal信号增强

| 子任务 | 完成度 | 备注 |
|--------|--------|------|
| RSRP真实值查询 | ✅ 100% | **已改为AT+QENG真实值（`nb_get_rsrp_dbm10()`）**，不再使用getSignal()等级估算。Signal.rsrp只有在获取到真实值时才会添加到JSON中 |

### 2.12 Factory属性读写 (新增)

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| Factory属性读取 | `zk_property.c` | ✅ 100% | 读取Factory属性（含MID/SET_OUTCUR/HWMAX_OUTCUR等）并构建json |
| Factory属性写入 | `zk_property.c` | ✅ 100% | 支持通过prop W写入Factory字段，含范围校验 |
| 出厂配置恢复 | `zk_property.c` | ✅ 100% | `zk_device_config_restore_defaults()`走属性Flash持久化通道 |

### 2.13 关键集成点

| 集成点 | 文件 | 状态 |
|--------|------|------|
| MQTT连接成功后自动发送登录包 | `TcpClient.c` | ✅ |
| 断连自动重置会话 | `TcpClient.c` | ✅ |
| 会话状态机(登录重发/心跳/周期上报/校时/告警/OTA挂起上报/plan执行) | `zk_mqtt_session_process()` | ✅ |
| 告警处理集成(抽离为独立模块) | `zk_alarm_process()` | ✅ |
| 运行时统计集成 | `zk_runtime_counter_process()` | ✅ |
| 服务器时间校准(相差>30秒时覆盖) | `mqtt_zk_protocol.c` | ✅ |
| 控制恢复机制(last参数) | `mqtt_zk_protocol.c` | ✅ |
| 发送忙自动清理(连续3次失败后复位) | `mqtt_zk_protocol.c` | ✅ |
| 延迟响应队列(response/OTA progress/OTA error pending) | `mqtt_zk_protocol.c` | ✅ |

---

## 三、未完成项

2026-05-25 修复后结论：按当前确认边界（不做光感计划、不做双回路、不做10006灯杆倾斜），Plan 相关 P1 协议缺口已修复；剩余 P1 为 10005 关灯异常采集源，其他问题按 P2 或范围外处理。

### P1 — 协议闭环问题

| # | 问题 | 协议要求 | 当前代码现状 | 影响 |
|---|------|----------|--------------|------|
| 1 | 关灯异常(10005)未接入采集源 | 当前确认告警范围包含10004开灯异常和10005关灯异常 | `zk_alarm.c` 中10004使用 `Error_0_linght` 接入；10005目前仅状态表占位，未在 `zk_alarm_collect_sources()` 中采集 | 平台如果验收10005关灯异常，则不会产生/恢复上报 |

### P2 — 完善项

#### 3.1 范围外或待确认项

| 告警ID | 名称 | 状态 | 接入方案 |
|--------|------|------|----------|
| 10006 | 灯杆倾斜 | 范围外 | 当前硬件无倾角/加速度传感器，本阶段不实现，仅保留协议ID占位 |
| Angle | 倾角周期上报 | 范围外 | 当前硬件无倾角传感器，本阶段周期上报不携带Angle |
| Type=3 | 光感工作计划 | 范围外 | 当前不实现光感计划，平台侧不要下发type=3 |
| cns=2 | 双回路控制/上报 | 范围外 | 当前项目按单回路验收，平台侧按cns=1下发和验收 |

---

## 四、当前代码已知问题

### 🔴 需要修正后才能按 100% 提交审核

| # | 问题 | 文件 | 优先级 |
|---|------|------|--------|
| 1 | 关灯异常(10005)目前只有状态表占位，未接入实际采集源；开灯异常(10004)已接入 | `zk_alarm.c` | P1 |
| 2 | Keil输出产物缺失，烧录前需要重新构建生成`.bin/.hex/.map` | `MDK-ARM-8008000/out` | P1 |

### 🟡 需要完善

| # | 问题 | 文件 | 优先级 |
|---|------|------|--------|
| 5 | 10006灯杆倾斜仅保留协议ID占位；当前硬件无传感器，本阶段不实现 | `zk_alarm.c` | 范围外 |
| 6 | 周期上报不携带 `Angle`；当前硬件无倾角传感器，本阶段不实现 | `mqtt_zk_protocol.c` | 范围外 |

### 🟢 可优化

| # | 问题 | 状态 |
|---|------|------|
| 7 | 属性Flash持久化占用2个Flash页(1KB×2)，可接受 | ⬜ 维持现状 |
| 8 | OTA进度上报使用固定30/50/90节点，非实际下载百分比 | ⬜ 维持现状 |
| 9 | Plan每次变更做双页Flash擦写，10K次寿命对低频场景够用 | ⬜ 维持现状 |
| 10 | 日出日落计算依赖math.h浮点运算(无FPU)，查询频率低可接受 | ⬜ 维持现状 |

### ✅ 已修复（2026-05-19 及之前）

| # | 原问题 | 修复方式 |
|---|--------|----------|
| ~~1~~ | ~~Plan仅支持CT="W"，不支持R查询~~ | 实现nid/id/now/del全部查询操作 |
| ~~2~~ | ~~en=0禁用计划时销毁全部数据~~ | 保留原记录仅翻转en位 |
| ~~3~~ | ~~删除命令格式不匹配协议~~ | CT=W + DT.del=[1,3,5] 数组格式 |
| ~~4~~ | ~~now查询返回格式与协议不符~~ | 改为DT.now=cns，返回完整plan JSON |
| ~~5~~ | ~~结构体缺少packed属性~~ | 5个结构体全加__attribute__((packed)) |
| ~~6~~ | ~~cns_mask解析存储但不使用~~ | cns_mask已用于通道过滤匹配 |
| ~~7~~ | ~~缺少执行日志~~ | printf输出执行关键信息 |
| ~~8~~ | ~~优先级方向无注释~~ | 添加注释说明1最低~8最高 |
| ~~9~~ | ~~分钟匹配无容错窗口~~ | 增加+1分钟时间窗口 |
| ~~10~~ | ~~年循环(0xFFFF)闰年判断不正确~~ | 0xFFFF返回非闰年 |

### ✅ 已修复（2026-05-21 本轮新增）

| # | 原问题 | 修复方式 |
|---|--------|----------|
| ~~11~~ | ~~Signal信号强度使用简单估算（getSignal()），非AT查询真实值~~ | 改为`nb_get_rsrp_dbm10()` AT+QENG真实值，获取不到时不上报rsrp字段 |
| ~~12~~ | ~~EleInfo中的e(电能)字段当前固定填0~~ | e[0]上报当前电源故障位图`error_flag_byte` |
| ~~13~~ | ~~告警模块、运行时统计、属性管理全部堆积在mqtt_zk_protocol.c~~ | 抽离为独立文件：`zk_alarm.c`(227行)、`zk_runtime_stats.c`(259行)、`zk_property.c`(1271行) |
| ~~14~~ | ~~Factory工厂参数不支持远程读写~~ | 新增Factory属性组，支持读取(MID/SET_OUTCUR等)和写入，含范围校验 |
| ~~15~~ | ~~无NB信号采集与设备运行时长统计上报~~ | 新增`zk_runtime_stats`模块，RunTm/LightTm改用独立统计 |
| ~~16~~ | ~~SV="alam"告警配置读写尚未实现~~ | 新增`ZK_SV_ALAM`常量、`zk_handle_alam_message()`处理CT=R/W、Flash持久化、告警检测改用可配置阈值 |
| ~~17~~ | ~~RunTm/LightTm/Angle属性读取未支持~~ | 在`zk_add_property_to_dt()`中新增3个分支 |
| ~~18~~ | ~~告警阈值硬编码在zk_alarm.c中~~ | 改为从`zk_device_config_get()`读取，平台alam写入优先于出厂默认值 |

### ⚠️ 2026-05-22 新增项复核修正

| # | 原问题 | 2026-05-25复核结论 |
|---|--------|----------|
| 19 | Plan DO="sr"日出日落查询未实现 | 已修复：入口在`SV="plan"`/`CT="R"`/`DT.DO="sr"`，响应为`DT.sr`数组；`SV="rqst"`下不再处理该命令 |
| 20 | Plan不支持type=2(日出日落类型) | 已修复：`timetp=0`解析`time[]`，`timetp=1`解析`dTime[]`，查询回显也按`time/dTime`输出 |
| 21 | 告警状态表缺少10005/10006占位 | 状态表从8路扩展至10路已完成；10004开灯异常已接入，10005关灯异常仍需接入采集源，10006灯杆倾斜因无传感器不纳入当前范围 |

---

## 五、文件改动清单

| 文件 | 改动类型 | 行数 | 内容 |
|------|----------|------|------|
| `MDK-ARM-8008000/startup_stm32f103xe.s` | ✅ 修改 | — | 栈大小 0x400 → 0x800 |
| `Core/Src/LampProtocolLib/mqtt_zk_protocol.c` | ✅ 重构+增强 | 2467行 | CRC改为Modbus(0xA001)；属性读写入口；周期/变化上报；控制命令+last恢复；校时请求/响应；OTA入口；cJSON双池；发送忙处理；延迟队列；**告警配置读写(alam handler)**。告警/属性/运行时统计已抽离 |
| `Core/Src/LampProtocolLib/mqtt_zk_protocol.h` | ✅ 修改 | 186行 | 完整SV/CT/告警ID枚举；zk_device_config_t扩展（含almValue/almRecValue/almEn数组）；zk_mqtt_config_t定义；handler函数声明 |
| `Core/Src/LampProtocolLib/zk_property.c` | 🆕 新增→增强 | 1271行 | **设备属性管理模块**：Flash双副本持久化 + 14组属性读取分发表（**含RunTm/LightTm/Angle**）+ Factory属性读写 + 出厂恢复 + **告警阈值写入(zl_device_config_save_alam)** |
| `Core/Src/LampProtocolLib/zk_property.h` | 🆕 新增 | 9行 | 属性管理头文件（含zk_device_config_save_alam声明） |
| `Core/Src/LampProtocolLib/zk_alarm.c` | 🆕 新增→增强 | 247行 | **告警独立模块**：10种告警状态表 + pending/reported/one-shot状态机 + **阈值改为从zk_device_config_get()动态读取**。10004开灯异常已接入，10005关灯异常待接入采集源，10006灯杆倾斜仅保留占位且本阶段不实现 |
| `Core/Src/LampProtocolLib/zk_alarm.h` | 🆕 新增 | 15行 | 告警头文件 |
| `Core/Src/LampProtocolLib/zk_runtime_stats.c` | 🆕 新增 | 259行 | **运行时统计模块**：运行/亮灯秒级累加 + Flash历史持久化 |
| `Core/Src/LampProtocolLib/zk_runtime_stats.h` | 🆕 新增 | 27行 | 运行时统计头文件 |
| `Core/Src/LampProtocolLib/zk_protocol_internal.h` | 🆕 新增 | 25行 | **内部共享函数声明**：cJSON工具函数、RTC操作、属性组构建 |
| `Core/Src/LampProtocolLib/zk_work_plan.c` | ✅ 增强 | 1426行 | 工作计划已实现写入/查询(nid/id/now/del/DO=sr)/存储/执行引擎/cns过滤/分钟容错；Type=2已支持`timetp=0/time[]`和`timetp=1/dTime[]`；Type=3光感计划不属于当前范围 |
| `Core/Src/LampProtocolLib/zk_work_plan.h` | ✅ 新增 | 13行 | 工作计划头文件 |
| `Core/Src/LampProtocolLib/zk_sunriset.c` | 🆕 新增 | 165行 | **日出日落计算模块**：NOAA天文算法，基于经纬度+时区+RTC日期计算 |
| `Core/Src/LampProtocolLib/zk_sunriset.h` | 🆕 新增 | 14行 | 日出日落头文件 |
| `Core/Src/LampProtocolLib/Json_Protocol.c` | ✅ 修改 | 66行 | 已收口为中科JSON统一分发入口 |
| `Core/Src/LampProtocolLib/App.c` | ✅ 修改 | — | 周期上报逻辑改为中科格式 |
| `Core/Src/LampProtocolLib/NbDriver.h` | ✅ 修改 | — | 双槽接收缓冲、协议模式标识 |
| `Core/Src/LampProtocolLib/NbDriver.c` | ✅ 修改 | — | 中科MQTT连接、订阅、发布路径集成 |
| `Core/Src/LampProtocolLib/TcpClient.c` | ✅ 修改 | — | 连接成功后登录、断连重置会话、会话状态机驱动 |
| `Core/Src/LampProtocolLib/ota.c` | ✅ 修改 | — | OTA URL使用中科下发地址，进度上报接入中科格式 |
| `Core/Src/main.c` | ✅ 修改 | — | 集成`zk_runtime_stats_init()`、`zk_runtime_counter_process()` |
| `Core/Src/sys_data.h` | ✅ 修改 | — | 数据结构调整 |

---

## 六、CRC密码生成算法

**统一使用 CRC16-Modbus (反射多项式 0xA001, 初始值 0xFFFF)** ✅

```
输入: 15位IMEI字符串 (如 "864294053651521")
步骤:
  1. 将IMEI按5字节分为3段: "86429", "40536", "51521"
  2. 每段调用 crc16_modbus_get() 计算CRC16
  3. 每个CRC结果取反 (~crc)
  4. 按 第3段-第1段-第2段 顺序拼接为12字符的16进制密码

已有函数: Core/Src/crc16_modbus.c::crc16_modbus_get(u8* ptr, u8 len)

测试向量:
  IMEI=864294053651521 -> Password=BD9D0EE1D3E1
  IMEI=860608074646596 -> Password=FBE709622252
  IMEI=868120257654321 -> Password=707D3A606B13
```

---

## 七、关键协议常量参考

```c
// 服务器
#define ZK_MQTT_SERVER_IP       "47.120.15.220"
#define ZK_MQTT_SERVER_PORT     1883

// SV 服务类型
#define ZK_SV_PROP              "prop"    // 属性类
#define ZK_SV_CTRL              "ctrl"    // 控制类
#define ZK_SV_REPT              "rept"    // 上报类
#define ZK_SV_RQST              "rqst"    // 请求类
#define ZK_SV_OTA               "ota"     // 升级类
#define ZK_SV_PLAN              "plan"    // 工作计划类
#define ZK_SV_ALAM             "alam"    // 告警配置类 ✅ 已实现

// CT 操作类型(非上报类)
#define ZK_CT_READ              "R"       // 读
#define ZK_CT_WRITE             "W"       // 写

// CT 上报类型(上报类)
#define ZK_CT_LOGIN             "L"       // 登录上报
#define ZK_CT_HEARTBEAT         "H"       // 心跳上报
#define ZK_CT_CYCLIC            "C"       // 周期上报
#define ZK_CT_CHANGE            "B"       // 变化上报
#define ZK_CT_ALARM             "A"       // 告警上报

// CT OTA类型
#define ZK_CT_PROGRESS          "P"       // OTA进度
#define ZK_CT_ERROR             "E"       // OTA错误

// 告警ID
#define ZK_ALARM_OVER_VOLTAGE   10000     // 过压
#define ZK_ALARM_UNDER_VOLTAGE  10001     // 欠压
#define ZK_ALARM_OVER_CURRENT   10002     // 过流
#define ZK_ALARM_UNDER_CURRENT  10003     // 欠流
#define ZK_ALARM_LIGHT_ON_FAIL  10004     // 开灯异常
#define ZK_ALARM_LIGHT_OFF_FAIL 10005     // 关灯异常(待接入采集源)
#define ZK_ALARM_POLE_TILT      10006     // 灯杆倾斜(无传感器，本阶段不实现)
#define ZK_ALARM_LEAK_CURRENT   10007     // 灯杆漏电
#define ZK_ALARM_DEVICE_FAULT   10008     // 设备故障
#define ZK_ALARM_POWER_DOWN     10009     // 设备失电

// 错误码
#define ZK_ER_OK                0         // 成功
#define ZK_ER_NOT_SUPPORTED     1         // 命令不支持
#define ZK_ER_TYPE_ERROR        2         // 数据类型错误
#define ZK_ER_RANGE_ERROR       3         // 数据范围错误
#define ZK_ER_FIELD_ERROR       4         // 字段属性错误
#define ZK_ER_MISSING_FIELD     5         // 必要字段缺失
#define ZK_ER_ARRAY_BOUNDS      6         // 数组越界
#define ZK_ER_ARRAY_EMPTY       7         // 数组为空
#define ZK_ER_STRING_LEN        8         // 字符串长度错误
#define ZK_ER_LOGIC_ERROR       9         // 逻辑错误
#define ZK_ER_STORE_ERROR       10        // 参数存储错误
#define ZK_ER_DEVICE_BUSY       12        // 设备忙(OTA进行中)
#define ZK_ER_URL_INVALID       91        // URL无效
#define ZK_ER_FLASH_SAVE        99        // Flash存储失败
```

---

## 八、推荐实施顺序（当前阶段）

```
✅ 第1步:  修改栈大小 0x400 → 0x800
✅ 第2步:  修改CRC密码生成为Modbus(0xA001)
✅ 第3步:  实现属性读取框架 → DevInfo/RunSts/EleInfo/Signal/RTC等共13组
✅ 第4步:  实现周期上报(C) + 变化上报(B)
✅ 第5步:  实现OTA升级入口(SV="ota")
✅ 第6步:  实现属性写入框架 → Gis/Dim/Sense/Svr/RTC
✅ 第7步:  完善控制命令 → cnCtrl(last参数) + reboot + restore + patrol
✅ 第8步:  实现告警上报(CT="A") → 8种告警映射 + 硬件采集源接入
✅ 第9步:  实现主动校时(SV="rqst") + 登录响应校时
✅ 第10步: 实现属性Flash持久化 → 双副本+序列号
✅ 第11步: 实现cJSON RX/TX双池 → 内存隔离
✅ 第12步: 实现控制恢复机制(last参数定时恢复)
✅ 第13步: 实现发送忙处理 + 延迟响应队列
✅ 第14步: 实现工作计划(SV="plan") → 写入/存储/执行引擎
⚠️ 第15步: 实现Plan查询操作(CT="R") → nid / id / now / del 已实现；sr入口仍需按协议接入plan/R
✅ 第16步: 修复工作计划模块Bug → 禁用保留数据/删除协议对齐/结构体packed/分钟容错/cns过滤/执行日志
✅ 第17步: Signal改为AT查询真实值 → nb_get_rsrp_dbm10()替代getSignal()
✅ 第18步: EleInfo.e接入实际值 → 上报电源故障位图替代固定填0
✅ 第19步: Factory属性读写 → MID/SET_OUTCUR/HWMAX_OUTCUR等工厂参数
✅ 第20步: 模块重构 → 告警/属性/运行时统计抽离为独立文件
✅ 第21步: 运行时统计功能 → 运行/亮灯时长Flash持久化 + 周期上报
✅ 第22步: 补齐属性读取RunTm/LightTm/Angle → 3行代码
✅ 第23步: 实现告警配置读写(SV="alam") → 新handler + Flash存储 + 告警阈值可配置化
✅ 第24步: 实现日出日落查询(Plan DO="sr") → 已按`SV="plan"`接入，返回`DT.sr`数组
⚠️ 第25步: 接入关灯异常(10005)采集源 → 目前仅状态表占位，采集源未完成；10006灯杆倾斜无传感器，本阶段不实现
✅ 第26步: 实现Type=2日出日落计划支持 → 已支持`timetp=0/time[]`和`timetp=1/dTime[]`
```

---

## 九、资源预估（MDK-ARM-8008000工程）

| 项目 | 初版 | 当前值 | 预估新增 | 预估总量 | 安全阈值 | 状态 |
|------|------|--------|----------|----------|----------|------|
| ROM | ~60KB | ~74KB | ~1KB | ~75KB | <100KB | ✅ |
| RAM | ~30KB | ~33KB | ~1KB | ~34KB | <40KB | ✅ |
| Stack | 2KB (0x800) | 2KB | 不变 | 2KB | ≥2KB | ✅ |
| Heap | 512B (0x200) | 512B | 不变 | 512B | - | ✅ |

> 注：代码总量从~5579行增至~5830行。`mqtt_zk_protocol.c` 从2292行增至2467行（+175行，alam handler + DO="sr"），`zk_property.c` 从1217行增至1271行（+54行，告警字段+save函数），`zk_alarm.c` 从205行增至244行（+39行，状态表扩展至10路），`zk_work_plan.c` 从1237行增至1310行（+73行，Type=2支持），新增`zk_sunriset.c`(165行)。ROM从~72KB增至~76KB，仍在100KB安全阈值内。

---

## 十、实机验证指南

> 以下所有验证操作均需设备已烧录最新固件、4G模块已注册网络、已连接到MQTT服务器`47.120.15.220:1883`。
> 推荐使用 **MQTTX** 或 **MQTT Explorer** 作为桌面端测试工具。

### 10.0 验证前准备

#### 10.0.1 工具准备

| 工具 | 用途 | 获取方式 |
|------|------|----------|
| MQTTX | MQTT桌面客户端，用于收发协议报文 | https://mqttx.app |
| 串口调试助手 | 查看设备串口日志(115200bps) | 任意串口工具 |
| USB转TTL模块 | 连接设备调试串口 | 3.3V电平 |
| 可调交流电源 | 模拟过压/欠压告警 | 0~300V可调 |
| 万用表 | 测量实际电压/电流 | - |

#### 10.0.2 MQTTX连接配置

```
Broker: 47.120.15.220
Port: 1883
Client ID: test_tool_<随机数>
Username: (留空)
Password: (留空)
```

#### 10.0.3 订阅主题

首先订阅以下主题以监听设备上报：

| 主题 | 说明 |
|------|------|
| `MS/<IMEI>/dev2plt` | 设备→平台上行数据（主要观察） |
| `MS/<IMEI>/dev2pcp` | 设备→平台OTA数据 |
| `MS/<IMEI>/offline` | 设备离线遗嘱消息 |

> 将 `<IMEI>` 替换为设备的15位IMEI号（可查看设备外壳标签或串口启动日志）。

#### 10.0.4 发布主题

平台通过以下主题向设备下发指令：

| 主题 | 说明 |
|------|------|
| `MS/<IMEI>/plt2dev` | 平台→设备下行指令（属性读写、控制、校时等） |
| `MS/<IMEI>/pcp2dev` | 平台→设备OTA升级指令 |

---

### 10.1 登录验证 (CT="L")

**目的：** 验证设备上电后能正确发送登录报文，并收到平台登录确认。

**验证步骤：**

1. 给设备上电，等待约10~20秒（4G模块注册网络 + MQTT连接）
2. 在MQTTX中观察主题 `MS/<IMEI>/dev2plt`，应收到登录上报报文

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "L",
  "DT": {
    "DevInfo": {
      "protId": 100,
      "clas": "MS-SLC-01",
      "prottp": "iX7-075SC028-4G",
      "hver": 100,
      "sver": <APP_VERSION>,
      "mver": "BC28GJAR01A01",
      "iccid": "<20位ICCID>"
    },
    "MdlInfo": { "mdlId": "<IMEI>" },
    "Gis": { "lng": ..., "lat": ..., "zone": 8 },
    "Dim": [{ "cns": 1, "dim": 0, "sov": 0 }],
    "Sense": {}
  }
}
```

**验证通过标准：**
- ✅ 收到CT="L"的登录上报
- ✅ DT中包含DevInfo、MdlInfo、Gis、Dim、Sense五个属性组
- ✅ DevInfo.iccid为20位字符串
- ✅ MdlInfo.mdlId等于设备IMEI
- ✅ 设备串口日志显示"login sent"

---

### 10.2 心跳验证 (CT="H")

**目的：** 验证设备登录成功后每60秒发送心跳，平台回复ACK后设备维持在线。

**验证步骤：**

1. 确认设备已登录成功
2. 在MQTTX中等待约60秒，观察 `MS/<IMEI>/dev2plt`
3. 检查心跳报文的MID（消息ID）是否逐次递增

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "H"
}
```

**验证通过标准：**
- ✅ 每60秒±5秒收到一条CT="H"的心跳上报
- ✅ 心跳报文无DT节点，结构精简
- ✅ MID逐次递增不重复

---

### 10.3 周期上报验证 (CT="C")

**目的：** 验证设备按配置周期（默认300秒）上送完整运行数据。

**验证步骤：**

1. 确认设备已登录成功
2. 等待约5分钟（默认300秒周期），观察 `MS/<IMEI>/dev2plt`
3. 检查报文内容完整性

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "C",
  "DT": {
    "RunTm": { "tTmLt": ..., "rTmLt": ... },
    "LightTm": { "tLtTime": [...], "rLtTime": [...] },
    "RunSts": { "sts": [...], "bri": [...] },
    "EleInfo": {
      "e": [...], "c": [...], "v": [...],
      "f": [...], "p": [...], "rEc": [...], "tEc": [...]
    },
    "PerSts": { "lux": 0, "temp": ... },
    "Signal": { "rsrp": ..., "reg": 1 }
  }
}
```

**验证通过标准：**
- ✅ 每300秒±10秒收到一条CT="C"的周期上报
- ✅ 当前代码实际包含RunTm、LightTm、RunSts、EleInfo、PerSts、Signal；当前硬件无倾角传感器，Angle不纳入周期上报验收
- ✅ EleInfo.v(电压)、c(电流)、p(功率)数值在合理范围内
- ✅ Signal.rsrp来自AT+QENG真实值（非等级估算）

---

### 10.4 变化上报验证 (CT="B")

**目的：** 验证设备亮度或开关状态变化时，立即触发变化上报。

**验证步骤：**

1. 确认设备已登录且正常运行
2. 通过MQTTX向 `MS/<IMEI>/plt2dev` 发送调光指令改变亮度：
   ```json
   {
     "SN": "<IMEI>",
     "SV": "ctrl",
     "CT": "W",
     "DT": { "cnCtrl": [{ "cns": 1, "bri": 50, "last": 0 }] }
   }
   ```
3. 观察 `MS/<IMEI>/dev2plt`，应在调光完成后立即收到变化上报

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "B",
  "DT": {
    "RunSts": { "sts": [1], "bri": [50] },
    "Dim": [{ "cns": 1, "dim": 0, "sov": 0 }]
  }
}
```

**验证通过标准：**
- ✅ 亮度/开关变化后3秒内收到CT="B"的变化上报
- ✅ DT.RunSts.bri值与调光指令一致
- ✅ 设备串口日志出现"change report triggered"

---

### 10.5 属性读取验证 (SV="prop", CT="R")

**目的：** 验证平台可通过属性读取指令查询设备各类属性。

#### 10.5.1 读取设备信息 (DevInfo)

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["DevInfo"]}}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","ER":0,"DT":{"DevInfo":{"protId":100,"clas":"MS-SLC-01","prottp":"iX7-075SC028-4G","hver":100,"sver":<版本>,"mver":"BC28GJAR01A01","iccid":"<ICCID>"}}}
```

#### 10.5.2 读取Factory属性 (新增)

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["Factory"]}}
```

**预期收到回复：** DT中包含Factory（MID/SET_OUTCUR/HWMAX_OUTCUR等工厂参数）。

#### 10.5.3 读取运行状态 (RunSts) + 电气信息 (EleInfo) + 信号 (Signal)

**批量发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["RunSts","EleInfo","Signal"]}}
```

**预期收到回复：** DT中包含RunSts（sts/bri）、EleInfo（e/c/v/f/p/rEc/tEc/pwr/lc）、Signal（rsrp/snr/cellid/pci/reg）三个属性组。注意EleInfo.e数组不再固定为0。
> Signal.rsrp通过AT+QENG真实值获取；若获取不到则不出现rsrp字段。

#### 10.5.4 读取运行时间与姿态属性 (RunTm/LightTm/Angle)

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["RunTm"]}}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","ER":0,"DT":{"RunTm":{...},"LightTm":{...}}}
```
> RunTm/LightTm/Angle 已加入属性读取分发表。RunTm/LightTm 会同时返回运行时间组；Angle 目前无倾角传感器，属性读取可返回 x/y/z=0，周期上报不携带 Angle。

**验证通过标准：**
- ✅ 支持的14组属性均返回ER=0且数据完整（含新加Factory组）
- ✅ 不支持的属性返回ER=1
- ✅ items为空数组时返回ER=7

---

### 10.6 属性写入验证 (SV="prop", CT="W")

**目的：** 验证平台可通过属性写入指令修改设备配置，且配置持久化到Flash。

#### 10.6.1 写入Gis（地理位置）

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Gis": { "lng": 120123456, "lat": 30123456, "zone": 8 }
  }
}
```

**预期收到回复：** ER=0

**验证持久化：** 设备断电重启后，读取Gis属性，确认经纬度值已保存。

#### 10.6.2 写入Svr（服务器参数）

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Svr": { "svrIp": "47.120.15.220", "svrPort": 1883, "uPeriod": 600, "hPeriod": 120, "tPeriod": 7200 }
  }
}
```

**预期收到回复：** ER=0

#### 10.6.3 写入Factory参数 (新增)

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Factory": { "MID": 1, "SET_OUTCUR": 3800 }
  }
}
```

**预期收到回复：** ER=0（注意Factory参数需范围校验通过）

#### 10.6.4 范围校验验证

**发送超出范围的lng值：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Gis": { "lng": 200000000, "lat": 30123456 }
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"W","ER":3,"DT":{}}
```

**验证通过标准：**
- ✅ 正常值写入返回ER=0
- ✅ 超出范围返回ER=3
- ✅ 错误类型返回ER=2
- ✅ 断电重启后写入值不丢失（Flash持久化验证）

---

### 10.7 控制命令验证 (SV="ctrl", CT="W")

**目的：** 验证平台可通过控制指令远程调光、重启、恢复出厂设置、巡检。

#### 10.7.1 cnCtrl调光控制

**发送（立即调光到80%亮度，30分钟后恢复）：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": {
    "cnCtrl": [{ "cns": 1, "bri": 80, "last": 30 }]
  }
}
```

**验证步骤：**
1. 发送指令后，肉眼观察灯具亮度变为80%
2. 30分钟后，观察灯具亮度恢复到调光前的值
3. 期间应收到CT="B"的变化上报

**预期：**
- ✅ 灯具亮度立即变为80%
- ✅ ER=0响应
- ✅ 约30分钟后亮度自动恢复
- ✅ 亮度变化触发变化上报

#### 10.7.2 cnCtrl last=0（永久调光）

**发送（永久调到60%，不恢复）：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": {
    "cnCtrl": [{ "cns": 1, "bri": 60, "last": 0 }]
  }
}
```

**预期：**
- ✅ 灯具亮度变为60%
- ✅ 持续观察3分钟以上，亮度不恢复

#### 10.7.3 reboot重启

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": { "DO": "reboot" }
}
```

**预期：**
- ✅ 立即收到ER=0响应
- ✅ 约500ms后设备重启
- ✅ MQTT连接断开（收到offline遗嘱消息）
- ✅ 设备重启后自动重新连接MQTT并发送登录上报

#### 10.7.4 patrol巡检

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": { "DO": "patrol" }
}
```

**预期：**
- ✅ ER=0响应
- ✅ 随后立即收到一条CT="C"的周期上报（包含当前硬件可采集的运行数据）

---

### 10.8 告警上报验证 (CT="A")

**目的：** 验证设备在异常条件下能正确检测并上报告警。

#### 10.8.1 过压告警 (almId=10000)

**验证方法：**
1. 使用可调交流电源为设备供电
2. 将电压调至触发阈值以上
3. 观察MQTTX中 `MS/<IMEI>/dev2plt` 是否出现告警

> 注意：此验证涉及高压操作，请在确保安全的前提下进行，或使用信号发生器模拟电压采样值。

**预期报文：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "A",
  "DT": {
    "alm": {
      "almId": 10000,
      "almStatus": 1,
      "almValue": <实测电压>,
      "almThreshold": 3200
    }
  }
}
```

**验证通过标准：**
- ✅ 电压超过阈值后30秒内上报告警（almStatus=1）
- ✅ 电压恢复正常后上报恢复（almStatus=0）
- ✅ 同一告警状态不重复上报

#### 10.8.2 其他告警快速验证

| 告警ID | 告警名称 | 模拟方法 | 预期 |
|--------|----------|----------|------|
| 10001 | 欠压 | 调低电压至80V以下 | 上报almStatus=1 |
| 10002 | 过流 | 接入大功率负载 | 上报almStatus=1 |
| 10007 | 漏电 | 输出端对地接电阻 | 上报almStatus=1 |
| 10008 | 设备故障 | 加热NTC传感器 | 上报almStatus=1 |
| 10009 | 设备失电 | 断开交流供电 | 上报一次almStatus=1(one_shot) |

---

### 10.9 校时功能验证 (SV="rqst")

**目的：** 验证设备能主动请求校时，且平台响应后正确校准RTC。

#### 10.9.1 主动校时请求

等待约1小时（默认3600秒周期），观察 `MS/<IMEI>/dev2plt`：

**预期报文：**
```json
{
  "SN": "<IMEI>",
  "SV": "rqst",
  "CT": "R",
  "DT": { "DO": "TmCali" }
}
```

#### 10.9.2 平台响应校时

**在MQTTX中回复（模拟平台）：**
```json
{
  "SN": "<IMEI>",
  "SV": "rqst",
  "CT": "R",
  "ER": 0,
  "DT": {
    "TmCali": { "time": "2026-05-21 15:30:00" }
  }
}
```

**验证通过标准：**
- ✅ 约3600秒后设备自动发送TmCali请求
- ✅ 收到校时响应后设备RTC被校准
- ✅ 登录响应中的time与RTC相差>30秒时自动覆盖

---

### 10.10 OTA升级验证 (SV="ota")

**目的：** 验证设备能接收OTA指令、下载固件、上报进度、完成升级。

#### 10.10.1 下发OTA指令

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "ota",
  "CT": "W",
  "DT": {
    "url": "http://192.168.1.100:8080/firmware_v1.2.3.bin"
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"ota","CT":"W","ER":0,"DT":{}}
```

#### 10.10.2 观察OTA进度

在 `MS/<IMEI>/dev2pcp` 主题观察进度上报：

```json
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":30}}
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":50}}
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":90}}
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":100}}
```

**验证通过标准：**
- ✅ 收到OTA指令后ER=0响应
- ✅ 进度上报30→50→90→100依次出现
- ✅ 下载完成后设备自动重启进入bootloader
- ✅ OTA失败时上报CT="E"错误
- ✅ 无效URL返回ER=91

---

### 10.11 工作计划验证 (SV="plan")

**目的：** 验证设备能接收工作计划、查询计划、删除计划、持久化存储、按时间表自动执行调光。

#### 10.11.1 下发周计划

**发送（每周一至周五 08:00亮度80%、18:00亮度30%）：**
```json
{
  "SN": "<IMEI>",
  "SV": "plan",
  "CT": "W",
  "DT": {
    "plan": {
      "id": 1,
      "en": 1,
      "type": 1,
      "priority": 5,
      "sDate": "2026-01-01 00:00:00",
      "eDate": "2026-12-31 23:59:59",
      "week": "1111100",
      "jobs": [
        {
          "cns": [1],
          "timetp": 0,
          "time": ["08:00", "18:00"],
          "bri": [80, 30]
        }
      ]
    }
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","ER":0,"DT":{}}
```

#### 10.11.2 查询有效计划列表 (nid)

**发送：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"R","DT":{"DO":"nid"}}
```

**预期回复（假设有id=1和id=3两个启用计划）：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"R","ER":0,"DT":{"nid":[1,3]}}
```

#### 10.11.3 查询指定计划内容 (id)

**发送：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"R","DT":{"id":1}}
```

**预期回复：** 返回id=1的完整计划JSON（含sDate/eDate/week/jobs等全部字段）。

#### 10.11.4 查询当前运行计划 (now)

**发送（查询通道1当前执行的计划）：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"R","DT":{"now":1}}
```

**预期回复（当前有匹配计划时）：** 返回完整plan JSON。当前无匹配时返回空DT。

#### 10.11.5 删除计划 (del)

**发送（批量删除id=1和id=3）：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","DT":{"del":[1,3]}}
```

**预期回复：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","ER":0,"DT":{}}
```

#### 10.11.6 禁用计划验证 (en=0)

**发送（禁用id=1计划，仅需id和en两个字段）：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","DT":{"plan":{"id":1,"en":0}}}
```

**预期回复：** ER=0

**验证配置保留：**
```json
// 查询id=1的计划内容
{"SN":"<IMEI>","SV":"plan","CT":"R","DT":{"id":1}}
// 预期返回: {"plan":{"id":1,"en":0}} — 其他字段在内部保留
```

#### 10.11.7 验证计划自动执行

1. 确认设备RTC时间正确
2. 设置计划时间点为当前时间+2分钟
3. 到时间观察灯具亮度变化
4. 串口日志应输出：`[plan] exec id=1 job=0 act=0 bri=80`

**验证通过标准：**
- ✅ 写入计划返回ER=0
- ✅ 设备断电重启后计划不丢失
- ✅ 到计划时间±1分钟内，灯具亮度自动变化（含+1分钟容错）
- ✅ 多个计划同时生效时，priority值大的优先执行
- ✅ 同一分钟内同一计划不重复执行
- ✅ nid/id/now/del查询全部返回正确数据
- ✅ en=0禁用后计划不执行，但内部配置保留
- ✅ del删除后计划不再出现在nid列表中

#### 10.11.8 日出日落查询 (DO="sr") ✅ 已按协议实现

| 操作 | 请求示例 | 当前结果 |
|------|----------|----------|
| 协议要求 | `{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"sr"}}` | `zk_work_plan.c` 已处理，返回ER=0 + `DT.sr=["06:20","18:35"]` |
| 非协议入口 | `{"SN":"...","SV":"rqst","CT":"R","DT":{"DO":"sr"}}` | 不再处理，避免平台误判为`rqst`能力 |

> DO="sr" 的 NOAA 天文算法已实现，并已在 `SV="plan"` 下闭环。若经纬度或RTC未就绪，仍会按错误响应处理，需要实机校时和Gis参数验证。

---

### 10.12 Flash持久化验证

**目的：** 验证设备配置在断电重启后正确保留。

**验证步骤：**

1. 通过属性写入修改Gis（经纬度）和Svr（上报周期）
2. 确认写入成功（ER=0）
3. 读取Gis和Svr，确认值已更新
4. 断电重启，重新上电等待登录
5. 读取Gis和Svr属性

**验证通过标准：**
- ✅ 重启后Gis和Svr值与写入值一致
- ✅ 周期上报按新周期执行
- ✅ 工作计划断电不丢失
- ✅ Factory工厂参数断电不丢失

---

### 10.13 CRC密码验证

**目的：** 验证设备生成的MQTT密码符合中科协议规范。

**验证方法（通过串口日志或测试脚本）：**
```bash
cd tests
python3 test_login_validation.py
```

**测试向量：**
| IMEI | 预期密码 |
|------|----------|
| 864294053651521 | BD9D0EE1D3E1 |
| 860608074646596 | FBE709622252 |
| 868120257654321 | 707D3A606B13 |

**验证通过标准：**
- ✅ 三个测试向量全部匹配
- ✅ MQTT服务器接受设备连接（说明密码认证通过）

---

### 10.14 端到端集成验证流程

**目的：** 一次性验证设备完整生命周期。

**完整验证流程（约30分钟）：**

| 步骤 | 操作 | 观察点 | 时长 |
|------|------|--------|------|
| 1 | 设备上电 | 串口日志：IMEI读取、4G注册、MQTT连接 | ~20秒 |
| 2 | 自动登录 | MQTTX收到CT="L"登录上报 | 即时 |
| 3 | 首次心跳 | MQTTX收到CT="H"心跳 | ~60秒 |
| 4 | 写入配置 | 修改Gis/Svr/Factory属性，ER=0 | 即时 |
| 5 | 调光指令 | 下发cnCtrl bri=50，灯具变暗 | 即时 |
| 6 | 变化上报 | 收到CT="B"上报 | <3秒 |
| 7 | 恢复验证 | 等待last分钟数后亮度恢复 | 按参数 |
| 8 | 断电重启 | 断开电源后重新上电 | ~30秒 |
| 9 | 持久化验证 | 读取Gis/Svr，确认值保留 | 即时 |
| 10 | 周期上报 | 收到CT="C"完整周期数据（含AT真实RSRP） | ~5分钟 |
| 11 | 下发计划 | 写入周计划，到点自动执行 | 按计划时间 |
| 12 | 查询计划 | nid/id/now查询，确认数据正确 | 即时 |
| 13 | 删除计划 | del删除，nid确认已移除 | 即时 |
| 14 | 巡检指令 | 下发patrol，收到即时周期数据 | 即时 |
| 15 | OTA升级 | 下发OTA URL，观察进度→跳boot | ~2分钟 |

**最终验收标准：**
- ✅ 设备从冷启动到成功登录 < 30秒
- ✅ 心跳60秒间隔稳定 ±5秒
- ✅ 周期上报内容完整、数值合理（RSRP为AT真实值，EleInfo.e为实际故障位图）
- ✅ 属性读写双向通信正常（14组读 + 5组写，含Factory组）
- ✅ 控制指令响应正确、灯光变化符合预期
- ✅ 告警检测灵敏、不误报不漏报
- ✅ 配置断电重启不丢失
- ✅ OTA升级流程完整可用
- ✅ 计划写入/查询/删除/自动执行全链路正常

---

### 10.15 常见问题排查

| 现象 | 可能原因 | 排查方法 |
|------|----------|----------|
| 设备不上报登录 | IMEI未正确读取或SIM卡未注册 | 检查串口日志IMEI打印；确认SIM卡有流量 |
| 登录报ER≠0 | CRC密码错误 | 用测试向量验证crc16_modbus输出 |
| 心跳中断 | 网络波动或服务器断开 | 检查4G信号质量；观察设备是否自动重连 |
| 属性读取返回ER=1 | 该属性组未加入分发表 | 检查属性名是否拼写正确 |
| 配置重启丢失 | Flash写入失败 | 检查串口是否有"Flash save error"日志 |
| 计划未按时执行 | RTC时间不准或计划不在有效期内 | 读取RTC属性确认时间；检查计划en/start/end/week/type |
| 日出日落查询失败 | 经纬度未配置或RTC未就绪 | 检查Gis属性(lng/lat/zone)；确认RTC时间已同步 |
| OTA下载失败 | URL不可达或网络问题 | 确认HTTP服务器可达；检查URL格式 |
| 告警不触发 | 阈值不匹配或采集源未接入 | 检查对应almId的threshold配置；10005/10006为预留 |

---

**文档更新时间：** 2026-05-22
**基于：** 重构开发方案v2.0.md + 中科协议.docx + 当前代码(commit acc4810)交叉复核
