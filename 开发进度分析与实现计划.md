# 中科MQTT协议开发进度分析与实现计划

> 基于「重构开发方案v2.0.md」与「中科协议.docx」交叉复核
> 目标工程：`MDK-ARM-8008000`
> 统一服务器：`47.120.15.220:1883`
> CRC校验：统一使用 **CRC16-Modbus (0xA001)** ✅ 已修正

---

## 一、总体进度：约 90%

```
已完成   ██████████████████████████░░  90%
未完成   ██████░░░░░░░░░░░░░░░░░░░░  10%
```

---

## 二、已完成项

### 2.1 基础架构（第一阶段）

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 数据结构定义 | `mqtt_zk_protocol.h` | ✅ 95% | `zk_mqtt_config_t`、`zk_device_config_t`、`zk_message_header_t`。SV/CT枚举、10种告警ID枚举均已定义。新增svrIp/svrPort/uPeriod/hPeriod/tPeriod/commMain/commSub/commSAuto/spreadOffset/spreadWindow/spreadInterval/dimTp字段 |
| CRC密码生成 | `mqtt_zk_protocol.c` | ✅ 100% | **CRC16-Modbus(0xA001)**，调用`crc16_modbus_get()` |
| MQTT连接参数 | `NbDriver.c` | ✅ 100% | 全部切换到ZK函数，IMEI认证，plt2dev+pcp2dev双主题订阅 |
| 栈大小 | `MDK-ARM-8008000/startup_stm32f103xe.s:33` | ✅ 100% | `Stack_Size EQU 0x800` (2KB) |

### 2.2 协议处理层（第二阶段）

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| JSON消息处理 | `mqtt_zk_protocol.c` | ✅ 95% | 消息头解析、登录/心跳/周期/变化/告警报文构建、属性读写、控制命令、校时请求、OTA入口均已实现 |
| 协议入口路由 | `Json_Protocol.c:766-807` | ✅ 95% | 已处理prop(R/W)、ctrl(W)、rqst(R)、ota(W)、plan(W)；**alam路由尚未接入** |
| cJSON双池内存管理 | `mqtt_zk_protocol.c` | ✅ 100% | RX池4KB + TX池4KB，8字节对齐，线性分配器，TX耗尽日志告警 |

### 2.3 上报机制（第三阶段）

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 登录上报(L) | `mqtt_zk_protocol.c` | ✅ 100% | DevInfo+MdlInfo+Gis+Dim+Sense，与协议要求一致 |
| 心跳上报(H) | `mqtt_zk_protocol.c` | ✅ 100% | 60秒间隔（可配置），无DT节点，与协议要求一致 |
| 周期上报(C) | `mqtt_zk_protocol.c` | ✅ 100% | RunTm+LightTm+RunSts+EleInfo+PerSts+Signal+Angle，默认300秒间隔 |
| 变化上报(B) | `mqtt_zk_protocol.c` | ✅ 100% | 亮度/开关变化时触发，通过`zk_notify_state_changed()`设置标志 |
| 告警上报(A) | `mqtt_zk_protocol.c` | ✅ 95% | 8种告警已接入硬件采集源（电压/电流/漏电/温度/失电），带pending/reported/one-shot状态机；灯杆倾斜(10006)/关灯异常(10005)未接入采集源 |

### 2.4 属性读写

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 属性读取框架 | `mqtt_zk_protocol.c` | ✅ 90% | 支持DevInfo/MdlInfo/Gis/Dim/Sense/RunSts/EleInfo/PerSts/Signal/RTC/Svr/Comm/Spread共13组；**RunTm/LightTm/Angle未加入属性读取分发表** |
| 属性写入框架 | `mqtt_zk_protocol.c` | ✅ 100% | 支持Gis/Dim/Sense/Svr/RTC写入，含范围校验和错误码返回；**写入值已通过Flash双副本持久化，重启不丢失** |
| 属性Flash持久化 | `mqtt_zk_protocol.c` | ✅ 100% | `zk_property_flash_store_config()`双副本+序列号方案，main+backup互为冗余，magic=0x5A4B5052 |

### 2.5 控制命令

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| cnCtrl调光 | `mqtt_zk_protocol.c` | ✅ 100% | 支持cns=1单回路、last持续时间、亮度/开关控制、多item批量处理；带范围校验 |
| cnCtrl恢复机制 | `mqtt_zk_protocol.c` | ✅ 100% | last秒数到期后自动恢复原亮度，`zk_control_restore_process()`定时器驱动 |
| reboot重启 | `mqtt_zk_protocol.c` | ✅ 100% | 先响应ER=0，延迟500ms后NVIC_SystemReset() |
| restore恢复 | `mqtt_zk_protocol.c` | ✅ 100% | 支持0/1/5/6四种恢复类型（0/1/6恢复出厂配置到Flash，5清零电能），2/3/4返回不支持 |
| patrol巡检 | `mqtt_zk_protocol.c` | ✅ 100% | 响应ER=0后延迟上报完整周期数据 |

### 2.6 校时功能

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 主动校时请求 | `mqtt_zk_protocol.c` | ✅ 100% | SV="rqst", CT="R", DT.DO="TmCali"，默认3600秒周期 |
| 校时响应处理 | `mqtt_zk_protocol.c` | ✅ 100% | 解析TmCali响应，覆盖RTC |
| 服务器时间校准 | `mqtt_zk_protocol.c` | ✅ 100% | 登录响应中解析time字段，相差>30秒时覆盖RTC |

### 2.7 OTA升级

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| OTA指令接收 | `mqtt_zk_protocol.c` | ✅ 100% | SV="ota", CT="W", DT.url解析，提取文件名，触发HTTP下载 |
| OTA进度上报 | `mqtt_zk_protocol.c`、`ota.c` | ✅ 90% | CT="P"上报API，30/50/90/100进度节点；支持pending队列延迟发送 |
| OTA错误上报 | `mqtt_zk_protocol.c`、`ota.c` | ✅ 90% | CT="E"上报API，支持pending队列和挂起重试 |
| OTA URL提取 | `mqtt_zk_protocol.c` | ✅ 100% | `zk_get_ota_url()`供ota.c获取中科下发URL |

### 2.8 工作计划 (SV="plan") — 部分完成

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| Plan写入/更新 | `zk_work_plan.c` | ✅ 100% | `zk_handle_plan_message()`处理CT="W"，`zk_plan_upsert()`支持新增/覆盖/禁用 |
| Plan禁用 | `zk_work_plan.c` | ✅ 100% | 支持`en=0`下发禁用计划，执行引擎检查`en!=1`时自动跳过 |
| Plan Flash持久化 | `zk_work_plan.c` | ✅ 100% | 双副本+magic=0x5A4B504C，plan存储区0x08006000/0x08007800，最多8个计划 |
| Plan执行引擎 | `zk_work_plan.c` | ✅ 100% | Type=1(周日期)计划，按分钟匹配action，优先级仲裁(priority值越大越优先)，防重复执行 |
| 主循环集成 | `main.c` | ✅ 100% | `zk_work_plan_init()`初始化，`zk_work_plan_process()`每循环调用 |
| 有效计划查询(`DO:"nid"`) | — | ❌ 0% | 应返回所有valid=1且en=1的计划ID列表 |
| 计划内容查询(`id`) | — | ❌ 0% | 应返回指定id的完整计划内容(含jobs/actions) |
| 当前运行查询(`DO:"now"`) | — | ❌ 0% | 应返回当前时刻正在执行的计划及其action |
| 日出日落查询(`DO:"sr"`) | — | ❌ 0% | 需天文算法(NOAA公式)，基于经纬度计算日出日落时间 |
| 计划删除(`del`) | — | ❌ 0% | 应将指定id的计划置为invalid=0并写Flash |

> **现状说明：** `zk_handle_plan_message()`（`zk_work_plan.c:735`）只处理`CT="W"`。对于`CT="R"`的查询请求，函数返回`BOOL_FALSE`，在`Json_Protocol.c`中落入兜底逻辑返回**ER=1（命令不支持）**。平台无法查询设备上存储了哪些计划、某个计划的详情、或当前正在执行哪个计划。但执行引擎本身逻辑正确——即使平台无法查询，设备仍会自动按已存储的计划执行调光。

### 2.9 告警处理增强 — 新完成

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 硬件采集源接入 | `mqtt_zk_protocol.c` | ✅ 100% | 过压(ac_voltage_8209)、欠压(ac_voltage_8209)、过流(Io_value)、欠流(Io_value)、开灯异常(Po_value)、漏电(dangeo_out)、设备故障(ntc温度)、设备失电(power_drop)均已接入 |
| 告警状态机 | `mqtt_zk_protocol.c` | ✅ 100% | pending/pending_status/reported/one_shot四状态跟踪，防重复上报 |
| 告警阈值动态计算 | `mqtt_zk_protocol.c` | ✅ 100% | 过流阈值按额定电流150%计算，欠流按80%，温度阈值按NTC读数 |

### 2.10 关键集成点

| 集成点 | 文件 | 状态 |
|--------|------|------|
| MQTT连接成功后自动发送登录包 | `TcpClient.c` | ✅ |
| 断连自动重置会话 | `TcpClient.c` | ✅ |
| 会话状态机(登录重发/心跳/周期上报/校时/告警/OTA挂起上报/plan执行) | `zk_mqtt_session_process()` | ✅ |
| 服务器时间校准(相差>30秒时覆盖) | `mqtt_zk_protocol.c` | ✅ |
| 控制恢复机制(last参数) | `mqtt_zk_protocol.c` | ✅ |
| 发送忙自动清理(连续3次失败后复位) | `mqtt_zk_protocol.c` | ✅ |
| 延迟响应队列(response/OTA progress/OTA error pending) | `mqtt_zk_protocol.c` | ✅ |

---

## 三、未完成项（按优先级排序）

### P1 — 核心功能补充

#### 3.1 告警配置读写 (SV="alam")

当前SV_ALAM常量尚未定义，Json_Protocol.c路由未接入。

```json
// 读取
{"SN":"...","SV":"alam","CT":"R","DT":{"alms":[10000,10001]}}
// 响应
{"SN":"...","SV":"alam","CT":"R","ER":0,"DT":{"alms":[{"almId":10000,"almValue":2740,"recValue":2640,"almEn":1}]}}

// 写入
{"SN":"...","SV":"alam","CT":"W","DT":{"alms":[{"almId":10000,"almValue":2740,"recValue":2640,"almEn":1}]}}
```

#### 3.2 工作计划查询与删除操作 (SV="plan", CT="R")

当前`zk_handle_plan_message()`仅处理CT="W"，CT="R"的全部操作尚未实现。详解如下：

##### 3.2.1 有效计划查询 (DO:"nid")

返回设备中所有 valid=1 且 en=1 的计划ID列表。

```json
// 请求
{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"nid"}}
// 响应
{"SN":"...","SV":"plan","CT":"R","ER":0,"DT":{"nid":[1,3,5]}}
```

实现方式：遍历`zk_plan_store.plans[]`，筛选`valid==1 && en==1`的plan，收集id到JSON数组。

##### 3.2.2 计划内容查询 (id)

返回指定id的完整计划内容。

```json
// 请求
{"SN":"...","SV":"plan","CT":"R","DT":{"id":1}}
// 响应
{"SN":"...","SV":"plan","CT":"R","ER":0,"DT":{"plan":{...完整计划JSON...}}}
// 计划不存在
{"SN":"...","SV":"plan","CT":"R","ER":1,"DT":{}}
```

实现方式：按id索引`zk_plan_store.plans[id-1]`，校验valid==1，将`zk_plan_record_t`结构体逆向序列化为JSON。

##### 3.2.3 当前运行查询 (DO:"now")

返回当前时刻正在执行的计划及action。

```json
// 请求
{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"now"}}
// 有运行中的计划
{"SN":"...","SV":"plan","CT":"R","ER":0,"DT":{"now":{"id":1,"jobIndex":0,"actionIndex":1,"bri":80,"priority":5}}}
// 当前无计划运行
{"SN":"...","SV":"plan","CT":"R","ER":0,"DT":{}}
```

实现方式：复用`zk_work_plan_process()`中的匹配逻辑，但不执行调光，仅返回匹配结果。

##### 3.2.4 日出日落查询 (DO:"sr")

基于设备经纬度计算当日日出日落时间。需实现**NOAA天文算法**。

```json
// 请求
{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"sr"}}
// 响应
{"SN":"...","SV":"plan","CT":"R","ER":0,"DT":{"sr":{"sunrise":"06:15","sunset":"18:42"}}}
```

实现要点：
- 输入：设备当前经纬度（取自`zk_dev_cfg.lng/lat`）、当前日期、时区（`zk_dev_cfg.zone`）
- 算法：NOAA太阳高度角公式，计算太阳天顶角=90°50'（民用晨光始）的时刻
- 此查询用于平台按日出日落自动生成type=2计划的时间参考

##### 3.2.5 计划删除 (DO:"del")

删除指定id的计划（置valid=0并写Flash）。

```json
// 请求
{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"del","id":1}}
// 响应
{"SN":"...","SV":"plan","CT":"R","ER":0,"DT":{}}
// 计划不存在
{"SN":"...","SV":"plan","CT":"R","ER":1,"DT":{}}
```

实现方式：校验id对应plan的valid==1，置valid=0，调用`zk_plan_save_store()`写Flash。

> **实现顺序建议：** nid → del → id → now → sr。前两者实现最简单（只需遍历/标记），id需要逆向JSON序列化，now需复用执行引擎匹配逻辑，sr需要完整的天文算法实现。

### P2 — 完善项

#### 3.3 属性读取补充

RunTm、LightTm、Angle三组属性已在周期/变化上报中构建，但未加入`zk_add_property_to_dt()`属性读取分发表。平台下发prop读请求时这三组返回不支持(ER=1)。

#### 3.4 Signal信号质量

当前RSRP通过`getSignal()`简单估算（2格=-70dBm，1格=-90dBm，0格=-110dBm），需改为AT查询真实值。

#### 3.5 EleInfo电能字段

EleInfo中的e(电能)字段当前固定填0，需接入BL0942电能计量芯片的实际读数。

#### 3.6 告警阈值可配置化

当前告警阈值（过压3200、欠压800等）硬编码在代码中，alam功能完成后需从平台同步。

---

## 四、当前代码已知问题

### 🟡 需要完善

| # | 问题 | 文件 |
|---|------|------|
| 1 | RunTm/LightTm/Angle已在上报中生成，但属性读取未支持 | `mqtt_zk_protocol.c` |
| 2 | RTC默认fallback时间硬编码为`"2026-04-02 08:30:30"` | `mqtt_zk_protocol.c` |
| 3 | Signal信号强度使用简单估算，非AT查询真实值 | `mqtt_zk_protocol.c` |
| 4 | EleInfo中的e(电能)字段当前固定填0 | `mqtt_zk_protocol.c` |
| 5 | Plan仅支持CT="W"写入，不支持R查询(nid/id/now/sr/del)。平台无法查询设备上的计划列表/详情/当前运行状态 | `zk_work_plan.c` |
| 6 | SV="alam"告警配置读写尚未实现 | 多个文件 |
| 7 | 告警阈值(过压3200/欠压800等)硬编码，未从平台同步 | `mqtt_zk_protocol.c` |
| 8 | 灯杆倾斜(10006)/关灯异常(10005)告警ID已定义但未接入采集源 | `mqtt_zk_protocol.c` |

### 🟢 可优化

| # | 问题 |
|---|------|
| 9 | 属性Flash持久化占用2个Flash页(1KB×2)，可接受 |
| 10 | `Json_Protocol.c` 已收口为中科 JSON 分发入口，旧示例上传代码已移除 |
| 11 | OTA进度上报使用固定30/50/90节点，非实际下载百分比 |

---

## 五、文件改动清单

| 文件 | 改动类型 | 内容 |
|------|----------|------|
| `MDK-ARM-8008000/startup_stm32f103xe.s` | ✅ 修改 | 栈大小 0x400 → 0x800 |
| `Core/Src/LampProtocolLib/mqtt_zk_protocol.c` | ✅ 重大修改 | ~3249行：CRC改为Modbus(0xA001)；属性读写+Flash持久化；周期/变化/告警上报(硬件源接入)；控制命令+last恢复；校时请求/响应；OTA入口+进度/错误API；cJSON双池；告警状态机；发送忙处理；延迟队列 |
| `Core/Src/LampProtocolLib/mqtt_zk_protocol.h` | ✅ 重大修改 | 完整SV/CT/告警ID枚举；zk_device_config_t扩展(新增svrIp/dimTp/comm/spread等字段)；handler函数声明 |
| `Core/Src/LampProtocolLib/zk_work_plan.c` | ✅ 新增 | ~912行：工作计划完整实现(写入/存储/执行引擎) |
| `Core/Src/LampProtocolLib/zk_work_plan.h` | ✅ 新增 | 工作计划头文件 |
| `Core/Src/LampProtocolLib/Json_Protocol.c` | ✅ 修改 | SV路由分支(prop/ctrl/rqst/ota/plan)、旧协议隔离 |
| `Core/Src/LampProtocolLib/App.c` | ✅ 修改 | 周期上报逻辑改为中科格式 |
| `Core/Src/LampProtocolLib/NbDriver.h` | ✅ 修改 | 双槽接收缓冲、协议模式标识 |
| `Core/Src/LampProtocolLib/NbDriver.c` | ✅ 修改 | 中科MQTT连接、订阅、发布路径集成 |
| `Core/Src/LampProtocolLib/TcpClient.c` | ✅ 修改 | 连接成功后登录、断连重置会话、会话状态机驱动 |
| `Core/Src/LampProtocolLib/ota.c` | ✅ 修改 | OTA URL使用中科下发地址，进度上报接入中科格式 |
| `Core/Src/main.c` | ✅ 修改 | 集成zk_work_plan_init()和zk_work_plan_process() |
| `Core/Src/sys_data.h` | ✅ 修改 | 数据结构调整 |

---

## 六、CRC密码生成算法

**统一使用 CRC16-Modbus (反射多项式 0xA001, 初始值 0xFFFF)** ✅

```
输入: 15位IMEI字符串 (如 "864294053651521")
步骤:
  1. 将IMEI按5字节分为3段: "86429", "40536", "51521"
  2. 每段调用 crc16_modbus_get() 计算CRC16
  3. 每个CRC结果取反 (~crc)
  4. 按 第3段-第1段-第2段 顺序拼接为12字符的16进制密码

已有函数: Core/Src/crc16_modbus.c::crc16_modbus_get(u8* ptr, u8 len)

测试向量:
  IMEI=864294053651521 -> Password=BD9D0EE1D3E1
  IMEI=860608074646596 -> Password=FBE709622252
  IMEI=868120257654321 -> Password=707D3A606B13
```

---

## 七、关键协议常量参考

```c
// 服务器
#define ZK_MQTT_SERVER_IP       "47.120.15.220"
#define ZK_MQTT_SERVER_PORT     1883

// SV 服务类型
#define ZK_SV_PROP              "prop"    // 属性类
#define ZK_SV_CTRL              "ctrl"    // 控制类
#define ZK_SV_REPT              "rept"    // 上报类
#define ZK_SV_RQST              "rqst"    // 请求类
#define ZK_SV_OTA               "ota"     // 升级类
#define ZK_SV_PLAN              "plan"    // 工作计划类
// 待实现: ZK_SV_ALAM "alam"

// CT 操作类型(非上报类)
#define ZK_CT_READ              "R"       // 读
#define ZK_CT_WRITE             "W"       // 写

// CT 上报类型(上报类)
#define ZK_CT_LOGIN             "L"       // 登录上报
#define ZK_CT_HEARTBEAT         "H"       // 心跳上报
#define ZK_CT_CYCLIC            "C"       // 周期上报
#define ZK_CT_CHANGE            "B"       // 变化上报
#define ZK_CT_ALARM             "A"       // 告警上报

// CT OTA类型
#define ZK_CT_PROGRESS          "P"       // OTA进度
#define ZK_CT_ERROR             "E"       // OTA错误

// 告警ID
#define ZK_ALARM_OVER_VOLTAGE   10000     // 过压
#define ZK_ALARM_UNDER_VOLTAGE  10001     // 欠压
#define ZK_ALARM_OVER_CURRENT   10002     // 过流
#define ZK_ALARM_UNDER_CURRENT  10003     // 欠流
#define ZK_ALARM_LIGHT_ON_FAIL  10004     // 开灯异常
#define ZK_ALARM_LIGHT_OFF_FAIL 10005     // 关灯异常(尚未接入采集源)
#define ZK_ALARM_POLE_TILT      10006     // 灯杆倾斜(尚未接入采集源)
#define ZK_ALARM_LEAK_CURRENT   10007     // 灯杆漏电
#define ZK_ALARM_DEVICE_FAULT   10008     // 设备故障
#define ZK_ALARM_POWER_DOWN     10009     // 设备失电

// 错误码
#define ZK_ER_OK                0         // 成功
#define ZK_ER_NOT_SUPPORTED     1         // 命令不支持
#define ZK_ER_TYPE_ERROR        2         // 数据类型错误
#define ZK_ER_RANGE_ERROR       3         // 数据范围错误
#define ZK_ER_FIELD_ERROR       4         // 字段属性错误
#define ZK_ER_MISSING_FIELD     5         // 必要字段缺失
#define ZK_ER_ARRAY_BOUNDS      6         // 数组越界
#define ZK_ER_ARRAY_EMPTY       7         // 数组为空
#define ZK_ER_STRING_LEN        8         // 字符串长度错误
#define ZK_ER_LOGIC_ERROR       9         // 逻辑错误
#define ZK_ER_STORE_ERROR       10        // 参数存储错误
#define ZK_ER_DEVICE_BUSY       12        // 设备忙(OTA进行中)
#define ZK_ER_URL_INVALID       91        // URL无效
#define ZK_ER_FLASH_SAVE        99        // Flash存储失败
```

---

## 八、推荐实施顺序（当前阶段）

```
✅ 第1步: 修改栈大小 0x400 → 0x800
✅ 第2步: 修改CRC密码生成为Modbus(0xA001)
✅ 第3步: 实现属性读取框架 → DevInfo/RunSts/EleInfo/Signal/RTC等
✅ 第4步: 实现周期上报(C) + 变化上报(B)
✅ 第5步: 实现OTA升级入口(SV="ota")
✅ 第6步: 实现属性写入框架 → Gis/Dim/Sense/Svr/RTC
✅ 第7步: 完善控制命令 → cnCtrl(last参数) + reboot + restore + patrol
✅ 第8步: 实现告警上报(CT="A") → 8种告警映射 + 硬件采集源接入
✅ 第9步: 实现主动校时(SV="rqst") + 登录响应校时
✅ 第10步: 实现属性Flash持久化 → 双副本+序列号
✅ 第11步: 实现cJSON RX/TX双池 → 内存隔离
✅ 第12步: 实现控制恢复机制(last参数定时恢复)
✅ 第13步: 实现发送忙处理 + 延迟响应队列
✅ 第14步: 实现工作计划(SV="plan") → 写入/存储/执行引擎
               ↓
第15步 (待做): 补齐属性读取RunTm/LightTm/Angle
               ↓
第16步 (待做): 实现告警配置读写(SV="alam")
               ↓
第17步 (待做): 实现Plan查询操作(CT="R") → 顺序: nid → del → id → now → sr(需NOAA天文算法)
               ↓
第18步 (待做): Signal改为AT查询真实值
               ↓
第19步 (待做): EleInfo.e接入BL0942电能数据
               ↓
第20步 (待做): 告警阈值改为alam可配置
```

---

## 九、资源预估（MDK-ARM-8008000工程）

| 项目 | 当前值 | 预估新增 | 预估总量 | 安全阈值 | 状态 |
|------|--------|----------|----------|----------|------|
| ROM | ~65KB | ~12KB | ~77KB | <100KB | ✅ |
| RAM | ~30KB | ~2KB | ~35KB(含栈2KB) | <40KB | ✅ |
| Stack | 2KB (0x800) | 不变 | 2KB | ≥2KB | ✅ 已修改 |
| Heap | 512B (0x200) | 不变 | 512B | - | ✅ |

> 注：新增zk_work_plan.c(~912行) + mqtt_zk_protocol.c扩展至~3249行，ROM从~60KB增至~65KB。

---

## 十、实机验证指南

> 以下所有验证操作均需设备已烧录最新固件、4G模块已注册网络、已连接到MQTT服务器`47.120.15.220:1883`。
> 推荐使用 **MQTTX** 或 **MQTT Explorer** 作为桌面端测试工具。

### 10.0 验证前准备

#### 10.0.1 工具准备

| 工具 | 用途 | 获取方式 |
|------|------|----------|
| MQTTX | MQTT桌面客户端，用于收发协议报文 | https://mqttx.app |
| 串口调试助手 | 查看设备串口日志(115200bps) | 任意串口工具 |
| USB转TTL模块 | 连接设备调试串口 | 3.3V电平 |
| 可调交流电源 | 模拟过压/欠压告警 | 0~300V可调 |
| 万用表 | 测量实际电压/电流 | - |

#### 10.0.2 MQTTX连接配置

```
Broker: 47.120.15.220
Port: 1883
Client ID: test_tool_<随机数>
Username: (留空)
Password: (留空)
```

#### 10.0.3 订阅主题

首先订阅以下主题以监听设备上报：

| 主题 | 说明 |
|------|------|
| `MS/<IMEI>/dev2plt` | 设备→平台上行数据（主要观察） |
| `MS/<IMEI>/dev2pcp` | 设备→平台OTA数据 |
| `MS/<IMEI>/offline` | 设备离线遗嘱消息 |

> 将 `<IMEI>` 替换为设备的15位IMEI号（可查看设备外壳标签或串口启动日志）。

#### 10.0.4 发布主题

平台通过以下主题向设备下发指令：

| 主题 | 说明 |
|------|------|
| `MS/<IMEI>/plt2dev` | 平台→设备下行指令（属性读写、控制、校时等） |
| `MS/<IMEI>/pcp2dev` | 平台→设备OTA升级指令 |

---

### 10.1 登录验证 (CT="L")

**目的：** 验证设备上电后能正确发送登录报文，并收到平台登录确认。

**验证步骤：**

1. 给设备上电，等待约10~20秒（4G模块注册网络 + MQTT连接）
2. 在MQTTX中观察主题 `MS/<IMEI>/dev2plt`，应收到登录上报报文

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "L",
  "DT": {
    "DevInfo": {
      "protId": 100,
      "clas": "MS-SLC-01",
      "prottp": "iX7-075SC028-4G",
      "hver": 100,
      "sver": <APP_VERSION>,
      "mver": "BC28GJAR01A01",
      "iccid": "<20位ICCID>"
    },
    "MdlInfo": { "mdlId": "<IMEI>" },
    "Gis": { "lng": ..., "lat": ..., "zone": 8 },
    "Dim": [{ "cns": 1, "dim": 0, "sov": 0 }],
    "Sense": {}
  }
}
```

**验证通过标准：**
- ✅ 收到CT="L"的登录上报
- ✅ DT中包含DevInfo、MdlInfo、Gis、Dim、Sense五个属性组
- ✅ DevInfo.iccid为20位字符串
- ✅ MdlInfo.mdlId等于设备IMEI
- ✅ 设备串口日志显示"login sent"

---

### 10.2 心跳验证 (CT="H")

**目的：** 验证设备登录成功后每60秒发送心跳，平台回复ACK后设备维持在线。

**验证步骤：**

1. 确认设备已登录成功
2. 在MQTTX中等待约60秒，观察 `MS/<IMEI>/dev2plt`
3. 检查心跳报文的MID（消息ID）是否逐次递增

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "H"
}
```

**验证通过标准：**
- ✅ 每60秒±5秒收到一条CT="H"的心跳上报
- ✅ 心跳报文无DT节点，结构精简
- ✅ MID逐次递增不重复

---

### 10.3 周期上报验证 (CT="C")

**目的：** 验证设备按配置周期（默认300秒）上送完整运行数据。

**验证步骤：**

1. 确认设备已登录成功
2. 等待约5分钟（默认300秒周期），观察 `MS/<IMEI>/dev2plt`
3. 检查报文内容完整性

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "C",
  "DT": {
    "RunTm": { "tTmLt": ..., "rTmLt": ... },
    "LightTm": { "tLtTime": [...], "rLtTime": [...] },
    "RunSts": { "sts": [...], "bri": [...] },
    "EleInfo": {
      "e": [...], "c": [...], "v": [...],
      "f": [...], "p": [...], "rEc": [...], "tEc": [...]
    },
    "PerSts": { "dlmt": ..., "ulmt": ..., "rti": ..., "rtPwr": ... },
    "Signal": { "rsrp": ..., "snr": ..., "cellId": "..." },
    "Angle": { "p": ..., "r": ... }
  }
}
```

**验证通过标准：**
- ✅ 每300秒±10秒收到一条CT="C"的周期上报
- ✅ DT中包含RunTm、LightTm、RunSts、EleInfo、PerSts、Signal、Angle全部7个属性组
- ✅ RunSts.sts数组各回路状态正确（1路单回路则为单元素数组）
- ✅ EleInfo.v(电压)、c(电流)、p(功率)数值在合理范围内

---

### 10.4 变化上报验证 (CT="B")

**目的：** 验证设备亮度或开关状态变化时，立即触发变化上报。

**验证步骤：**

1. 确认设备已登录且正常运行
2. 通过MQTTX向 `MS/<IMEI>/plt2dev` 发送调光指令改变亮度：
   ```json
   {
     "SN": "<IMEI>",
     "SV": "ctrl",
     "CT": "W",
     "DT": { "bri": 50 }
   }
   ```
3. 观察 `MS/<IMEI>/dev2plt`，应在调光完成后立即收到变化上报

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "B",
  "DT": {
    "RunSts": { "sts": [1], "bri": [50] },
    "Dim": [{ "cns": 1, "dim": 0, "sov": 0 }]
  }
}
```

**验证通过标准：**
- ✅ 亮度/开关变化后3秒内收到CT="B"的变化上报
- ✅ DT.RunSts.bri值与调光指令一致
- ✅ 设备串口日志出现"change report triggered"

---

### 10.5 属性读取验证 (SV="prop", CT="R")

**目的：** 验证平台可通过属性读取指令查询设备各类属性。

**验证步骤：**

1. 确认设备已登录
2. 在MQTTX中向 `MS/<IMEI>/plt2dev` 依次发送以下读取指令

#### 10.5.1 读取设备信息 (DevInfo)

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["DevInfo"]}}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","ER":0,"DT":{"DevInfo":{"protId":100,"clas":"MS-SLC-01","prottp":"iX7-075SC028-4G","hver":100,"sver":<版本>,"mver":"BC28GJAR01A01","iccid":"<ICCID>"}}}
```

#### 10.5.2 读取运行状态 (RunSts)

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["RunSts"]}}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","ER":0,"DT":{"RunSts":{"sts":[1或0],"bri":[<当前亮度>]}}}
```

#### 10.5.3 读取电气信息 (EleInfo)

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["EleInfo"]}}
```

**预期收到回复：** 包含e/c/v/f/p/rEc/tEc数组的电气数据

#### 10.5.4 读取信号质量 (Signal)

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["Signal"]}}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","ER":0,"DT":{"Signal":{"rsrp":<值>,"snr":<值>,"cellId":"<小区ID>"}}}
```

#### 10.5.5 批量读取多个属性

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["DevInfo","RunSts","EleInfo","Signal","RTC","Svr"]}}
```

**预期收到回复：** DT中包含所有请求的属性组

#### 10.5.6 读取不支持的属性

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["RunTm"]}}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","ER":1,"DT":{}}
```
> 当前RunTm/LightTm/Angle尚未加入属性读取分发表，返回ER=1。

**验证通过标准：**
- ✅ 支持的13组属性均返回ER=0且数据完整
- ✅ 不支持的属性返回ER=1
- ✅ items为空数组时返回ER=7
- ✅ 属性值范围合理（如亮度0~100，电压0~9999等）

---

### 10.6 属性写入验证 (SV="prop", CT="W")

**目的：** 验证平台可通过属性写入指令修改设备配置，且配置持久化到Flash。

#### 10.6.1 写入Gis（地理位置）

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Gis": { "lng": 120123456, "lat": 30123456, "zone": 8 }
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"W","ER":0,"DT":{}}
```

**验证持久化：** 设备断电重启后，读取Gis属性，确认经纬度值已保存。

#### 10.6.2 写入Dim（调光参数）

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Dim": [{ "cns": 1, "dim": 0, "sov": 0 }]
  }
}
```
> dim: 0=模拟调光, 1=PWM调光; sov: 0=0-10V, 1=PWM

**预期收到回复：** ER=0

#### 10.6.3 写入Svr（服务器参数）

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Svr": { "svrIp": "47.120.15.220", "svrPort": 1883, "uPeriod": 600, "hPeriod": 120, "tPeriod": 7200 }
  }
}
```

**预期收到回复：** ER=0
**验证：** 读取Svr属性，确认周期参数已更新为600/120/7200。

#### 10.6.4 写入RTC（时钟校准）

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "RTC": { "time": "2026-05-16 14:30:00" }
  }
}
```

**预期收到回复：** ER=0
**验证：** 读取RTC属性，确认时间已校准。

#### 10.6.5 范围校验验证

**发送超出范围的lng值：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Gis": { "lng": 200000000, "lat": 30123456 }
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"W","ER":3,"DT":{}}
```
> lng超出±180000000范围，返回ER=3(数据范围错误)。

**验证通过标准：**
- ✅ 正常值写入返回ER=0
- ✅ 超出范围返回ER=3
- ✅ 错误类型返回ER=2
- ✅ 断电重启后写入值不丢失（Flash持久化验证）
- ✅ 两次连续写入，断电后取sequence更大的那份

---

### 10.7 控制命令验证 (SV="ctrl", CT="W")

**目的：** 验证平台可通过控制指令远程调光、重启、恢复出厂设置、巡检。

#### 10.7.1 cnCtrl调光控制

**发送（立即调光到80%亮度，30秒后恢复）：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": {
    "cnCtrl": [{ "cns": 1, "bri": 80, "last": 30 }]
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"ctrl","CT":"W","ER":0,"DT":{}}
```

**验证步骤：**
1. 发送指令后，肉眼观察灯具亮度变为80%
2. 30秒后，观察灯具亮度恢复到调光前的值
3. 期间应收到CT="B"的变化上报

**预期：**
- ✅ 灯具亮度立即变为80%
- ✅ ER=0响应
- ✅ 约30秒后亮度自动恢复
- ✅ 亮度变化触发变化上报
- ✅ 串口日志显示"control restore"

#### 10.7.2 cnCtrl关闭/开启

**发送（关闭，持续120秒后恢复）：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": {
    "cnCtrl": [{ "cns": 1, "bri": 0, "last": 120 }]
  }
}
```

**预期：**
- ✅ 灯具立即熄灭
- ✅ 120秒后自动恢复原亮度

#### 10.7.3 cnCtrl last=0（永久调光）

**发送（永久调到60%，不恢复）：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": {
    "cnCtrl": [{ "cns": 1, "bri": 60, "last": 0 }]
  }
}
```

**预期：**
- ✅ 灯具亮度变为60%
- ✅ 持续观察3分钟以上，亮度不恢复

#### 10.7.4 reboot重启

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": { "DO": "reboot" }
}
```

**预期：**
- ✅ 立即收到ER=0响应
- ✅ 约500ms后设备重启
- ✅ MQTT连接断开（收到offline遗嘱消息）
- ✅ 设备重启后自动重新连接MQTT并发送登录上报

#### 10.7.5 restore恢复出厂配置

**发送（恢复默认配置）：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": { "restore": 0 }
}
```

**预期：**
- ✅ ER=0响应
- ✅ 读取Gis/Svr/Dim属性，确认已恢复为默认值
- ✅ Flash中存储的配置被覆盖为默认值
- ✅ restore=5（清零电能）：读取EleInfo，确认电能归零

#### 10.7.6 patrol巡检

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": { "DO": "patrol" }
}
```

**预期：**
- ✅ ER=0响应
- ✅ 随后立即收到一条CT="C"的周期上报（包含完整运行数据）

---

### 10.8 告警上报验证 (CT="A")

**目的：** 验证设备在异常条件下能正确检测并上报告警。

#### 10.8.1 过压告警 (almId=10000)

**验证方法：**
1. 使用可调交流电源为设备供电
2. 将电压调至270V以上（超过3200×0.1=320V阈值，需考虑分压比）
3. 观察MQTTX中 `MS/<IMEI>/dev2plt` 是否出现告警

> 注意：此验证涉及高压操作，请在确保安全的前提下进行，或使用信号发生器模拟电压采样值。

**预期报文：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "A",
  "DT": {
    "alm": {
      "almId": 10000,
      "almStatus": 1,
      "almValue": <实测电压>,
      "almThreshold": 3200
    }
  }
}
```

**验证通过标准：**
- ✅ 电压超过阈值后30秒内上报告警（almStatus=1）
- ✅ 电压恢复正常后上报恢复（almStatus=0）
- ✅ 告警值与实际测量值一致
- ✅ 同一告警状态不重复上报

#### 10.8.2 欠压告警 (almId=10001)

将电压调至80V以下（低于800×0.1=80V阈值）。预期上报告警。

#### 10.8.3 过流告警 (almId=10002)

接入大功率负载，使输出电流超过额定电流的150%。预期上报告警。

#### 10.8.4 漏电告警 (almId=10007)

使用漏电模拟器或在输出端对地接电阻。漏电流超过30mA时预期上报告警。

#### 10.8.5 设备失电告警 (almId=10009)

**验证方法：**
1. 设备正常运行中
2. 突然断开交流供电
3. 设备依赖内部电容短时维持，应在断电瞬间发送失电告警

**预期：** 收到almId=10009、almStatus=1的告警。此告警为单次上报(one_shot=1)，上报后不再重复。

#### 10.8.6 设备故障告警 (almId=10008)

**验证方法：**
1. 使用热风枪加热驱动器的NTC温度传感器
2. 温度超过保护阈值后预期上报告警

---

### 10.9 校时功能验证 (SV="rqst")

**目的：** 验证设备能主动请求校时，且平台响应后正确校准RTC。

#### 10.9.1 主动校时请求

**验证步骤：**
1. 确认设备已登录
2. 等待约1小时（默认3600秒周期），观察 `MS/<IMEI>/dev2plt`

**预期报文：**
```json
{
  "SN": "<IMEI>",
  "SV": "rqst",
  "CT": "R",
  "DT": { "DO": "TmCali" }
}
```

#### 10.9.2 平台响应校时

**在MQTTX中回复（模拟平台）：**
```json
{
  "SN": "<IMEI>",
  "SV": "rqst",
  "CT": "R",
  "ER": 0,
  "DT": {
    "TmCali": { "time": "2026-05-16 15:30:00" }
  }
}
```

**验证通过标准：**
- ✅ 约3600秒后设备自动发送TmCali请求
- ✅ 收到校时响应后设备RTC被校准
- ✅ 后续读取RTC属性，时间与设定值一致

#### 10.9.3 登录时自动校时

**验证步骤：**
1. 设备上电登录
2. 平台在登录响应中携带time字段
3. 观察设备RTC是否被校准

**验证通过标准：**
- ✅ 登录响应中的time与设备当前RTC相差>30秒时，自动覆盖RTC
- ✅ 相差≤30秒时，不覆盖（避免频繁微调）

---

### 10.10 OTA升级验证 (SV="ota")

**目的：** 验证设备能接收OTA指令、下载固件、上报进度、完成升级。

#### 10.10.1 前置条件

1. 准备一个可HTTP访问的固件文件（如放在内网HTTP服务器上）
2. 固件文件必须是`.bin`格式
3. 记录固件文件的完整URL

#### 10.10.2 下发OTA指令

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "ota",
  "CT": "W",
  "DT": {
    "url": "http://192.168.1.100:8080/firmware_v1.2.3.bin"
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"ota","CT":"W","ER":0,"DT":{}}
```

#### 10.10.3 观察OTA进度

在 `MS/<IMEI>/dev2pcp` 主题观察进度上报：

```json
// 进度30%
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":30}}

// 进度50%
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":50}}

// 进度90%
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":90}}

// 下载完成 100%
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":100}}
```

#### 10.10.4 OTA失败验证

**测试场景1：无效URL**
```json
{"SN":"<IMEI>","SV":"ota","CT":"W","DT":{"url":"ftp://invalid/url"}}
```
预期返回ER=91（URL无效）。

**测试场景2：设备忙**
在OTA进行中再次发送OTA指令，预期返回ER=12（设备忙）。

**测试场景3：URL过长**
发送超过192字符的URL，预期返回ER=91。

**验证通过标准：**
- ✅ 收到OTA指令后ER=0响应
- ✅ 设备串口日志显示"OTA started"
- ✅ 进度上报30→50→90→100依次出现
- ✅ 下载完成后设备自动重启进入bootloader
- ✅ 升级后新固件版本号正确
- ✅ OTA失败时上报CT="E"错误

---

### 10.11 工作计划验证 (SV="plan")

**目的：** 验证设备能接收工作计划、持久化存储、按时间表自动执行调光。

#### 10.11.1 下发周计划

**发送（每周一至周五 08:00亮度80%、18:00亮度30%）：**
```json
{
  "SN": "<IMEI>",
  "SV": "plan",
  "CT": "W",
  "DT": {
    "plan": {
      "id": 1,
      "en": 1,
      "type": 1,
      "priority": 1,
      "week": "1,2,3,4,5",
      "startTime": "2026-01-01 00:00:00",
      "endTime": "2026-12-31 23:59:59",
      "jobs": [
        {
          "cns": [1],
          "timetp": 0,
          "actions": [
            { "time": "08:00", "bri": 80 },
            { "time": "18:00", "bri": 30 }
          ]
        }
      ]
    }
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","ER":0,"DT":{}}
```

#### 10.11.2 验证计划执行

1. 确认设备RTC时间正确（或在RTC属性写入中设置正确时间）
2. 等待到计划设定时间（如08:00）
3. 观察灯具亮度是否自动变为80%
4. 等待到18:00，观察亮度是否自动变为30%

**验证通过标准：**
- ✅ 写入计划返回ER=0
- ✅ 设备断电重启后计划不丢失（Flash持久化验证）
- ✅ 到计划时间±1分钟内，灯具亮度自动变化
- ✅ 多个计划同时生效时，优先级高的优先执行
- ✅ 同一分钟内同一计划不重复执行

#### 10.11.3 Plan范围校验验证

**发送无效时间：**
```json
{
  "SN": "<IMEI>",
  "SV": "plan",
  "CT": "W",
  "DT": {
    "plan": {
      "id": 1,
      "en": 1,
      "type": 1,
      "priority": 1,
      "week": "1,2,3,4,5",
      "startTime": "2026-02-30 00:00:00",
      "endTime": "2026-12-31 25:00:00",
      "jobs": []
    }
  }
}
```
> 2月30日不存在，25:00无效。预期返回相应的错误码。

#### 10.11.4 禁用计划验证

**下发en=0禁用计划：**
```json
{
  "SN": "<IMEI>",
  "SV": "plan",
  "CT": "W",
  "DT": {
    "plan": { "id": 1, "en": 0 }
  }
}
```
> en=0时只需提供id和en两个字段，其他字段可省略。

**验证通过标准：**
- ✅ 返回ER=0
- ✅ 到原计划时间点，灯具不再自动调光（计划已被禁用）
- ✅ 断电重启后禁用状态保留

#### 10.11.5 计划查询功能（当前不可用）

> **注意：** 当前版本尚未实现CT="R"的计划查询操作。以下操作均会返回 **ER=1（命令不支持）**：

| 操作 | 请求示例 | 当前结果 |
|------|----------|----------|
| 查询有效计划列表 | `{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"nid"}}` | ER=1 |
| 查询指定计划内容 | `{"SN":"...","SV":"plan","CT":"R","DT":{"id":1}}` | ER=1 |
| 查询当前运行计划 | `{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"now"}}` | ER=1 |
| 查询日出日落时间 | `{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"sr"}}` | ER=1 |
| 删除计划 | `{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"del","id":1}}` | ER=1 |

> 如需验证计划是否已正确存储，可通过以下间接方式确认：
> 1. 串口日志观察启动时Flash读取结果
> 2. 设置计划到某整点时刻，到时间观察灯具是否自动调光（说明执行引擎正常工作）
> 3. 断电重启后再次到时间观察（说明Flash持久化正常）

---

### 10.12 Flash持久化验证

**目的：** 验证设备配置在断电重启后正确保留。

**验证步骤：**

1. 通过属性写入修改Gis（经纬度）和Svr（上报周期）：
   ```json
   {
     "SN": "<IMEI>",
     "SV": "prop",
     "CT": "W",
     "DT": {
       "Gis": { "lng": 121000000, "lat": 31000000 },
       "Svr": { "uPeriod": 600, "hPeriod": 120 }
     }
   }
   ```
2. 确认写入成功（ER=0）
3. 读取Gis和Svr，确认值已更新
4. 断开设备电源，等待10秒
5. 重新上电，等待设备登录
6. 读取Gis和Svr属性

**验证通过标准：**
- ✅ 重启后Gis.lng=121000000, Gis.lat=31000000
- ✅ 重启后Svr.uPeriod=600, Svr.hPeriod=120
- ✅ 周期上报按新周期(600秒)执行
- ✅ 心跳按新周期(120秒)执行

---

### 10.13 CRC密码验证

**目的：** 验证设备生成的MQTT密码符合中科协议规范。

**验证方法（通过串口日志或测试脚本）：**

使用测试脚本验证密码生成算法：
```bash
cd tests
python3 test_login_validation.py
```

**测试向量：**
| IMEI | 预期密码 |
|------|----------|
| 864294053651521 | BD9D0EE1D3E1 |
| 860608074646596 | FBE709622252 |
| 868120257654321 | 707D3A606B13 |

**验证通过标准：**
- ✅ 三个测试向量全部匹配
- ✅ MQTT服务器接受设备连接（说明密码认证通过）

---

### 10.14 端到端集成验证流程

**目的：** 一次性验证设备完整生命周期。

**完整验证流程（约30分钟）：**

| 步骤 | 操作 | 观察点 | 时长 |
|------|------|--------|------|
| 1 | 设备上电 | 串口日志：IMEI读取、4G注册、MQTT连接 | ~20秒 |
| 2 | 自动登录 | MQTTX收到CT="L"登录上报 | 即时 |
| 3 | 首次心跳 | MQTTX收到CT="H"心跳 | ~60秒 |
| 4 | 写入配置 | 修改Gis/Svr属性，ER=0 | 即时 |
| 5 | 调光指令 | 下发cnCtrl bri=50，灯具变暗 | 即时 |
| 6 | 变化上报 | 收到CT="B"上报 | <3秒 |
| 7 | 恢复验证 | 等待last秒数后亮度恢复 | 按参数 |
| 8 | 断电重启 | 断开电源后重新上电 | ~30秒 |
| 9 | 持久化验证 | 读取Gis/Svr，确认值保留 | 即时 |
| 10 | 周期上报 | 收到CT="C"完整周期数据 | ~5分钟 |
| 11 | 下发计划 | 写入周计划，到点自动执行 | 按计划时间 |
| 12 | 巡检指令 | 下发patrol，收到即时周期数据 | 即时 |
| 13 | OTA升级 | 下发OTA URL，观察进度→跳boot | ~2分钟 |

**最终验收标准：**
- ✅ 设备从冷启动到成功登录 < 30秒
- ✅ 心跳60秒间隔稳定 ±5秒
- ✅ 周期上报内容完整、数值合理
- ✅ 属性读写双向通信正常
- ✅ 控制指令响应正确、灯光变化符合预期
- ✅ 告警检测灵敏、不误报不漏报
- ✅ 配置断电重启不丢失
- ✅ OTA升级流程完整可用
- ✅ 计划按时间表自动执行

---

### 10.15 常见问题排查

| 现象 | 可能原因 | 排查方法 |
|------|----------|----------|
| 设备不上报登录 | IMEI未正确读取或SIM卡未注册 | 检查串口日志IMEI打印；确认SIM卡有流量 |
| 登录报ER≠0 | CRC密码错误 | 用测试向量验证crc16_modbus输出 |
| 心跳中断 | 网络波动或服务器断开 | 检查4G信号质量；观察设备是否自动重连 |
| 属性读取返回ER=1 | 该属性组未加入分发表 | 确认属性名在`zk_add_property_to_dt()`中 |
| 配置重启丢失 | Flash写入失败 | 检查串口是否有"Flash save error"日志 |
| 计划未按时执行 | RTC时间不准或计划不在有效期内 | 读取RTC属性确认时间；检查计划en/start/end |
| OTA下载失败 | URL不可达或网络问题 | 确认HTTP服务器可达；检查URL格式 |
| 告警不触发 | 阈值不匹配或采集源未接入 | 检查对应almId的threshold配置 |

---

**文档更新时间：** 2026-05-16
**基于：** 重构开发方案v2.0.md + 中科协议.docx + 当前代码(含工作区未提交变更)交叉复核
