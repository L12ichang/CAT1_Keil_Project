# 中科MQTT协议开发进度分析与实现计划

> 基于「重构开发方案v2.0.md」与「中科协议.docx」交叉复核
> 目标工程：`MDK-ARM-8008000`
> 统一服务器：`47.120.15.220:1883`
> CRC校验：统一使用 **CRC16-Modbus (0xA001)** ✅ 已修正
> 最后更新：2026-05-19

---

## 一、总体进度：约 94%

```
已完成   ████████████████████████████░░  94%
未完成   ██████░░░░░░░░░░░░░░░░░░░░░░   6%
```

### 进度变化说明（2026-05-18 → 2026-05-19）

本轮重点完成了工作计划模块的 **查询与删除操作**，补齐了 CT="R" 路径下的 4 个核心操作（nid/id/now/del），修复了初版代码评审中发现的全部 5 个 Bug 和 4 个设计缺陷。剩余工作主要集中在告警配置读写、属性读取补充和天文算法。

```
进度增量明细:
  工作计划查询操作 (nid/id/now/del)     +3%
  工作计划bug修复与设计改进              +1%
  ─────────────────────────────────────────
  累计进度提升                          +4%  (90% → 94%)
```

---

## 二、已完成项

### 2.1 基础架构（第一阶段）

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 数据结构定义 | `mqtt_zk_protocol.h` | ✅ 95% | `zk_mqtt_config_t`、`zk_device_config_t`、`zk_message_header_t`。SV/CT枚举、10种告警ID枚举均已定义。新增svrIp/svrPort/uPeriod/hPeriod/tPeriod/commMain/commSub/commSAuto/spreadOffset/spreadWindow/spreadInterval/dimTp字段 |
| CRC密码生成 | `mqtt_zk_protocol.c` | ✅ 100% | **CRC16-Modbus(0xA001)**，调用`crc16_modbus_get()` |
| MQTT连接参数 | `NbDriver.c` | ✅ 100% | 全部切换到ZK函数，IMEI认证，plt2dev+pcp2dev双主题订阅 |
| 栈大小 | `MDK-ARM-8008000/startup_stm32f103xe.s:33` | ✅ 100% | `Stack_Size EQU 0x800` (2KB) |

### 2.2 协议处理层（第二阶段）

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| JSON消息处理 | `mqtt_zk_protocol.c` | ✅ 95% | 消息头解析、登录/心跳/周期/变化/告警报文构建、属性读写、控制命令、校时请求、OTA入口均已实现 |
| 协议入口路由 | `Json_Protocol.c` | ✅ 100% | **已收口为中科JSON统一分发入口**，通过`zk_dispatch_message()`路由prop/ctrl/rqst/ota/plan全部SV类型 |
| cJSON双池内存管理 | `mqtt_zk_protocol.c` | ✅ 100% | RX池4KB + TX池4KB，8字节对齐，线性分配器，TX耗尽日志告警 |

```
                      MQTT 收包 (app_mqtt_rx)
                              │
                              ▼
                    ┌──────────────────┐
                    │  cJSON_Parse()    │
                    │  解析 JSON 字符串  │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ 消息头解析+设备SN  │
                    │  校验(IMEI匹配)    │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ 登录ACK/心跳ACK   │
                    │  快速路径拦截      │
                    └────────┬─────────┘
                             │ 非ACK消息
                             ▼
               ┌─────────────────────────┐
               │   zk_dispatch_message()  │
               │   按 SV+CT 联合路由       │
               └────────┬────────────────┘
                        │
        ┌───────┬───────┼───────┬───────┬───────┐
        ▼       ▼       ▼       ▼       ▼       ▼
      prop    prop    ctrl    plan    rqst     ota
      (R)     (W)     (W)    (R/W)    (R)     (W)
```

### 2.3 上报机制（第三阶段）

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 登录上报(L) | `mqtt_zk_protocol.c` | ✅ 100% | DevInfo+MdlInfo+Gis+Dim+Sense，与协议要求一致 |
| 心跳上报(H) | `mqtt_zk_protocol.c` | ✅ 100% | 60秒间隔（可配置），无DT节点，与协议要求一致 |
| 周期上报(C) | `mqtt_zk_protocol.c` | ✅ 100% | RunTm+LightTm+RunSts+EleInfo+PerSts+Signal+Angle，默认300秒间隔 |
| 变化上报(B) | `mqtt_zk_protocol.c` | ✅ 100% | 亮度/开关变化时触发，通过`zk_notify_state_changed()`设置标志 |
| 告警上报(A) | `mqtt_zk_protocol.c` | ✅ 95% | 8种告警已接入硬件采集源（电压/电流/漏电/温度/失电），带pending/reported/one-shot状态机；灯杆倾斜(10006)/关灯异常(10005)未接入采集源 |

```
上报时间线 (设备在线期间):
  
  Login    H(60s)   H(120s)  H(180s)  H(240s)  C(300s)  H(360s) ...
  ──●────────●────────●────────●────────●────────●────────●─────▶
    │                                      │
    L                                    C(周期上报)
  (登录)                              RunTm+LightTm+RunSts
                                      +EleInfo+PerSts+Signal
                                      +Angle (7组属性)
  
  变化上报(B): 亮度/开关变化时立即触发 (非周期性)
  告警上报(A): 异常发生时立即触发 (带状态机防重复)
```

### 2.4 属性读写

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 属性读取框架 | `mqtt_zk_protocol.c` | ✅ 90% | 支持DevInfo/MdlInfo/Gis/Dim/Sense/RunSts/EleInfo/PerSts/Signal/RTC/Svr/Comm/Spread共13组；**RunTm/LightTm/Angle未加入属性读取分发表** |
| 属性写入框架 | `mqtt_zk_protocol.c` | ✅ 100% | 支持Gis/Dim/Sense/Svr/RTC写入，含范围校验和错误码返回；**写入值已通过Flash双副本持久化，重启不丢失** |
| 属性Flash持久化 | `mqtt_zk_protocol.c` | ✅ 100% | `zk_property_flash_store_config()`双副本+序列号方案，main+backup互为冗余，magic=0x5A4B5052 |

```
属性读取分发表 (zk_add_property_to_dt):

  items[] ──▶ 遍历每个属性名 ──▶ strcmp 匹配:
  ┌──────────┬──────────┬──────────┬──────────┬──────────┐
  │ DevInfo  │ MdlInfo  │   Gis    │   Dim    │  Sense   │
  │   ✅     │   ✅     │   ✅     │   ✅     │   ✅     │
  ├──────────┼──────────┼──────────┼──────────┼──────────┤
  │ RunSts   │ EleInfo  │ PerSts   │ Signal   │   RTC    │
  │   ✅     │   ✅     │   ✅     │   ✅     │   ✅     │
  ├──────────┼──────────┼──────────┼──────────┼──────────┤
  │   Svr    │  Comm    │  Spread  │  RunTm   │ LightTm  │
  │   ✅     │   ✅     │   ✅     │   ❌     │   ❌     │
  ├──────────┴──────────┴──────────┼──────────┼──────────┤
  │           Angle                │  (其他)   │          │
  │            ❌                  │  ER=1    │          │
  └────────────────────────────────┴──────────┴──────────┘
```

### 2.5 控制命令

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| cnCtrl调光 | `mqtt_zk_protocol.c` | ✅ 100% | 支持cns=1单回路、last持续时间、亮度/开关控制、多item批量处理；带范围校验 |
| cnCtrl恢复机制 | `mqtt_zk_protocol.c` | ✅ 100% | last分钟数到期后自动恢复原亮度，`zk_control_restore_process()`定时器驱动 |
| reboot重启 | `mqtt_zk_protocol.c` | ✅ 100% | 先响应ER=0，延迟500ms后NVIC_SystemReset() |
| restore恢复 | `mqtt_zk_protocol.c` | ✅ 100% | 支持0/1/5/6四种恢复类型（0/1/6恢复出厂配置到Flash，5清零电能），2/3/4返回不支持 |
| patrol巡检 | `mqtt_zk_protocol.c` | ✅ 100% | 响应ER=0后延迟上报完整周期数据 |

```
控制命令处理流程:
  SV="ctrl", CT="W"
        │
        ├── DT.cnCtrl 存在?
        │     ├── 解析 cns/bri/last 字段 (last: 分钟, 范围0~1440)
        │     ├── 范围校验 (bri: 0~100, last: 0~1440分钟)
        │     ├── 执行调光 zk_apply_brightness()
        │     ├── last>0 → 启动恢复定时器 zk_control_restore_delay_ms
        │     └── 触发变化上报 zk_notify_state_changed()
        │
        ├── DT.DO = "reboot"?
        │     ├── 响应 ER=0
        │     └── 延迟500ms → NVIC_SystemReset()
        │
        ├── DT.DO = "patrol"?
        │     ├── 响应 ER=0
        │     └── 立即触发一次周期上报(C)
        │
        └── DT.restore = N?
              ├── 0/1/6 → 恢复出厂配置到Flash + 重启
              ├── 5 → 清零电能数据
              └── 2/3/4 → 返回 ER=1 (不支持)
```

### 2.6 校时功能

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 主动校时请求 | `mqtt_zk_protocol.c` | ✅ 100% | SV="rqst", CT="R", DT.DO="TmCali"，默认3600秒周期 |
| 校时响应处理 | `mqtt_zk_protocol.c` | ✅ 100% | 解析TmCali响应，覆盖RTC |
| 服务器时间校准 | `mqtt_zk_protocol.c` | ✅ 100% | 登录响应中解析time字段，相差>30秒时覆盖RTC |

### 2.7 OTA升级

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| OTA指令接收 | `mqtt_zk_protocol.c` | ✅ 100% | SV="ota", CT="W", DT.url解析，提取文件名，触发HTTP下载 |
| OTA进度上报 | `mqtt_zk_protocol.c`、`ota.c` | ✅ 90% | CT="P"上报API，30/50/90/100进度节点；支持pending队列延迟发送 |
| OTA错误上报 | `mqtt_zk_protocol.c`、`ota.c` | ✅ 90% | CT="E"上报API，支持pending队列和挂起重试 |
| OTA URL提取 | `mqtt_zk_protocol.c` | ✅ 100% | `zk_get_ota_url()`供ota.c获取中科下发URL |

### 2.8 工作计划 (SV="plan") — 大幅更新

> **本轮重点完成**: CT="R" 路径下的 4 个核心查询操作（nid/id/now/del）已全部实现。初版评审中发现的 5 个 Bug 和 4 个设计缺陷已全部修复。详见 `docs/工作计划模块代码评审报告.md`。

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| Plan写入/更新 | `zk_work_plan.c` | ✅ 100% | `zk_handle_plan_message()`处理CT="W"，`zk_plan_upsert()`支持新增/覆盖/禁用 |
| Plan禁用 (en=0) | `zk_work_plan.c` | ✅ 100% | **已修复Bug#1**: en=0时保留全部配置仅翻转en位，符合协议7.4.5"其他字段可省略"的设计意图 |
| Plan删除 (del) | `zk_work_plan.c` | ✅ 100% | **已实现**: CT="W" + DT.del=[1,3,5] 数组格式，符合协议7.13.2 |
| Plan Flash持久化 | `zk_work_plan.c` | ✅ 100% | 双副本+magic=0x5A4B504C，plan存储区0x08006000/0x08007800，最多8个计划 |
| Plan执行引擎 | `zk_work_plan.c` | ✅ 100% | Type=1(周日期)计划，按分钟匹配action，优先级仲裁(1最低~8最高)，防重复执行，**+1分钟容错窗口** |
| 主循环集成 | `main.c` | ✅ 100% | `zk_work_plan_init()`初始化，`zk_work_plan_process()`每循环调用 |
| 有效计划查询 (DO="nid") | `zk_work_plan.c` | ✅ 100% | **已实现**: 返回所有valid=1且en=1的计划ID数组 |
| 计划内容查询 (id) | `zk_work_plan.c` | ✅ 100% | **已实现**: 返回指定id的完整计划JSON (含jobs/actions)，en=0时仅返回{id, en} |
| 当前运行查询 (DT.now) | `zk_work_plan.c` | ✅ 100% | **已实现**: DT.now=cns (Number), 支持通道过滤, 返回完整plan JSON (符合协议7.9) |
| 计划执行日志 | `zk_work_plan.c` | ✅ 100% | **已实现**: `printf("[plan] exec id=%d job=%d act=%d bri=%d\n", ...)` |
| 结构体packed属性 | `zk_work_plan.c` | ✅ 100% | **已实现**: 全部5个结构体加`__attribute__((packed))` |
| cns_mask通道过滤 | `zk_work_plan.c` | ✅ 100% | **已实现**: `zk_plan_find_current_match_for_cns()`支持按cns位掩码过滤job |
| 日出日落查询 (DO="sr") | — | ❌ 0% | 需天文算法(NOAA公式)，基于经纬度计算日出日落时间 |

```
Plan 全链路操作图 (更新后):

  SV="plan" ──▶ zk_handle_plan_message()
                    │
        ┌───────────┴───────────┐
        │ CT="R"                │ CT="W"
        ▼                       ▼
  zk_plan_handle_read()    ┌─────────────────┐
        │                   │ DT.del 存在?     │
  ┌─────┼─────┬─────┐       │  YES → 批量删除  │
  ▼     ▼     ▼     ▼       │  遍历数组        │
 DO=  DT.now DT.id  (其他)  │  zk_plan_delete()│
"nid" =cns   =N    ER=1     └────────┬────────┘
  │     │     │                NO → DT.plan存在?
  ▼     ▼     ▼                      ▼
 nid[] 完整   完整             zk_plan_upsert()
 数组  plan   plan             解析→校验→Flash写
       JSON   JSON                 │
       (cns    (en=0时        ┌────┴────┐
       过滤)   仅id+en)       │ en=1    │ en=0
                              ▼         ▼
                          完整写入   保留原数据
                          Flash      仅翻转en位
```

### 2.9 告警处理增强

| 子任务 | 文件 | 完成度 | 备注 |
|--------|------|--------|------|
| 硬件采集源接入 | `mqtt_zk_protocol.c` | ✅ 100% | 过压(ac_voltage_8209)、欠压(ac_voltage_8209)、过流(Io_value)、欠流(Io_value)、开灯异常(Po_value)、漏电(dangeo_out)、设备故障(ntc温度)、设备失电(power_drop)均已接入 |
| 告警状态机 | `mqtt_zk_protocol.c` | ✅ 100% | pending/pending_status/reported/one_shot四状态跟踪，防重复上报 |
| 告警阈值动态计算 | `mqtt_zk_protocol.c` | ✅ 100% | 过流阈值按额定电流150%计算，欠流按80%，温度阈值按NTC读数 |

```
告警状态机:
  ┌─────────┐  检测到异常   ┌─────────┐  MQTT发送   ┌──────────┐
  │  idle   │ ───────────▶ │ pending │ ──────────▶ │ reported │
  └─────────┘              └─────────┘             └──────────┘
                                 │                       │
                                 │ 异常恢复               │ 异常恢复
                                 ▼                       ▼
                            ┌─────────┐             ┌──────────┐
                            │pending  │             │ pending  │
                            │_status  │             │ (恢复)   │
                            └─────────┘             └──────────┘
                                                        │
                                                        ▼
                                                   ┌──────────┐
                                                   │ reported │
                                                   │ (恢复)   │
                                                   └──────────┘

  one_shot 告警 (如失电10009): 上报一次后不再跟踪状态变化
```

### 2.10 关键集成点

| 集成点 | 文件 | 状态 |
|--------|------|------|
| MQTT连接成功后自动发送登录包 | `TcpClient.c` | ✅ |
| 断连自动重置会话 | `TcpClient.c` | ✅ |
| 会话状态机(登录重发/心跳/周期上报/校时/告警/OTA挂起上报/plan执行) | `zk_mqtt_session_process()` | ✅ |
| 服务器时间校准(相差>30秒时覆盖) | `mqtt_zk_protocol.c` | ✅ |
| 控制恢复机制(last参数) | `mqtt_zk_protocol.c` | ✅ |
| 发送忙自动清理(连续3次失败后复位) | `mqtt_zk_protocol.c` | ✅ |
| 延迟响应队列(response/OTA progress/OTA error pending) | `mqtt_zk_protocol.c` | ✅ |

---

## 三、未完成项（按优先级排序）

### P1 — 核心功能补充

#### 3.1 告警配置读写 (SV="alam")

当前SV_ALAM常量尚未定义，Json_Protocol.c路由未接入。需要新增`zk_handle_alam_message()`处理函数。

```json
// 读取告警配置
{"SN":"...","SV":"alam","CT":"R","DT":{"alms":[10000,10001]}}
// 响应
{"SN":"...","SV":"alam","CT":"R","ER":0,"DT":{"alms":[
  {"almId":10000,"almValue":2740,"recValue":2640,"almEn":1},
  {"almId":10001,"almValue":800,"recValue":820,"almEn":1}
]}}

// 写入告警配置
{"SN":"...","SV":"alam","CT":"W","DT":{"alms":[
  {"almId":10000,"almValue":2740,"recValue":2640,"almEn":1}
]}}
```

**实现要点：**
- 定义 `ZK_SV_ALAM "alam"` 常量
- 新增 `zk_handle_alam_message()` 处理CT=R/W
- 告警阈值结构体：almId(10000~10009) + almValue(触发阈值) + recValue(恢复阈值) + almEn(使能)
- Flash持久化告警阈值配置
- 告警检测逻辑改用可配置阈值替代硬编码

**工作量预估：** 中（~300行，涉及新handler + Flash存储 + 告警检测改造）

#### 3.2 日出日落查询 (SV="plan", CT="R", DO="sr")

需实现NOAA天文算法，基于设备经纬度计算当日日出日落时间。这是工作计划模块唯一的遗留功能缺口。

```json
// 请求
{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"sr"}}
// 响应
{"SN":"...","SV":"plan","CT":"R","ER":0,"DT":{"sr":{"sunrise":"06:15","sunset":"18:42"}}}
```

**实现要点：**
- 输入：设备当前经纬度（取自`zk_dev_cfg.lng/lat`）、当前日期、时区（`zk_dev_cfg.zone`）
- 算法：NOAA太阳高度角公式，计算太阳天顶角=90°50'（民用晨光始）的时刻
- 精度要求：±5分钟
- 此查询用于平台按日出日落自动生成type=2计划的时间参考

**工作量预估：** 中（~200行，纯算法实现）

### P2 — 完善项

#### 3.3 属性读取补充 (RunTm/LightTm/Angle)

RunTm、LightTm、Angle三组属性已在周期/变化上报中构建，但未加入`zk_add_property_to_dt()`属性读取分发表。平台下发prop读请求时这三组返回不支持(ER=1)。

**改动点：** 在`zk_add_property_to_dt()`中增加3个`else if`分支，复用已有的`zk_add_runtime_time_groups()`和`zk_add_angle_group()`。

**工作量预估：** 极小（~10行）

#### 3.4 Signal信号质量

当前RSRP通过`getSignal()`简单估算（2格=-70dBm，1格=-90dBm，0格=-110dBm），需改为AT查询真实值（AT+CSQ）。

**工作量预估：** 小（需AT指令交互，~50行）

#### 3.5 EleInfo电能字段

EleInfo中的e(电能)字段当前固定填0（`cJSON_AddItemToArray(e, cJSON_CreateNumber(0))`）。rEc(本次电能)和tEc(累计电能)已通过BL0942读取实际值。e字段为分相电能数组，需明确协议含义后接入。

**工作量预估：** 小（~5行，需先确认协议定义）

#### 3.6 告警阈值可配置化

当前告警阈值（过压3200、欠压800等）硬编码在代码中，alam功能完成后需改为从Flash中读取平台下发的可配置阈值。

**工作量预估：** 中（依赖3.1 alam功能的实现）

#### 3.7 未接入采集源的告警

| 告警ID | 名称 | 状态 | 接入方案 |
|--------|------|------|----------|
| 10005 | 关灯异常 | 未接入 | 需检测：调光输出>0但功率读数≈0 |
| 10006 | 灯杆倾斜 | 未接入 | 需加速度/倾角传感器硬件支持 |

---

## 四、当前代码已知问题

### 🔴 功能缺口

| # | 问题 | 文件 | 优先级 |
|---|------|------|--------|
| 1 | SV="alam"告警配置读写尚未实现 | 多个文件 | P1 |
| 2 | Plan DO="sr"日出日落查询未实现（需NOAA天文算法） | `zk_work_plan.c` | P1 |
| 3 | RunTm/LightTm/Angle已在上报中生成，但属性读取未支持 | `mqtt_zk_protocol.c` | P2 |

### 🟡 需要完善

| # | 问题 | 文件 | 优先级 |
|---|------|------|--------|
| 4 | Signal信号强度使用简单估算（getSignal()），非AT查询真实值 | `mqtt_zk_protocol.c` | P2 |
| 5 | EleInfo中的e(电能)字段当前固定填0 | `mqtt_zk_protocol.c` | P2 |
| 6 | 告警阈值(过压3200/欠压800等)硬编码，未从平台同步 | `mqtt_zk_protocol.c` | P2 |
| 7 | 灯杆倾斜(10006)/关灯异常(10005)告警ID已定义但未接入采集源 | `mqtt_zk_protocol.c` | P2 |

### 🟢 可优化

| # | 问题 | 状态 |
|---|------|------|
| 8 | 属性Flash持久化占用2个Flash页(1KB×2)，可接受 | ⬜ 维持现状 |
| 9 | OTA进度上报使用固定30/50/90节点，非实际下载百分比 | ⬜ 维持现状 |
| 10 | Plan每次变更做双页Flash擦写，10K次寿命对低频场景够用 | ⬜ 维持现状 |
| 11 | Plan仅支持type=1(周日期)，type=2(日出日落)/type=3(光感)未实现 | ⬜ 待业务需求 |

### ✅ 已修复（2026-05-19）

| # | 原问题 | 修复方式 |
|---|--------|----------|
| ~~1~~ | ~~Plan仅支持CT="W"，不支持R查询~~ | 实现nid/id/now/del全部查询操作 |
| ~~2~~ | ~~en=0禁用计划时销毁全部数据~~ | 保留原记录仅翻转en位 |
| ~~3~~ | ~~删除命令格式不匹配协议~~ | CT=W + DT.del=[1,3,5] 数组格式 |
| ~~4~~ | ~~now查询返回格式与协议不符~~ | 改为DT.now=cns，返回完整plan JSON |
| ~~5~~ | ~~结构体缺少packed属性~~ | 5个结构体全加__attribute__((packed)) |
| ~~6~~ | ~~cns_mask解析存储但不使用~~ | cns_mask已用于通道过滤匹配 |
| ~~7~~ | ~~缺少执行日志~~ | printf输出执行关键信息 |
| ~~8~~ | ~~优先级方向无注释~~ | 添加注释说明1最低~8最高 |
| ~~9~~ | ~~分钟匹配无容错窗口~~ | 增加+1分钟时间窗口 |
| ~~10~~ | ~~年循环(0xFFFF)闰年判断不正确~~ | 0xFFFF返回非闰年 |

---

## 五、文件改动清单

| 文件 | 改动类型 | 行数 | 内容 |
|------|----------|------|------|
| `MDK-ARM-8008000/startup_stm32f103xe.s` | ✅ 修改 | — | 栈大小 0x400 → 0x800 |
| `Core/Src/LampProtocolLib/mqtt_zk_protocol.c` | ✅ 重大修改 | ~3390行 | CRC改为Modbus(0xA001)；属性读写+Flash持久化；周期/变化/告警上报(硬件源接入)；控制命令+last恢复；校时请求/响应；OTA入口+进度/错误API；cJSON双池；告警状态机；发送忙处理；延迟队列 |
| `Core/Src/LampProtocolLib/mqtt_zk_protocol.h` | ✅ 重大修改 | — | 完整SV/CT/告警ID枚举；zk_device_config_t扩展(新增svrIp/dimTp/comm/spread等字段)；handler函数声明 |
| `Core/Src/LampProtocolLib/zk_work_plan.c` | ✅ 新增→增强 | ~1271行 | 工作计划完整实现：写入/查询(nid/id/now/del)/存储/执行引擎/日志/cns过滤/分钟容错/packed属性 |
| `Core/Src/LampProtocolLib/zk_work_plan.h` | ✅ 新增 | — | 工作计划头文件 |
| `Core/Src/LampProtocolLib/Json_Protocol.c` | ✅ 修改 | ~66行 | 已收口为中科JSON统一分发入口，旧协议代码已移除 |
| `Core/Src/LampProtocolLib/App.c` | ✅ 修改 | — | 周期上报逻辑改为中科格式 |
| `Core/Src/LampProtocolLib/NbDriver.h` | ✅ 修改 | — | 双槽接收缓冲、协议模式标识 |
| `Core/Src/LampProtocolLib/NbDriver.c` | ✅ 修改 | — | 中科MQTT连接、订阅、发布路径集成 |
| `Core/Src/LampProtocolLib/TcpClient.c` | ✅ 修改 | — | 连接成功后登录、断连重置会话、会话状态机驱动 |
| `Core/Src/LampProtocolLib/ota.c` | ✅ 修改 | — | OTA URL使用中科下发地址，进度上报接入中科格式 |
| `Core/Src/main.c` | ✅ 修改 | — | 集成zk_work_plan_init()和zk_work_plan_process() |
| `Core/Src/sys_data.h` | ✅ 修改 | — | 数据结构调整 |

---

## 六、CRC密码生成算法

**统一使用 CRC16-Modbus (反射多项式 0xA001, 初始值 0xFFFF)** ✅

```
输入: 15位IMEI字符串 (如 "864294053651521")
步骤:
  1. 将IMEI按5字节分为3段: "86429", "40536", "51521"
  2. 每段调用 crc16_modbus_get() 计算CRC16
  3. 每个CRC结果取反 (~crc)
  4. 按 第3段-第1段-第2段 顺序拼接为12字符的16进制密码

已有函数: Core/Src/crc16_modbus.c::crc16_modbus_get(u8* ptr, u8 len)

测试向量:
  IMEI=864294053651521 -> Password=BD9D0EE1D3E1
  IMEI=860608074646596 -> Password=FBE709622252
  IMEI=868120257654321 -> Password=707D3A606B13
```

---

## 七、关键协议常量参考

```c
// 服务器
#define ZK_MQTT_SERVER_IP       "47.120.15.220"
#define ZK_MQTT_SERVER_PORT     1883

// SV 服务类型
#define ZK_SV_PROP              "prop"    // 属性类
#define ZK_SV_CTRL              "ctrl"    // 控制类
#define ZK_SV_REPT              "rept"    // 上报类
#define ZK_SV_RQST              "rqst"    // 请求类
#define ZK_SV_OTA               "ota"     // 升级类
#define ZK_SV_PLAN              "plan"    // 工作计划类
// 待实现: #define ZK_SV_ALAM "alam"

// CT 操作类型(非上报类)
#define ZK_CT_READ              "R"       // 读
#define ZK_CT_WRITE             "W"       // 写

// CT 上报类型(上报类)
#define ZK_CT_LOGIN             "L"       // 登录上报
#define ZK_CT_HEARTBEAT         "H"       // 心跳上报
#define ZK_CT_CYCLIC            "C"       // 周期上报
#define ZK_CT_CHANGE            "B"       // 变化上报
#define ZK_CT_ALARM             "A"       // 告警上报

// CT OTA类型
#define ZK_CT_PROGRESS          "P"       // OTA进度
#define ZK_CT_ERROR             "E"       // OTA错误

// 告警ID
#define ZK_ALARM_OVER_VOLTAGE   10000     // 过压
#define ZK_ALARM_UNDER_VOLTAGE  10001     // 欠压
#define ZK_ALARM_OVER_CURRENT   10002     // 过流
#define ZK_ALARM_UNDER_CURRENT  10003     // 欠流
#define ZK_ALARM_LIGHT_ON_FAIL  10004     // 开灯异常
#define ZK_ALARM_LIGHT_OFF_FAIL 10005     // 关灯异常(尚未接入采集源)
#define ZK_ALARM_POLE_TILT      10006     // 灯杆倾斜(尚未接入采集源)
#define ZK_ALARM_LEAK_CURRENT   10007     // 灯杆漏电
#define ZK_ALARM_DEVICE_FAULT   10008     // 设备故障
#define ZK_ALARM_POWER_DOWN     10009     // 设备失电

// 错误码
#define ZK_ER_OK                0         // 成功
#define ZK_ER_NOT_SUPPORTED     1         // 命令不支持
#define ZK_ER_TYPE_ERROR        2         // 数据类型错误
#define ZK_ER_RANGE_ERROR       3         // 数据范围错误
#define ZK_ER_FIELD_ERROR       4         // 字段属性错误
#define ZK_ER_MISSING_FIELD     5         // 必要字段缺失
#define ZK_ER_ARRAY_BOUNDS      6         // 数组越界
#define ZK_ER_ARRAY_EMPTY       7         // 数组为空
#define ZK_ER_STRING_LEN        8         // 字符串长度错误
#define ZK_ER_LOGIC_ERROR       9         // 逻辑错误
#define ZK_ER_STORE_ERROR       10        // 参数存储错误
#define ZK_ER_DEVICE_BUSY       12        // 设备忙(OTA进行中)
#define ZK_ER_URL_INVALID       91        // URL无效
#define ZK_ER_FLASH_SAVE        99        // Flash存储失败
```

---

## 八、推荐实施顺序（当前阶段）

```
✅ 第1步:  修改栈大小 0x400 → 0x800
✅ 第2步:  修改CRC密码生成为Modbus(0xA001)
✅ 第3步:  实现属性读取框架 → DevInfo/RunSts/EleInfo/Signal/RTC等共13组
✅ 第4步:  实现周期上报(C) + 变化上报(B)
✅ 第5步:  实现OTA升级入口(SV="ota")
✅ 第6步:  实现属性写入框架 → Gis/Dim/Sense/Svr/RTC
✅ 第7步:  完善控制命令 → cnCtrl(last参数) + reboot + restore + patrol
✅ 第8步:  实现告警上报(CT="A") → 8种告警映射 + 硬件采集源接入
✅ 第9步:  实现主动校时(SV="rqst") + 登录响应校时
✅ 第10步: 实现属性Flash持久化 → 双副本+序列号
✅ 第11步: 实现cJSON RX/TX双池 → 内存隔离
✅ 第12步: 实现控制恢复机制(last参数定时恢复)
✅ 第13步: 实现发送忙处理 + 延迟响应队列
✅ 第14步: 实现工作计划(SV="plan") → 写入/存储/执行引擎
✅ 第15步: 实现Plan查询操作(CT="R") → nid / id / now / del (仅sr未完成)
✅ 第16步: 修复工作计划模块Bug → 禁用保留数据/删除协议对齐/结构体packed/分钟容错/cns过滤/执行日志
               ↓
第17步 (待做): 补齐属性读取RunTm/LightTm/Angle → 3行代码
               ↓
第18步 (待做): 实现告警配置读写(SV="alam") → 新handler + Flash存储
               ↓
第19步 (待做): 实现日出日落查询(Plan DO="sr") → NOAA天文算法
               ↓
第20步 (待做): Signal改为AT查询真实值
               ↓
第21步 (待做): EleInfo.e接入BL0942电能数据
               ↓
第22步 (待做): 告警阈值改为alam可配置
               ↓
第23步 (待做): 接入关灯异常(10005)/灯杆倾斜(10006)采集源
```

---

## 九、资源预估（MDK-ARM-8008000工程）

| 项目 | 初版 | 当前值 | 预估新增 | 预估总量 | 安全阈值 | 状态 |
|------|------|--------|----------|----------|----------|------|
| ROM | ~60KB | ~68KB | ~8KB | ~76KB | <100KB | ✅ |
| RAM | ~30KB | ~32KB | ~2KB | ~34KB | <40KB | ✅ |
| Stack | 2KB (0x800) | 2KB | 不变 | 2KB | ≥2KB | ✅ |
| Heap | 512B (0x200) | 512B | 不变 | 512B | - | ✅ |

> 注：zk_work_plan.c 从初版~912行增至~1271行（+359行，查询功能+bug修复）；mqtt_zk_protocol.c 从~3249行微增至~3390行。ROM从初版~60KB增至~68KB。预计alam+sr+信号+电能补齐后ROM总量约76KB，仍在100KB安全阈值内。

---

## 十、实机验证指南

> 以下所有验证操作均需设备已烧录最新固件、4G模块已注册网络、已连接到MQTT服务器`47.120.15.220:1883`。
> 推荐使用 **MQTTX** 或 **MQTT Explorer** 作为桌面端测试工具。

### 10.0 验证前准备

#### 10.0.1 工具准备

| 工具 | 用途 | 获取方式 |
|------|------|----------|
| MQTTX | MQTT桌面客户端，用于收发协议报文 | https://mqttx.app |
| 串口调试助手 | 查看设备串口日志(115200bps) | 任意串口工具 |
| USB转TTL模块 | 连接设备调试串口 | 3.3V电平 |
| 可调交流电源 | 模拟过压/欠压告警 | 0~300V可调 |
| 万用表 | 测量实际电压/电流 | - |

#### 10.0.2 MQTTX连接配置

```
Broker: 47.120.15.220
Port: 1883
Client ID: test_tool_<随机数>
Username: (留空)
Password: (留空)
```

#### 10.0.3 订阅主题

首先订阅以下主题以监听设备上报：

| 主题 | 说明 |
|------|------|
| `MS/<IMEI>/dev2plt` | 设备→平台上行数据（主要观察） |
| `MS/<IMEI>/dev2pcp` | 设备→平台OTA数据 |
| `MS/<IMEI>/offline` | 设备离线遗嘱消息 |

> 将 `<IMEI>` 替换为设备的15位IMEI号（可查看设备外壳标签或串口启动日志）。

#### 10.0.4 发布主题

平台通过以下主题向设备下发指令：

| 主题 | 说明 |
|------|------|
| `MS/<IMEI>/plt2dev` | 平台→设备下行指令（属性读写、控制、校时等） |
| `MS/<IMEI>/pcp2dev` | 平台→设备OTA升级指令 |

---

### 10.1 登录验证 (CT="L")

**目的：** 验证设备上电后能正确发送登录报文，并收到平台登录确认。

**验证步骤：**

1. 给设备上电，等待约10~20秒（4G模块注册网络 + MQTT连接）
2. 在MQTTX中观察主题 `MS/<IMEI>/dev2plt`，应收到登录上报报文

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "L",
  "DT": {
    "DevInfo": {
      "protId": 100,
      "clas": "MS-SLC-01",
      "prottp": "iX7-075SC028-4G",
      "hver": 100,
      "sver": <APP_VERSION>,
      "mver": "BC28GJAR01A01",
      "iccid": "<20位ICCID>"
    },
    "MdlInfo": { "mdlId": "<IMEI>" },
    "Gis": { "lng": ..., "lat": ..., "zone": 8 },
    "Dim": [{ "cns": 1, "dim": 0, "sov": 0 }],
    "Sense": {}
  }
}
```

**验证通过标准：**
- ✅ 收到CT="L"的登录上报
- ✅ DT中包含DevInfo、MdlInfo、Gis、Dim、Sense五个属性组
- ✅ DevInfo.iccid为20位字符串
- ✅ MdlInfo.mdlId等于设备IMEI
- ✅ 设备串口日志显示"login sent"

---

### 10.2 心跳验证 (CT="H")

**目的：** 验证设备登录成功后每60秒发送心跳，平台回复ACK后设备维持在线。

**验证步骤：**

1. 确认设备已登录成功
2. 在MQTTX中等待约60秒，观察 `MS/<IMEI>/dev2plt`
3. 检查心跳报文的MID（消息ID）是否逐次递增

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "H"
}
```

**验证通过标准：**
- ✅ 每60秒±5秒收到一条CT="H"的心跳上报
- ✅ 心跳报文无DT节点，结构精简
- ✅ MID逐次递增不重复

---

### 10.3 周期上报验证 (CT="C")

**目的：** 验证设备按配置周期（默认300秒）上送完整运行数据。

**验证步骤：**

1. 确认设备已登录成功
2. 等待约5分钟（默认300秒周期），观察 `MS/<IMEI>/dev2plt`
3. 检查报文内容完整性

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "C",
  "DT": {
    "RunTm": { "tTmLt": ..., "rTmLt": ... },
    "LightTm": { "tLtTime": [...], "rLtTime": [...] },
    "RunSts": { "sts": [...], "bri": [...] },
    "EleInfo": {
      "e": [...], "c": [...], "v": [...],
      "f": [...], "p": [...], "rEc": [...], "tEc": [...]
    },
    "PerSts": { "lux": 0, "temp": ... },
    "Signal": { "rsrp": ..., "snr": 0, "cellid": 0, "pci": 0, "reg": 1 },
    "Angle": { "x": 0, "y": 0, "z": 0 }
  }
}
```

**验证通过标准：**
- ✅ 每300秒±10秒收到一条CT="C"的周期上报
- ✅ DT中包含RunTm、LightTm、RunSts、EleInfo、PerSts、Signal、Angle全部7个属性组
- ✅ EleInfo.v(电压)、c(电流)、p(功率)数值在合理范围内

---

### 10.4 变化上报验证 (CT="B")

**目的：** 验证设备亮度或开关状态变化时，立即触发变化上报。

**验证步骤：**

1. 确认设备已登录且正常运行
2. 通过MQTTX向 `MS/<IMEI>/plt2dev` 发送调光指令改变亮度：
   ```json
   {
     "SN": "<IMEI>",
     "SV": "ctrl",
     "CT": "W",
     "DT": { "cnCtrl": [{ "cns": 1, "bri": 50, "last": 0 }] }
   }
   ```
3. 观察 `MS/<IMEI>/dev2plt`，应在调光完成后立即收到变化上报

**预期报文格式：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "B",
  "DT": {
    "RunSts": { "sts": [1], "bri": [50] },
    "Dim": [{ "cns": 1, "dim": 0, "sov": 0 }]
  }
}
```

**验证通过标准：**
- ✅ 亮度/开关变化后3秒内收到CT="B"的变化上报
- ✅ DT.RunSts.bri值与调光指令一致
- ✅ 设备串口日志出现"change report triggered"

---

### 10.5 属性读取验证 (SV="prop", CT="R")

**目的：** 验证平台可通过属性读取指令查询设备各类属性。

#### 10.5.1 读取设备信息 (DevInfo)

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["DevInfo"]}}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","ER":0,"DT":{"DevInfo":{"protId":100,"clas":"MS-SLC-01","prottp":"iX7-075SC028-4G","hver":100,"sver":<版本>,"mver":"BC28GJAR01A01","iccid":"<ICCID>"}}}
```

#### 10.5.2 读取运行状态 (RunSts) + 电气信息 (EleInfo) + 信号 (Signal)

**批量发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["RunSts","EleInfo","Signal"]}}
```

**预期收到回复：** DT中包含RunSts（sts/bri）、EleInfo（e/c/v/f/p/rEc/tEc/pwr/lc）、Signal（rsrp/snr/cellid/pci/reg）三个属性组。

#### 10.5.3 读取不支持的属性 (RunTm/LightTm/Angle)

**发送：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","DT":{"items":["RunTm"]}}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"R","ER":1,"DT":{}}
```
> 当前RunTm/LightTm/Angle尚未加入属性读取分发表，返回ER=1。

**验证通过标准：**
- ✅ 支持的13组属性均返回ER=0且数据完整
- ✅ 不支持的属性返回ER=1
- ✅ items为空数组时返回ER=7

---

### 10.6 属性写入验证 (SV="prop", CT="W")

**目的：** 验证平台可通过属性写入指令修改设备配置，且配置持久化到Flash。

#### 10.6.1 写入Gis（地理位置）

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Gis": { "lng": 120123456, "lat": 30123456, "zone": 8 }
  }
}
```

**预期收到回复：** ER=0

**验证持久化：** 设备断电重启后，读取Gis属性，确认经纬度值已保存。

#### 10.6.2 写入Svr（服务器参数）

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Svr": { "svrIp": "47.120.15.220", "svrPort": 1883, "uPeriod": 600, "hPeriod": 120, "tPeriod": 7200 }
  }
}
```

**预期收到回复：** ER=0
**验证：** 读取Svr属性，确认周期参数已更新。

#### 10.6.3 范围校验验证

**发送超出范围的lng值：**
```json
{
  "SN": "<IMEI>",
  "SV": "prop",
  "CT": "W",
  "DT": {
    "Gis": { "lng": 200000000, "lat": 30123456 }
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"prop","CT":"W","ER":3,"DT":{}}
```

**验证通过标准：**
- ✅ 正常值写入返回ER=0
- ✅ 超出范围返回ER=3
- ✅ 错误类型返回ER=2
- ✅ 断电重启后写入值不丢失（Flash持久化验证）

---

### 10.7 控制命令验证 (SV="ctrl", CT="W")

**目的：** 验证平台可通过控制指令远程调光、重启、恢复出厂设置、巡检。

#### 10.7.1 cnCtrl调光控制

**发送（立即调光到80%亮度，30分钟后恢复）：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": {
    "cnCtrl": [{ "cns": 1, "bri": 80, "last": 30 }]
  }
}
```

**验证步骤：**
1. 发送指令后，肉眼观察灯具亮度变为80%
2. 30分钟后，观察灯具亮度恢复到调光前的值
3. 期间应收到CT="B"的变化上报

**预期：**
- ✅ 灯具亮度立即变为80%
- ✅ ER=0响应
- ✅ 约30分钟后亮度自动恢复
- ✅ 亮度变化触发变化上报

#### 10.7.2 cnCtrl last=0（永久调光）

**发送（永久调到60%，不恢复）：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": {
    "cnCtrl": [{ "cns": 1, "bri": 60, "last": 0 }]
  }
}
```

**预期：**
- ✅ 灯具亮度变为60%
- ✅ 持续观察3分钟以上，亮度不恢复

#### 10.7.3 reboot重启

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": { "DO": "reboot" }
}
```

**预期：**
- ✅ 立即收到ER=0响应
- ✅ 约500ms后设备重启
- ✅ MQTT连接断开（收到offline遗嘱消息）
- ✅ 设备重启后自动重新连接MQTT并发送登录上报

#### 10.7.4 patrol巡检

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "ctrl",
  "CT": "W",
  "DT": { "DO": "patrol" }
}
```

**预期：**
- ✅ ER=0响应
- ✅ 随后立即收到一条CT="C"的周期上报（包含完整运行数据）

---

### 10.8 告警上报验证 (CT="A")

**目的：** 验证设备在异常条件下能正确检测并上报告警。

#### 10.8.1 过压告警 (almId=10000)

**验证方法：**
1. 使用可调交流电源为设备供电
2. 将电压调至触发阈值以上
3. 观察MQTTX中 `MS/<IMEI>/dev2plt` 是否出现告警

> 注意：此验证涉及高压操作，请在确保安全的前提下进行，或使用信号发生器模拟电压采样值。

**预期报文：**
```json
{
  "SN": "<IMEI>",
  "SV": "rept",
  "CT": "A",
  "DT": {
    "alm": {
      "almId": 10000,
      "almStatus": 1,
      "almValue": <实测电压>,
      "almThreshold": 3200
    }
  }
}
```

**验证通过标准：**
- ✅ 电压超过阈值后30秒内上报告警（almStatus=1）
- ✅ 电压恢复正常后上报恢复（almStatus=0）
- ✅ 同一告警状态不重复上报

#### 10.8.2 其他告警快速验证

| 告警ID | 告警名称 | 模拟方法 | 预期 |
|--------|----------|----------|------|
| 10001 | 欠压 | 调低电压至80V以下 | 上报almStatus=1 |
| 10002 | 过流 | 接入大功率负载 | 上报almStatus=1 |
| 10007 | 漏电 | 输出端对地接电阻 | 上报almStatus=1 |
| 10008 | 设备故障 | 加热NTC传感器 | 上报almStatus=1 |
| 10009 | 设备失电 | 断开交流供电 | 上报一次almStatus=1(one_shot) |

---

### 10.9 校时功能验证 (SV="rqst")

**目的：** 验证设备能主动请求校时，且平台响应后正确校准RTC。

#### 10.9.1 主动校时请求

等待约1小时（默认3600秒周期），观察 `MS/<IMEI>/dev2plt`：

**预期报文：**
```json
{
  "SN": "<IMEI>",
  "SV": "rqst",
  "CT": "R",
  "DT": { "DO": "TmCali" }
}
```

#### 10.9.2 平台响应校时

**在MQTTX中回复（模拟平台）：**
```json
{
  "SN": "<IMEI>",
  "SV": "rqst",
  "CT": "R",
  "ER": 0,
  "DT": {
    "TmCali": { "time": "2026-05-19 15:30:00" }
  }
}
```

**验证通过标准：**
- ✅ 约3600秒后设备自动发送TmCali请求
- ✅ 收到校时响应后设备RTC被校准
- ✅ 登录响应中的time与RTC相差>30秒时自动覆盖

---

### 10.10 OTA升级验证 (SV="ota")

**目的：** 验证设备能接收OTA指令、下载固件、上报进度、完成升级。

#### 10.10.1 下发OTA指令

**发送：**
```json
{
  "SN": "<IMEI>",
  "SV": "ota",
  "CT": "W",
  "DT": {
    "url": "http://192.168.1.100:8080/firmware_v1.2.3.bin"
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"ota","CT":"W","ER":0,"DT":{}}
```

#### 10.10.2 观察OTA进度

在 `MS/<IMEI>/dev2pcp` 主题观察进度上报：

```json
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":30}}
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":50}}
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":90}}
{"SN":"<IMEI>","SV":"ota","CT":"P","DT":{"progress":100}}
```

**验证通过标准：**
- ✅ 收到OTA指令后ER=0响应
- ✅ 进度上报30→50→90→100依次出现
- ✅ 下载完成后设备自动重启进入bootloader
- ✅ OTA失败时上报CT="E"错误
- ✅ 无效URL返回ER=91

---

### 10.11 工作计划验证 (SV="plan") — 已更新

**目的：** 验证设备能接收工作计划、查询计划、删除计划、持久化存储、按时间表自动执行调光。

#### 10.11.1 下发周计划

**发送（每周一至周五 08:00亮度80%、18:00亮度30%）：**
```json
{
  "SN": "<IMEI>",
  "SV": "plan",
  "CT": "W",
  "DT": {
    "plan": {
      "id": 1,
      "en": 1,
      "type": 1,
      "priority": 5,
      "sDate": "2026-01-01 00:00:00",
      "eDate": "2026-12-31 23:59:59",
      "week": "1111100",
      "jobs": [
        {
          "cns": [1],
          "timetp": 0,
          "time": ["08:00", "18:00"],
          "bri": [80, 30]
        }
      ]
    }
  }
}
```

**预期收到回复：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","ER":0,"DT":{}}
```

#### 10.11.2 查询有效计划列表 (nid) — ✅ 已可用

**发送：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"R","DT":{"DO":"nid"}}
```

**预期回复（假设有id=1和id=3两个启用计划）：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"R","ER":0,"DT":{"nid":[1,3]}}
```

#### 10.11.3 查询指定计划内容 (id) — ✅ 已可用

**发送：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"R","DT":{"id":1}}
```

**预期回复：** 返回id=1的完整计划JSON（含sDate/eDate/week/jobs等全部字段）。

#### 10.11.4 查询当前运行计划 (now) — ✅ 已可用

**发送（查询通道1当前执行的计划）：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"R","DT":{"now":1}}
```

**预期回复（当前有匹配计划时）：** 返回完整plan JSON。当前无匹配时返回空DT。

#### 10.11.5 删除计划 (del) — ✅ 已可用

**发送（批量删除id=1和id=3）：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","DT":{"del":[1,3]}}
```

**预期回复：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","ER":0,"DT":{}}
```

**验证：** 再次发送nid查询，确认id=1和id=3不再出现在有效计划列表中。

#### 10.11.6 禁用计划验证 (en=0)

**发送（禁用id=1计划，仅需id和en两个字段）：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","DT":{"plan":{"id":1,"en":0}}}
```

**预期回复：** ER=0

**验证配置保留：**
```json
// 查询id=1的计划内容
{"SN":"<IMEI>","SV":"plan","CT":"R","DT":{"id":1}}
// 预期返回: {"plan":{"id":1,"en":0}} — 其他字段在内部保留
```

**再次启用：**
```json
{"SN":"<IMEI>","SV":"plan","CT":"W","DT":{"plan":{"id":1,"en":1, ...原配置...}}}
```
> 注意：重新启用时需要重新下发完整配置。内部虽然保留了数据，但当前协议不支持"仅翻转en位"的语义。

#### 10.11.7 验证计划自动执行

1. 确认设备RTC时间正确
2. 设置计划时间点为当前时间+2分钟
3. 到时间观察灯具亮度变化
4. 串口日志应输出：`[plan] exec id=1 job=0 act=0 bri=80`

**验证通过标准：**
- ✅ 写入计划返回ER=0
- ✅ 设备断电重启后计划不丢失
- ✅ 到计划时间±1分钟内，灯具亮度自动变化（含+1分钟容错）
- ✅ 多个计划同时生效时，priority值大的优先执行
- ✅ 同一分钟内同一计划不重复执行
- ✅ nid/id/now/del查询全部返回正确数据
- ✅ en=0禁用后计划不执行，但内部配置保留
- ✅ del删除后计划不再出现在nid列表中

#### 10.11.8 当前不可用的查询 (sr) — ❌

| 操作 | 请求示例 | 当前结果 |
|------|----------|----------|
| 查询日出日落时间 | `{"SN":"...","SV":"plan","CT":"R","DT":{"DO":"sr"}}` | ER=1 |

> DO="sr"需要NOAA天文算法支持，预计下一版本实现。

---

### 10.12 Flash持久化验证

**目的：** 验证设备配置在断电重启后正确保留。

**验证步骤：**

1. 通过属性写入修改Gis（经纬度）和Svr（上报周期）
2. 确认写入成功（ER=0）
3. 读取Gis和Svr，确认值已更新
4. 断电重启，重新上电等待登录
5. 读取Gis和Svr属性

**验证通过标准：**
- ✅ 重启后Gis和Svr值与写入值一致
- ✅ 周期上报按新周期执行
- ✅ 工作计划断电不丢失

---

### 10.13 CRC密码验证

**目的：** 验证设备生成的MQTT密码符合中科协议规范。

**验证方法（通过串口日志或测试脚本）：**
```bash
cd tests
python3 test_login_validation.py
```

**测试向量：**
| IMEI | 预期密码 |
|------|----------|
| 864294053651521 | BD9D0EE1D3E1 |
| 860608074646596 | FBE709622252 |
| 868120257654321 | 707D3A606B13 |

**验证通过标准：**
- ✅ 三个测试向量全部匹配
- ✅ MQTT服务器接受设备连接（说明密码认证通过）

---

### 10.14 端到端集成验证流程

**目的：** 一次性验证设备完整生命周期。

**完整验证流程（约30分钟）：**

| 步骤 | 操作 | 观察点 | 时长 |
|------|------|--------|------|
| 1 | 设备上电 | 串口日志：IMEI读取、4G注册、MQTT连接 | ~20秒 |
| 2 | 自动登录 | MQTTX收到CT="L"登录上报 | 即时 |
| 3 | 首次心跳 | MQTTX收到CT="H"心跳 | ~60秒 |
| 4 | 写入配置 | 修改Gis/Svr属性，ER=0 | 即时 |
| 5 | 调光指令 | 下发cnCtrl bri=50，灯具变暗 | 即时 |
| 6 | 变化上报 | 收到CT="B"上报 | <3秒 |
| 7 | 恢复验证 | 等待last分钟数后亮度恢复 | 按参数 |
| 8 | 断电重启 | 断开电源后重新上电 | ~30秒 |
| 9 | 持久化验证 | 读取Gis/Svr，确认值保留 | 即时 |
| 10 | 周期上报 | 收到CT="C"完整周期数据 | ~5分钟 |
| 11 | 下发计划 | 写入周计划，到点自动执行 | 按计划时间 |
| 12 | 查询计划 | nid/id/now查询，确认数据正确 | 即时 |
| 13 | 删除计划 | del删除，nid确认已移除 | 即时 |
| 14 | 巡检指令 | 下发patrol，收到即时周期数据 | 即时 |
| 15 | OTA升级 | 下发OTA URL，观察进度→跳boot | ~2分钟 |

**最终验收标准：**
- ✅ 设备从冷启动到成功登录 < 30秒
- ✅ 心跳60秒间隔稳定 ±5秒
- ✅ 周期上报内容完整、数值合理
- ✅ 属性读写双向通信正常（13组读 + 5组写）
- ✅ 控制指令响应正确、灯光变化符合预期
- ✅ 告警检测灵敏、不误报不漏报
- ✅ 配置断电重启不丢失
- ✅ OTA升级流程完整可用
- ✅ 计划写入/查询/删除/自动执行全链路正常

---

### 10.15 常见问题排查

| 现象 | 可能原因 | 排查方法 |
|------|----------|----------|
| 设备不上报登录 | IMEI未正确读取或SIM卡未注册 | 检查串口日志IMEI打印；确认SIM卡有流量 |
| 登录报ER≠0 | CRC密码错误 | 用测试向量验证crc16_modbus输出 |
| 心跳中断 | 网络波动或服务器断开 | 检查4G信号质量；观察设备是否自动重连 |
| 属性读取返回ER=1 | 该属性组未加入分发表 | RunTm/LightTm/Angle暂不支持prop读 |
| 配置重启丢失 | Flash写入失败 | 检查串口是否有"Flash save error"日志 |
| 计划未按时执行 | RTC时间不准或计划不在有效期内 | 读取RTC属性确认时间；检查计划en/start/end/week |
| 计划查询返回ER=1 | 除sr外均已支持 | DO="sr"暂未实现 |
| OTA下载失败 | URL不可达或网络问题 | 确认HTTP服务器可达；检查URL格式 |
| 告警不触发 | 阈值不匹配或采集源未接入 | 检查对应almId的threshold配置；10005/10006未接入 |

---

**文档更新时间：** 2026-05-19
**基于：** 重构开发方案v2.0.md + 中科协议.docx + 当前代码(commit 25d01d5)交叉复核
