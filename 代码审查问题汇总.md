# 代码审查问题汇总

> 审查范围：`Core/Src/LampProtocolLib/` 目录下新增及修改的文件
> 审查日期：2026-05-22

---

## 问题分级说明

| 等级 | 含义 |
|------|------|
| **P1** | 必须修复：链接失败 / 功能错误 / 数据丢失 |
| **P2** | 应该修复：功能缺陷 / Flash 磨损放大 |
| **观察** | 当前逻辑正确，但存在可维护性或健壮性隐患 |

---

## P1 - 必须修复（3 项）

### P1-1: `zk_sunriset.c` 未加入 Keil 工程文件 → 链接失败

**文件**: `MDK-ARM-8008000/project.uvprojx`

**错误描述**:
`zk_sunriset.c` 文件已创建并放置于 `Core/Src/LampProtocolLib/` 目录下，但未添加到 Keil MDK 工程文件 `project.uvprojx` 的编译目标中。

**引用链**:
```
main.c
  └─ zk_work_plan_process()                  [main.c:236]
       └─ zk_sunriset_get()                  [zk_work_plan.c:968]
            └─ zk_sunriset.c → 缺失，链接失败

mqtt_zk_protocol.c (session_process)
  └─ DO="sr" 日出日落查询                    [mqtt_zk_protocol.c:2255]
       └─ zk_sunriset_get()                  → 缺失，链接失败
```

**MCU 性能影响**: 无。编译阶段即失败，无法生成固件。

**固件体积影响**: 修复后，`zk_sunriset.c` 引入 `<math.h>` 浮点库（`sinf`/`cosf`/`tanf`/`acosf`/`asinf`/`atanf`/`fmodf`/`floorf` 共 14 处调用），预计增加 **2~4 KB** 代码段。

**主循环阻塞风险**: 不适用（无法链接）。

---

### P1-2: Flash 属性记录扩容后无旧版本兼容迁移 → 升级后配置丢失

**文件**: `Core/Src/LampProtocolLib/zk_property.c`

**错误描述**:
`zk_property_flash_record_t` 结构体新增了 3 个数组字段（`almValue[10]`、`almRecValue[10]`、`almEn[10]`），共计 30 个 `s32` = **120 字节**的增量。

`zk_property_record_valid()` (line 116-125) 的校验逻辑：
```c
if (record->magic != ZK_PROPERTY_FLASH_MAGIC ||
    record->version != ZK_PROPERTY_FLASH_VERSION ||   // 仍为 1U，未递增
    record->size != sizeof(*record))                   // ★ 旧记录 size 不匹配
{
    return BOOL_FALSE;
}
```

旧设备 Flash 中的记录 size 为旧结构体大小（不含告警数组），与新 `sizeof(*record)` 不一致 → `zk_property_record_valid()` 返回 `BOOL_FALSE`。

`zk_device_config_init()` (line 290-295) 的初始化流程：
```c
void zk_device_config_init(void)
{
    zk_device_config_set_defaults(&zk_dev_cfg);    // 先设硬编码默认值
    (void)zk_property_flash_load(&zk_dev_cfg);      // 加载失败，静默忽略返回值
    ...
}
```

加载失败后设备使用硬编码默认值，**已配置设备的经纬度、服务器 IP/端口、上报周期等关键参数全部丢失**。

**影响范围**:
- 经纬度 (`lng`/`lat`) → 回退到默认 `120.0, 30.0`（杭州附近），非设备实际位置
- 时区 (`zone`) → 回退到默认 `8`
- 服务器地址 (`svrIp`/`svrPort`) → 回退到默认 `47.120.15.220:1883`
- 上报周期 (`uPeriod`/`hPeriod`/`tPeriod`) → 回退到默认值
- 告警阈值 (`almValue`/`almRecValue`/`almEn`) → 回退到硬编码默认值

**MCU 性能影响**: 无。仅在启动时执行一次 Flash 读取。

**固件体积影响**: 无。结构体大小变化不影响 `.text` 段。

**主循环阻塞风险**: 无。初始化阶段完成，不在主循环中。

---

### P1-3: 时区 `zone` 字段语义不匹配 → 日出日落时间计算错误（偏差 60 倍）

**文件**: `Core/Src/LampProtocolLib/zk_sunriset.c` (line 174)

**错误描述**:
```c
tz_min = cfg->zone;  /* 时区偏移分钟，如东八区=480 */
```

注释写"如东八区=480"，但实际赋值为 `cfg->zone`，其值为 **小时** 而非分钟：

| 证据 | 位置 |
|------|------|
| 默认值 `config->zone = 8` | `zk_property.c:255` |
| 属性上报 `cJSON_AddNumberToObject(item, "zone", zk_dev_cfg.zone)` 上报 `8` | `zk_property.c:388` |
| 写入校验 `if (value < -12 \|\| value > 12)` — 小时范围 | `zk_property.c:805` |

因此 `sr_calc()` 收到的 `tz_min = 8`（分钟），等价于 UTC+0:08，而非预期的 UTC+8:00（= 480 分钟）。

**后果**:
- 日出/日落 UTC 时间转换为本地时间时偏移仅 8 分钟
- 实际东八区设备计算的本地日出/日落时间偏差约 **7 小时 52 分钟**
- `type=2`（日出日落相对）工作计划在完全错误的时间触发
- `DO="sr"` 查询返回的日出日落时间完全不对

**MCU 性能影响**: 无。修复仅需 `tz_min = cfg->zone * 60;`，零性能开销。

**固件体积影响**: 几乎为零（一条乘法指令）。

**主循环阻塞风险**: 无。日出日落计算由缓存保护，每天仅执行一次。

---

## P2 - 应该修复（2 项）

### P2-1: `almEn` 告警使能标志未控制告警采集与上报

**文件**: `Core/Src/LampProtocolLib/zk_alarm.c` (line 140-179)

**错误描述**:
`zk_alarm_collect_sources()` 中所有 10 路告警仅根据硬件错误标志判断，**完全没有读取 `cfg->almEn[index]`**：

```c
static void zk_alarm_collect_sources(void)
{
    cfg = zk_device_config_get();

    // 告警 0: 仅看 Error_3_OV，不检查 cfg->almEn[0]
    zk_alarm_update_level(&zk_alarm_states[0],
        Error_3_OV ? 1 : 0, ...);
    // 告警 1: 仅看 Error_4_LV，不检查 cfg->almEn[1]
    zk_alarm_update_level(&zk_alarm_states[1],
        Error_4_LV ? 1 : 0, ...);
    // ... 所有 10 路均无 almEn 检查
}
```

掉电告警同样未检查：
```c
static void zk_alarm_update_power_down(void)   // line 122
{
    // ...不检查 cfg->almEn[ZK_ALARM_POWER_DOWN_INDEX]
    if (power_down_flag != 0 && alarm->pending == 0)
    {
        alarm->pending = 1;                     // 直接标记上报
        alarm->pending_status = 1;
    }
}
```

平台通过 MQTT 下发 `almEn=0` 意图禁用某路告警后，设备仍继续采集和上报该路告警。

**修复建议**: 在每路 `zk_alarm_update_level()` 调用前增加判断：
```c
if (cfg->almEn[index] == 0) continue;  // 或跳过该路采集
```

**MCU 性能影响**: 正优化（跳过禁用的告警路数反而减少计算）。

**固件体积影响**: 几乎为零（每条增加一次数组读取 + 比较 + 条件跳转，约 10 字节/路）。

**主循环阻塞风险**: 无。`zk_alarm_process()` 在 `zk_mqtt_session_process()` 中仅采集并最多发送一条告警，不会长时间阻塞。

---

### P2-2: 告警批量配置逐条写 Flash → 部分生效 + Flash 磨损放大

**文件**: `Core/Src/LampProtocolLib/mqtt_zk_protocol.c` (line 2463-2503)

**错误描述**:
```c
for (index = 0; index < count; ++index)
{
    // ... 解析 almId, almValue, recValue, almEn ...

    if (zk_device_config_save_alam(alm_id, value, rec_value, alm_en) == BOOL_FALSE)
    {
        zk_publish_simple_response(header, 99);
        return BOOL_TRUE;    // ← 已保存的前几项无法回滚
    }
}
```

**两个问题**:

1. **部分生效**: 若第 N 项保存成功后才遇到校验失败，前 N 项已写入 Flash 并提交到 RAM，无法回滚。设备返回错误码但配置已部分变更。

2. **Flash 磨损放大**: `zk_device_config_save_alam()` 每次都写主 + 备两个 Flash 页（`FLASH_PAGE_SIZE = 0x800 = 2048 字节`）。10 条告警全部下发时擦写 **20 次 Flash 页**。Flash 典型擦写寿命 ~10,000 次（STM32F1 内置 Flash），高频配置变更会加速磨损。

**修复建议**: 先遍历数组校验所有项并构造候选 `zk_device_config_t`，全部通过后一次性调用 `zk_property_flash_store_config()` 保存，只擦写 2 次页。

**MCU 性能影响**: 修复后 Flash 擦写次数从 2N 降为 2（N 为告警数组大小），显著减少 Flash 操作时间（每次页擦除约 20-40ms）。

**固件体积影响**: 微小增加（构造候选配置的临时变量 + 一次额外 memcpy，约几十字节）。

**主循环阻塞风险**: **当前存在**。逐条写 Flash 时，每次 `hw_flash_write_bytes` → 页擦除（~20-40ms）× 最多 20 次 = **400-800ms 阻塞主循环**。修复后仅需 2 次擦写 = **40-80ms**。

---

## 观察项（8 项）

> 以下问题当前逻辑正确，不会导致功能异常，但存在可维护性或健壮性隐患。

### O-1: Flash 布局变更 —— 升级兼容性风险

**文件**: `zk_property.c` (struct)、`zk_work_plan.c` (struct)

**内容**:
- `zk_property_flash_record_t` 增大 120 字节（新增告警数组），`ZK_PROPERTY_FLASH_VERSION` 仍为 `1U`。
- `zk_plan_record_t` 新增 `setoffset`、`riseoffset` 字段，`ZK_PLAN_FLASH_VERSION = 2`（假设从 1 递增），旧计划数据因 version 不匹配被擦除。

| MCU 性能 | 无影响 |
| 固件体积 | 无影响（结构体定义不占 .text） |
| 主循环阻塞 | 无影响（仅启动时 Flash 加载） |

**缓解**: 确认服务端在设备 OTA 升级重启后能自动重新下发工作计划和属性配置。

---

### O-2: 浮点数学库性能开销

**文件**: `zk_sunriset.c`

**内容**: 共有 14 处浮点函数调用（`sinf`×4, `cosf`×2, `tanf`×1, `atanf`×1, `asinf`×1, `acosf`×1, `fmodf`×1, `floorf`×2, 以及算数运算）。在 Cortex-M3（无硬件 FPU）上，每次 `sinf`/`cosf` 约需 **数百至上千个 CPU 周期**。

**缓存机制** (line 27-33, 158-167): 以 `(year, mon, day)` 为 key，同一天内重复查询直接返回缓存值，**不触发重算**。时钟同步（仅变更时分秒）不会导致缓存失效。

| MCU 性能 | 每日仅计算一次（首次查询触发），约数千周期，可接受 |
| 固件体积 | 引入 libm 浮点库增加 **2~4 KB** `.text` |
| 主循环阻塞 | **存在**。首次调用 `zk_sunriset_get()` 时在调用线程同步计算，约数千周期阻塞。若此调用发生在 `zk_work_plan_process()`（main.c:236 每循环调用），每次循环首次 miss 时会阻塞。但同一天内后续调用命中缓存（O(1) 比较），不阻塞。 |

---

### O-3: `zk_device_config_save_alam()` 的 `index` 参数类型

**文件**: `zk_property.c:322-327`, `zk_property.h:10`

**内容**:
```c
boolean_en zk_device_config_save_alam(int index, ...)  // index 为 int
{
    if (index < 0 || index >= 10)   // < 0 检查对 int 有效
        return BOOL_FALSE;
```

如果将来将 `index` 改为 `unsigned`（如 `size_t`/`uint32_t`），`index < 0` 变为编译期 no-op，编译器可能产生警告。当前无 bug。

| MCU 性能 | 无影响 |
| 固件体积 | 无影响 |
| 主循环阻塞 | 无影响 |

---

### O-4: 日出日落偏移归一化的 while 循环

**文件**: `zk_work_plan.c:1028-1029`

**内容**:
```c
while (effective_minute < 0)    effective_minute += 1440;
while (effective_minute >= 1440) effective_minute -= 1440;
```

受限于 `setoffset`/`riseoffset` ∈ [-128, 127]、`action->minute` ∈ [0, 1439]，`effective_minute` 范围约 [-128, 3006]，while 循环最多 **2-3 次迭代**。逻辑正确，无 bug。

使用 `% 1440` 配合 `((x % 1440) + 1440) % 1440` 可在一次运算完成归一化，但 Cortex-M3 无硬件除法，取模运算本身较慢。当前 while 循环对已知有界范围是合理的。

| MCU 性能 | 每次匹配计算约 2-3 次整数加减，可忽略 |
| 固件体积 | 无影响 |
| 主循环阻塞 | 无影响（每个计划匹配时执行，循环次数有界） |

---

### O-5: `zk_device_config_get()` 在循环内重复调用

**文件**: `mqtt_zk_protocol.c:2480`

**内容**:
```c
for (index = 0; index < count; ++index)
{
    cfg = zk_device_config_get();   // 每次迭代都调用
    ...
}
```

`zk_device_config_get()` 仅返回全局指针 `&zk_dev_cfg`，无副作用、几乎零开销。将其提出循环可略微提升代码清晰度，但对性能和体积均无实际影响。

| MCU 性能 | 无影响（函数调用被编译器内联优化） |
| 固件体积 | 无影响 |
| 主循环阻塞 | 无影响 |

---

### O-6: `almId` JSON 解析的隐式 NULL 依赖

**文件**: `mqtt_zk_protocol.c:2472`

**内容**:
```c
alm_id = (int)cJSON_GetNumberValue(cJSON_GetObjectItem(alm_item, "almId"));
```

若 `"almId"` 字段缺失，`cJSON_GetObjectItem()` 返回 `NULL`，`cJSON_GetNumberValue(NULL)` 返回 `0.0` → `alm_id = 0`。随后的 `if (alm_id < 10000 || alm_id > 10009)` 将 `0` 拦截为 `err = 4`。当前有效，但依赖 `0` 不在合法 ID 范围 [10000, 10009] 内的隐含假设。

**建议**: 显式检查 `cJSON_GetObjectItem()` 返回值是否为 NULL，避免将来 ID 范围变更后埋坑。

| MCU 性能 | 无影响 |
| 固件体积 | 增加 NULL 检查约 4 字节 |
| 主循环阻塞 | 无影响 |

---

### O-7: `almEn` 类型转换三元表达式过于复杂

**文件**: `mqtt_zk_protocol.c:2495`

**内容**:
```c
// 外层已过滤非数字/非布尔类型
if (!cJSON_IsNumber(node) && !cJSON_IsBool(node)) { err = 2; break; }
alm_en = cJSON_IsTrue(node) ? 1 : (node->valueint != 0 ? 1 : 0);
```

逻辑正确但嵌套三元难以一眼看懂。可简化为：
```c
alm_en = (node->valueint != 0) ? 1 : 0;
```
因为 cJSON 将 `true` 存储为 `valueint=1`，`false` 为 `valueint=0`，数字直接取 `valueint`——结果等价。

| MCU 性能 | 无影响 |
| 固件体积 | 无影响（编译器会优化） |
| 主循环阻塞 | 无影响 |

---

### O-8: `timetp` 范围验证正确性确认

**文件**: `zk_work_plan.c:525`

**内容**:
```c
if (timetp->valueint < 0 || timetp->valueint > 2)  return 1;
```

验证范围 [0, 2] 正确覆盖了 `0=绝对时间`、`1=日出偏移`、`2=日落偏移`。对于 type=1 计划，`timetp` 默认为 `0`（line 530），不用于偏移计算——验证对所有计划类型都宽松合理。

| MCU 性能 | 无影响 |
| 固件体积 | 无影响 |
| 主循环阻塞 | 无影响 |

---

## 主循环阻塞风险总结

`main.c` 主循环 (`while(1)`) 调用链：

```
main loop
├─ zk_work_plan_process()           [每次循环]
│    ├─ zk_sunriset_get()           [每天首次：浮点计算 ~数千周期]
│    │    └─ 后续同日命中缓存 → 零开销
│    └─ 计划匹配遍历                 [常数时间，有界]
│
└─ json_process()
     └─ zk_mqtt_session_process()
          └─ zk_alarm_process()     [每次循环，但发送一条后退回]
               ├─ zk_alarm_collect_sources()   [10路状态更新，常数时间]
               └─ zk_alarm_publish_pending()   [最多发送1条MQTT消息]
```

| 风险点 | 阻塞时长 | 频率 |
|--------|----------|------|
| 日出日落计算 | ~数千 CPU 周期（无 FPU） | 每天 1 次 |
| 告警批量 Flash 写入 (P2-2) | **400-800ms** | 每次平台下发告警配置 |
| 计划 Flash 保存 | ~40-80ms | 每次平台下发计划 |
| 属性 Flash 保存 | ~40-80ms | 每次平台下发属性 |

**最严重的主循环阻塞来自 P2-2**：告警批量配置逐条写 Flash 可能导致主循环停滞近 1 秒。

---

## 修复优先级建议

| 顺序 | 问题 | 理由 |
|------|------|------|
| **1** | P1-1: 工程文件缺失 | 无法编译，阻塞所有后续工作 |
| **2** | P1-3: 时区单位错误 | 所有 type=2 计划和 DO="sr" 查询都错了 |
| **3** | P1-2: Flash 旧记录兼容 | OTA 升级后已部署设备丢失配置 |
| **4** | P2-1: almEn 不生效 | 告警功能缺陷，无法远程禁用告警 |
| **5** | P2-2: 批量 Flash 写入 | Flash 磨损 + 主循环长时间阻塞 |
