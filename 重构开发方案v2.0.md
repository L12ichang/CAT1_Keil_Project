# 中科MQTT协议重构实现计划

## 一、Context（背景与目标）

### 1.1 项目背景

本项目是基于STM32F103CBT6/HK32F103CCT6A的CAT.1智慧路灯控制系统，目前使用自定义的MQTT协议与私有服务器通信。根据业务需求，需要切换到中科智城标准的MQTT通信协议，实现与中科平台的完整对接。

### 1.2 现有协议特点

- 服务器：`43.143.123.19:1883`
- ClientId：`client_{IMEI_DEC}`
- 无密码认证
- 订阅主题：`CAT.1/{设备ID}`
- 发布主题：`Cat.1_device/{设备ID}`
- 消息格式：`{cmd, token, seq_num, openid, level/status/data}`
- 支持命令：以中科协议 `prop/ctrl/alam/plan/rept/rqst` 为准

### 1.3 中科协议要求

- 服务器：`47.120.15.220:1883`
- ClientId/登录名：`{IMEI}` (15位完整IMEI)
- 密码：IMEI分段CRC16取反拼接生成
- 订阅主题：`MS/{SN}/plt2dev`, `MS/{SN}/pcp2dev` (QoS=1)
- 发布主题：`MS/{SN}/dev2plt`, `MS/{SN}/dev2pcp`
- 消息格式：标准结构 `{SN, TM, SV, ID, CT, DT, ER}`
- 支持服务类型：prop(属性)、ctrl(控制)、alam(告警)、plan(工作计划)、rept(上报)、rqst(请求)、OTA升级

### 1.4 实现目标

完整实现中科协议文档中定义的所有功能：

- 6种服务类型消息处理
- 30+属性读取/写入
- 10种告警检测与上报
- 3种工作计划模式
- OTA升级流程
- 登录/周期/变化/心跳四种上报机制

---

## 二、关键文件清单

| 序号 | 文件路径                                      | 改动类型 | 主要职责                         |
| ---- | --------------------------------------------- | -------- | -------------------------------- |
| 1    | `Core/Src/LampProtocolLib/NbDriver.c`       | 修改     | MQTT连接参数、密码生成、主题配置 |
| 2    | `Core/Src/LampProtocolLib/NbDriver.h`       | 修改     | 新增状态枚举、函数声明           |
| 3    | `Core/Src/LampProtocolLib/Json_Protocol.c`  | 修改     | 协议解析入口、双协议兼容         |
| 4    | `Core/Src/sys_data.h`                       | 修改     | 扩展数据结构                     |
| 5    | `Core/Src/sys_data.c`                       | 修改     | 新增字段存储/加载                |
| 6    | `Core/Src/sys_Vo_Io.c`                      | 修改     | 告警标志映射                     |
| 7    | `Core/Src/LampProtocolLib/App.c`            | 修改     | 上报机制改造                     |
| 8    | `Core/Src/LampProtocolLib/ZK_Protocol.h`    | 新增     | 中科协议数据结构定义             |
| 9    | `Core/Src/LampProtocolLib/ZK_Protocol.c`    | 新增     | 中科协议实现核心                 |
| 10   | `Core/Src/LampProtocolLib/ZK_JsonHandler.c` | 新增     | JSON消息构建/解析                |
| 11   | `Core/Src/LampProtocolLib/ZK_JsonHandler.h` | 新增     | JSON处理接口                     |
| 12   | `Core/Src/LampProtocolLib/ZK_Alarm.c`       | 新增     | 告警检测与上报                   |
| 13   | `Core/Src/LampProtocolLib/ZK_WorkPlan.c`    | 新增     | 工作计划处理                     |
| 14   | `Core/Src/LampProtocolLib/ZK_OTA.c`         | 新增     | 中科OTA协议适配                  |

---

## 三、分阶段实现计划

### 第一阶段：基础架构改造

#### 任务1.1：创建中科协议数据结构定义

**新增文件：** `Core/Src/LampProtocolLib/ZK_Protocol.h`

**改动内容：**

```c
// 服务类型枚举
typedef enum {
    SV_PROP,    // 属性类
    SV_CTRL,    // 控制类
    SV_ALAM,    // 告警类
    SV_PLAN,    // 工作计划类
    SV_REPT,    // 上报类
    SV_RQST,    // 请求类
} ZK_ServiceType_en;

// 操作类型枚举
typedef enum {
    CT_R,       // 读
    CT_W,       // 写
    CT_L,       // 登录上报
    CT_C,       // 周期上报
    CT_B,       // 变化上报
    CT_A,       // 告警上报
    CT_H,       // 心跳上报
} ZK_OperationType_en;

// 告警ID定义 (10种)
typedef enum {
    ALM_OVER_VOLTAGE = 10000,
    ALM_UNDER_VOLTAGE = 10001,
    ALM_OVER_CURRENT = 10002,
    ALM_UNDER_CURRENT = 10003,
    ALM_OPEN_LAMP_ABNORMAL = 10004,
    ALM_CLOSE_LAMP_ABNORMAL = 10005,
    ALM_LAMP_POLE_TILT = 10006,
    ALM_LAMP_POLE_LEAKAGE = 10007,
    ALM_DEVICE_FAULT = 10008,
    ALM_DEVICE_POWER_LOSS = 10009,
} ZK_AlarmId_en;

// 各属性结构定义
// DevInfo, Gis, Dim, Sense, RunSts, EleInfo, Signal, Angle, PerSts, RTC
```

**影响分析：**

- 无负面影响，纯新增代码
- 为后续实现提供数据结构基础

---

#### 任务1.2：创建中科协议实现核心

**新增文件：** `Core/Src/LampProtocolLib/ZK_Protocol.c`

**改动内容：**

1. **CRC16密码生成函数** (关键算法)

```c
void ZK_Password_Generate(char *imei, char *password_out)
{
    // 算法说明：
    // 1. 将15位IMEI分成3段，每段5字节: 86429 | 40536 | 51521
    // 2. 每段计算CRC16-MODBUS (多项式0xA001，初始值0xFFFF)
    // 3. 结果取反 (~crc)
    // 4. 按3段-1段-2段顺序拼接为16进制字符串
    // 示例: IMEI=864294053651521 -> 密码=BD9D0EE1D3E1

    u16 crc1, crc2, crc3;
    crc1 = crc16_modbus_get(imei, 5);      // 前5字节
    crc2 = crc16_modbus_get(imei+5, 5);    // 中5字节
    crc3 = crc16_modbus_get(imei+10, 5);   // 后5字节

    crc1 = ~crc1;
    crc2 = ~crc2;
    crc3 = ~crc3;

    sprintf(password_out, "%04X%04X%04X", crc3, crc1, crc2);
}
```

**测试方法：**

- 使用IMEI `864294053651521` 验证，预期输出 `BD9D0EE1D3E1`
- 单元测试：输入不同IMEI验证输出格式正确性

**涉及代码：**

- 调用 `Core/Src/crc16_modbus.c` 中的CRC计算函数

---

#### 任务1.3：修改NbDriver.c MQTT连接参数

**修改文件：** `Core/Src/LampProtocolLib/NbDriver.c`

**改动点1：服务器地址和端口 (第445行附近)**

```c
// 原代码：
send_AT_Command_machine_star("AT+QMTOPEN=0,43.143.123.19,1883\r\n", ...);

// 改为：
send_AT_Command_machine_star("AT+QMTOPEN=0,219.135.145.178,13004\r\n", ...);
```

**改动点2：ClientId和密码 (第453-459行附近)**

```c
// 原代码：
static char Strclient[52] ="AT+QMTCONN=0,client_%u\r\n";
sprintf(sendStringBuf1, Strclient, CLIENT_ID);

// 改为：需要用户名和密码
// AT+QMTCONN=<client_idx>,<clientid>[,<username>,<password>]
static char zk_password[13];  // 密码缓存
ZK_Password_Generate((char*)IMEI, zk_password);

static char Strclient[80] ="AT+QMTCONN=0,\"%s\",\"%s\",\"%s\"\r\n";
sprintf(sendStringBuf1, Strclient, IMEI, IMEI, zk_password);
```

**改动点3：订阅主题 (第465-468行附近)**

```c
// 原代码：订阅单个主题
static char Strsub[52] ="AT+QMTSUB=0,1,\"CAT.1/%u\",0\r\n";

// 改为：订阅两个主题 (plt2dev和pcp2dev)
static char Strsub[80] ="AT+QMTSUB=0,1,\"MS/%s/plt2dev\",1,\"MS/%s/pcp2dev\",1\r\n";
sprintf(sendStringBuf, Strsub, IMEI, IMEI);
```

**改动点4：发布主题 (第567-569行和第579行附近)**

```c
// 原代码：
static char Strpub3[52] ="AT+QMTPUBEX=0,0,0,0,\"Cat.1_device/%u\",%d\r\n";

// 改为：
static char Strpub3[60] ="AT+QMTPUBEX=0,0,1,0,\"MS/%s/dev2plt\",%d\r\n";
sprintf(sendStringBuf3, Strpub3, IMEI, length);
```

**风险分析：**

- AT指令格式变更可能需要调整等待响应时间
- 密码生成错误会导致连接失败

**应对措施：**

- 增加调试日志输出密码值
- 调整 `atwaitCount` 参数增加等待时间

---

#### 任务1.4：扩展sys_data.h数据结构

**修改文件：** `Core/Src/sys_data.h`

**改动内容：**

```c
typedef struct {
    // ... 现有字段保持不变 ...

    // ===== 新增中科协议相关字段 =====
    // 经纬度配置
    s32 zk_lng;             // 经度 (±180000000, 单位0.000001度)
    s32 zk_lat;             // 纬度 (±90000000)
    s8  zk_zone;            // 时区 (±128)

    // 调光配置扩展
    u16 zk_dlmt;            // 下限电压 mV (500~2000, 默认1000)
    u16 zk_ulmt;            // 上限电压 mV (7000~10000, 默认9000)
    u8  zk_dimTp;           // 调光类型 (0模拟量/1PWM)
    u8  zk_polar;           // 极性 (0正逻辑/1反逻辑)
    u16 zk_rtPwr;           // 额定功率 W

    // 上报周期配置
    u16 zk_uPeriod;         // 上报周期 秒 (0~1800, 默认300)
    u16 zk_hPeriod;         // 心跳周期 秒 (0~1800, 默认60)
    u16 zk_tPeriod;         // 校时周期 秒 (0~7200, 默认3600)

    // 告警阈值配置
    u16 zk_ov_value;        // 过压阈值 0.1V (默认2740=274V)
    u16 zk_ov_recovery;     // 过压恢复阈值 (默认2640)
    u16 zk_uv_value;        // 欠压阈值 (默认1660)
    u16 zk_uv_recovery;     // 欠压恢复阈值 (默认1760)
    u16 zk_oc_percent;      // 过流百分比 (默认150%)
    u16 zk_uc_percent;      // 欠流百分比 (默认80%)

    // 工作计划
    u8  zk_plan_count;      // 工作计划数量
    plan_st zk_plans[7];    // 最多7个工作计划

} sys_data_st;
```

**影响分析：**

- sys_data_st结构体变大，Flash存储空间需确认
- 存储地址固定，需确保新增字段不影响现有数据读取

**应对措施：**

- 新增字段添加到结构体末尾，保证向后兼容
- 首次运行时检测版本号，自动填充默认值

---

### 第二阶段：协议处理层重构

#### 任务2.1：创建JSON消息处理模块

**新增文件：** `Core/Src/LampProtocolLib/ZK_JsonHandler.c` 和 `.h`

**核心函数设计：**

| 函数名                        | 功能                 | 输入                | 输出                   |
| ----------------------------- | -------------------- | ------------------- | ---------------------- |
| `ZK_ParseMessage()`         | 解析中科消息基础字段 | JSON字符串          | ZK_BaseMsg_st + DT节点 |
| `ZK_BuildResponse()`        | 构建响应消息         | request_id, er_code | JSON字符串             |
| `ZK_HandlePropertyRead()`   | 处理属性读取         | DT.props数组        | 属性JSON响应           |
| `ZK_HandlePropertyWrite()`  | 处理属性写入         | DT属性数据          | 写入结果               |
| `ZK_HandleControlCommand()` | 处理控制命令         | DT.ctrlname         | 执行结果               |
| `ZK_HandleAlarmConfig()`    | 处理告警配置         | DT.alms数组         | 配置结果               |
| `ZK_HandleWorkPlan()`       | 处理工作计划         | DT.plan数据         | 计划结果               |

**ZK_ParseMessage 实现示例：**

```c
int ZK_ParseMessage(char *json_str, ZK_BaseMsg_st *msg, cJSON **dt_node)
{
    cJSON *root = cJSON_Parse(json_str);
    if (!root) return -1;

    // 解析基础字段
    cJSON *sn = cJSON_GetObjectItem(root, "SN");
    cJSON *tm = cJSON_GetObjectItem(root, "TM");
    cJSON *sv = cJSON_GetObjectItem(root, "SV");
    cJSON *id = cJSON_GetObjectItem(root, "ID");
    cJSON *ct = cJSON_GetObjectItem(root, "CT");

    if (!sn || !sv || !id || !ct) {
        msg->ER = 5;  // 必要字段缺失
        cJSON_Delete(root);
        return -2;
    }

    // 填充结构体
    strncpy(msg->SN, sn->valuestring, 15);
    strncpy(msg->TM, tm->valuestring, 19);
    strncpy(msg->ID, id->valuestring, 6);

    // 解析SV类型
    if (strcmp(sv->valuestring, "prop") == 0) msg->SV = SV_PROP;
    else if (strcmp(sv->valuestring, "ctrl") == 0) msg->SV = SV_CTRL;
    // ... 其他类型

    // 解析CT操作
    if (strcmp(ct->valuestring, "R") == 0) msg->CT = CT_R;
    else if (strcmp(ct->valuestring, "W") == 0) msg->CT = CT_W;
    // ... 其他操作

    // 返回DT节点供后续处理
    *dt_node = cJSON_GetObjectItem(root, "DT");

    return 0;
}
```

---

#### 任务2.2：修改Json_Protocol.c入口函数

**修改文件：** `Core/Src/LampProtocolLib/Json_Protocol.c`

**改动内容：修改 `app_mqtt_rx()` 函数 (第738行附近)**

```c
void app_mqtt_rx(char *rx)
{
    // 判断协议类型：检查是否有SN字段（中科协议特征）
    if (strstr(rx, "\"SN\"") != NULL && strstr(rx, "\"SV\"") != NULL)
    {
        // 调用中科协议处理
        ZK_HandleMessage(rx);
        return;
    }

    // 原协议处理逻辑保持不变
    cJSON *root = cJSON_Parse(rx);
    // ... 现有代码
}
```

**影响分析：**

- 不影响原有协议功能
- 通过字段判断自动切换协议处理路径

---

### 第三阶段：上报机制实现

#### 任务3.1：登录上报(L)

**触发时机：** MQTT连接成功后立即上报

**上报内容：**

```json
{
    "SN": "860001234567890",
    "TM": "2024-04-02 08:30:30",
    "SV": "rept",
    "ID": "000001",
    "CT": "L",
    "DT": {
        "DevInfo": {
            "protId": 100,
            "SN": "860001234567890",
            "clas": "MS-DK-4G-011101110",
            "prodtp": "MS-LC-4G01",
            "hver": 100,
            "sver": 100
        },
        "Gis": {
            "lng": 121317948,
            "lat": 31199680,
            "zone": 8
        },
        "RTC": "2024-04-02 08:30:30"
    }
}
```

**实现位置：** `ZK_JsonHandler.c` 中的 `ZK_BuildLoginReport()`

**涉及数据源：**

- IMEI: `NbDriver.c` 中的 `IMEI[]` 数组
- 时间: `sys_aip1302.c` 中的RTC数据
- 经纬度: `sys_data.zk_lng/zk_lat`

---

#### 任务3.2：周期上报(C)

**触发时机：** 定时器周期触发 (默认300秒)

**改动文件：** `Core/Src/LampProtocolLib/App.c`

**改动内容：**

```c
// 在 appProcess() 中增加周期上报计数
static u16 zk_cycle_counter = 0;

void appProcess(void)
{
    // ... 现有逻辑

    // 中科周期上报
    if (++zk_cycle_counter >= sys_data.zk_uPeriod)
    {
        zk_cycle_counter = 0;
        ZK_DoCycleReport();
    }
}
```

**上报内容：** RunSts, EleInfo, Signal

---

#### 任务3.3：变化上报(B)

**触发条件：**

- 亮度变化超过阈值 (如10%)
- 开关状态变化
- 工作模式变化

**改动文件：** `Core/Src/sys_pwm.c`

**改动内容：**

```c
void sys_pwm_output(u8 persent)
{
    static u8 last_bri = 0;

    // ... 执行调光

    // 检测亮度变化
    if (abs(persent - last_bri) >= ZK_CHANGE_THRESHOLD)
    {
        ZK_DoChangeReport("bri", persent);
        last_bri = persent;
    }
}
```

---

#### 任务3.4：心跳上报(H)

**触发时机：** 定时周期 (默认60秒)

**上报内容：**

```json
{
    "SN": "...",
    "TM": "...",
    "SV": "rept",
    "ID": "...",
    "CT": "H"
}
```

---

### 第四阶段：控制命令处理

#### 任务4.1：回路控制 (cnCtrl)

**处理流程：**

```c
int ZK_Handle_cnCtrl(cJSON *dt)
{
    cJSON *cnCtrl = cJSON_GetObjectItem(dt, "cnCtrl");
    if (!cnCtrl || !cJSON_IsArray(cnCtrl)) return -1;

    cJSON *item;
    for (int i = 0; i < cJSON_GetArraySize(cnCtrl); i++)
    {
        item = cJSON_GetArrayItem(cnCtrl, i);
        u8 cns = cJSON_GetObjectItem(item, "cns")->valueint;
        u8 bri = cJSON_GetObjectItem(item, "bri")->valueint;
        u16 last = cJSON_GetObjectItem(item, "last")->valueint;

        // 执行调光
        dim_level = bri;
        dim_ready();

        // 如果有持续时间，启动定时器
        if (last > 0)
        {
            ZK_StartDurationTimer(cns, last, bri);
        }
    }

    return 0;
}
```

**涉及代码：**

- `net_dim.c` 中的 `dim_level` 变量
- `sys_pwm.c` 中的 `sys_pwm_output()` 函数

---

#### 任务4.2：复位控制 (reboot)

```c
int ZK_Handle_reboot(void)
{
    // 存储当前状态
    sys_data_store();

    // 响应成功后执行复位
    NVIC_SystemReset();
    return 0;
}
```

---

#### 任务4.3：恢复出厂 (restore)

```c
int ZK_Handle_restore(u8 restore_type)
{
    switch (restore_type)
    {
        case 0: // 全部恢复
            sys_data_default();
            break;
        case 1: // 仅属性
            // 恢复属性默认值
            break;
        case 2: // 仅告警
            // 恢复告警默认阈值
            break;
        // ... 其他类型
    }
    sys_data_store();
    return 0;
}
```

---

#### 任务4.4：巡检控制 (patrol)

```c
int ZK_Handle_patrol(void)
{
    // 立即上报所有属性
    ZK_DoFullPropertyReport();
    return 0;
}
```

---

### 第五阶段：告警机制实现

#### 任务5.1：告警检测映射

**新增文件：** `Core/Src/LampProtocolLib/ZK_Alarm.c`

**告警映射表：**

| 中科告警ID     | 现有标志/来源           | 检测逻辑位置            | 判断条件                     |
| -------------- | ----------------------- | ----------------------- | ---------------------------- |
| 10000 过压     | Error_3_OV              | sys_Vo_Io.c             | Vin >= 2740(274V)            |
| 10001 欠压     | Error_4_LV              | sys_Vo_Io.c             | Vin <= 1660(166V) && Vin > 0 |
| 10002 过流     | 新增                    | sys_bl0942.c            | I >= 额定电流*150%           |
| 10003 欠流     | 新增                    | sys_bl0942.c            | I <= 额定电流*80%            |
| 10004 开灯异常 | Error_0_linght          | sys_Vo_Io.c             | 开灯时功率异常低             |
| 10005 关灯异常 | 新增                    | sys_Vo_Io.c             | 关灯时功率异常高             |
| 10006 灯杆倾斜 | 新增传感器              | -                       | 倾斜角度 >= 阈值             |
| 10007 灯杆漏电 | danger_current_warn     | danger_current_check.c  | 漏电流 >= 30mA               |
| 10008 设备故障 | driver_temperarure_warn | sys_temp_over_protect.c | 温度异常                     |
| 10009 设备失电 | power_down_flag         | sys_pow_drop_check.c    | 检测到失电事件               |

**改动文件：** `Core/Src/sys_Vo_Io.c` 和 `sys_bl0942.c`

---

#### 任务5.2：告警上报实现

```c
void ZK_ReportAlarm(ZK_AlarmId_en almId, u8 status, u16 value)
{
    char buf[256];
    cJSON *root = cJSON_CreateObject();

    cJSON_AddStringToObject(root, "SN", (char*)IMEI);
    cJSON_AddStringToObject(root, "TM", ZK_GetCurrentTime());
    cJSON_AddStringToObject(root, "SV", "rept");
    cJSON_AddStringToObject(root, "ID", ZK_GenerateID());
    cJSON_AddStringToObject(root, "CT", "A");

    cJSON *dt = cJSON_CreateObject();
    cJSON *alm = cJSON_CreateObject();
    cJSON_AddNumberToObject(alm, "almId", almId);
    cJSON_AddStringToObject(alm, "time", ZK_GetCurrentTime());
    cJSON_AddNumberToObject(alm, "status", status);  // 1产生/0恢复
    cJSON_AddNumberToObject(alm, "cns", 1);          // 回路号
    cJSON_AddNumberToObject(alm, "value", value);
    cJSON_AddNumberToObject(alm, "threshold", ZK_GetAlarmThreshold(almId));

    cJSON_AddItemToObject(dt, "alm", alm);
    cJSON_AddItemToObject(root, "DT", dt);

    char *json_str = cJSON_Print(root);
    send_MQTT_Data(zk_pub_topic_plt, json_str);

    cJSON_Delete(root);
    free(json_str);
}
```

---

#### 任务5.3：告警配置读写

**读取告警配置 (SV=alam, CT=R):**

```c
int ZK_HandleAlarmRead(cJSON *dt)
{
    cJSON *alms = cJSON_GetObjectItem(dt, "alms");
    // 返回请求的告警配置
    // almId, almValue, recValue, almEn
}
```

**写入告警配置 (SV=alam, CT=W):**

```c
int ZK_HandleAlarmWrite(cJSON *dt)
{
    cJSON *alms = cJSON_GetObjectItem(dt, "alms");
    // 更新 sys_data 中的告警阈值
    // 存储到Flash
}
```

---

### 第六阶段：工作计划实现

#### 任务6.1：周日期工作计划

**新增文件：** `Core/Src/LampProtocolLib/ZK_WorkPlan.c`

**计划类型1 - 周日期计划：**

```c
typedef struct {
    u8 planId;          // 计划ID
    u8 planTp;          // 1=周日期
    u8 weekMask;        // 位掩码 (bit0周日~bit6周六)
    char startDate[11]; // 开始日期 "2024-01-01"
    char endDate[11];   // 结束日期
    u8 timeCount;       // 时间点数量
    u16 *timePoints;    // 时间点数组 (分钟偏移)
    u8 *briValues;      // 对应亮度数组
} ZK_WeekPlan_st;
```

**处理逻辑：**

```c
int ZK_HandleWeekPlan(ZK_BaseMsg_st *msg, cJSON *dt)
{
    // 解析计划数据
    // 存储到 sys_data.zk_plans[]
    // 启用计划执行定时器
}
```

---

#### 任务6.2：日出日落工作计划

**需要实现日出日落计算：**

```c
void ZK_CalcSunriseSunset(float lat, float lng, u16 date, u16 *rise, u16 *set)
{
    // 根据经纬度和日期计算日出日落时间
    // 使用天文算法
}
```

**涉及代码：**

- `sys_data.zk_lng/zk_lat` 经纬度数据
- `sys_aip1302.c` RTC时间

---

#### 任务6.3：光感工作计划

**需要lux传感器支持：**

- 从 `sys_Vo_Io.c` 或新增ADC通道读取光照度
- 设置lux阈值触发调光

---

### 第七阶段：OTA升级适配

#### 任务7.1：中科OTA协议适配

**新增文件：** `Core/Src/LampProtocolLib/ZK_OTA.c`

**OTA流程 (pcp协议)：**

| 步骤 | 主题    | 消息类型 | 功能                |
| ---- | ------- | -------- | ------------------- |
| 1    | pcp2dev | 查询版本 | 设备上报当前版本    |
| 2    | pcp2dev | 下载通知 | 服务器通知新版本URL |
| 3    | dev2pcp | 请求下载 | 设备请求固件数据    |
| 4    | pcp2dev | 固件数据 | 服务器下发固件包    |
| 5    | dev2pcp | 上报结果 | 设备上报下载结果    |
| 6    | pcp2dev | 执行升级 | 服务器通知执行升级  |
| 7    | dev2pcp | 上报结果 | 设备上报升级结果    |

**改动文件：** `Core/Src/LampProtocolLib/ota.c`

**改动内容：**

- 保持现有HTTP下载机制
- 增加中科协议消息封装
- 修改进度上报格式

---

### 第八阶段：属性读取/写入实现

#### 任务8.1：属性组读取实现

**支持属性组：**

| 属性组  | 字段                                | 数据来源        |
| ------- | ----------------------------------- | --------------- |
| DevInfo | protId,SN,clas,prodtp,hver,sver     | 静态定义/版本号 |
| Gis     | lng,lat,zone                        | sys_data存储    |
| Dim     | cns,dimTp,polar,dlmt,ulmt,rti,rtPwr | sys_data存储    |
| Sense   | di,sBri,sBriTm                      | sys_data存储    |
| Spread  | offset,window,interval              | sys_data存储    |
| Comm    | main,sub,sAuto                      | sys_data存储    |
| Svr     | svrIp,svrPort,uPeriod,hPeriod       | NbDriver配置    |
| RunTm   | tTime,rTime                         | sys_bl0942累计  |
| LightTm | tLtTime,rLtTime                     | sys_bl0942累计  |
| RunSts  | sts,bri                             | net_dim.c       |
| EleInfo | e,c,v,f,p,rEc,tEc,pwr               | sys_bl0942.c    |
| Signal  | rsrp,cellid,pci,snr,reg             | 4G模块AT查询    |
| Angle   | x,y,z                               | 传感器(如有)    |
| PerSts  | lux,temp                            | ADC/NTC         |
| RTC     | 时间字符串                          | sys_aip1302.c   |

---

#### 任务8.2：属性写入实现

**可写入属性：**

- Gis: lng, lat, zone
- Dim: dlmt, ulmt, rtPwr
- Sense: di, sBri, sBriTm
- Spread: offset, window, interval
- Comm: main, sub, sAuto
- Svr: svrIp, svrPort, uPeriod
- 告警阈值: almValue, recValue, almEn
- RTC: 时间同步

---

## 四、风险评估与应对

### 4.1 技术风险

| 风险项              | 等级 | 可能后果     | 应对措施                              |
| ------------------- | ---- | ------------ | ------------------------------------- |
| CRC密码生成算法错误 | 中   | MQTT连接失败 | 编写单元测试验证，与平台联调确认      |
| JSON内存溢出        | 中   | 系统崩溃     | 使用静态缓冲区，限制消息长度≤512字节 |
| 状态机兼容性问题    | 高   | 连接流程中断 | 分阶段替换，保留调试日志              |
| Flash空间不足       | 低   | 存储失败     | 当前50KB+新增30KB<112KB，空间充足     |
| OTA升级失败         | 高   | 设备变砖     | 保持回滚机制，增加校验                |
| 告警误报/漏报       | 中   | 业务异常     | 设置延时确认机制                      |
| 时间同步失败        | 低   | 数据异常     | 增加RTC校准，定期请求服务器时间       |

### 4.2 业务风险

| 风险项       | 等级 | 应对措施                         |
| ------------ | ---- | -------------------------------- |
| 平台对接失败 | 高   | 先测试环境验证，分阶段切换生产   |
| 命令解析错误 | 中   | 严格校验字段，返回错误码         |
| 历史数据丢失 | 中   | 数据结构向后兼容，首次运行初始化 |

---

## 五、测试验证方案

### 5.1 单元测试清单

| 测试项        | 测试方法             | 预期结果     |
| ------------- | -------------------- | ------------ |
| CRC密码生成   | 输入已知IMEI验证输出 | BD9D0EE1D3E1 |
| JSON构建/解析 | 构造测试消息解析验证 | 字段正确提取 |
| 属性读取      | 模拟平台请求读取     | 返回正确数据 |
| 属性写入      | 模拟平台下发参数     | 存储并执行   |
| 控制命令      | 模拟调光/复位命令    | 正确执行     |
| 告警触发      | 模拟异常条件         | 正确上报告警 |

### 5.2 集成测试清单

| 测试项       | 测试步骤             | 验证要点           |
| ------------ | -------------------- | ------------------ |
| MQTT连接流程 | 启动设备观察连接日志 | 连接成功，订阅成功 |
| 登录上报     | 连接后检查平台数据   | DevInfo正确上报    |
| 周期上报     | 等待周期触发         | RunSts/EleInfo正确 |
| 调光控制     | 平台下发亮度命令     | 设备响应并执行     |
| 告警上报     | 模拟过压/欠压条件    | 告警消息正确上报   |
| 工作计划     | 平台下发定时计划     | 设备按时执行       |
| OTA升级      | 平台触发升级流程     | 完整升级流程       |

### 5.3 联调测试流程

1. **环境准备：** 连接中科测试服务器
2. **基础验证：** 连接、登录、心跳
3. **功能验证：** 逐项测试prop/ctrl/alam/plan/rept
4. **异常测试：** 断网恢复、超时处理
5. **压力测试：** 高频消息、长时间运行

---

## 六、代码改动影响分析

### 6.1 改动影响矩阵

| 改动文件        | 影响范围   | 需同步修改 | 回滚难度 |
| --------------- | ---------- | ---------- | -------- |
| NbDriver.c      | MQTT连接层 | NbDriver.h | 中       |
| Json_Protocol.c | 协议入口   | 无         | 低       |
| sys_data.h/c    | 数据存储   | 全局引用   | 高       |
| App.c           | 上报逻辑   | 无         | 低       |
| sys_Vo_Io.c     | 告警检测   | 无         | 低       |

### 6.2 Keil项目配置更新

**需要修改：** `MDK-ARM-8008000/project.uvprojx`

**新增文件到项目：**

- ZK_Protocol.c/h
- ZK_JsonHandler.c/h
- ZK_Alarm.c
- ZK_WorkPlan.c
- ZK_OTA.c

---

## 七、实现顺序建议

```
阶段1: 基础架构 (Day 1-3)
  └─ 任务1.1 → 任务1.2 → 任务1.3 → 任务1.4

阶段2: 协议处理 (Day 4-7)
  └─ 任务2.1 → 任务2.2

阶段3: 上报机制 (Day 8-10)
  └─ 任务3.1 → 任务3.2 → 任务3.3 → 任务3.4

阶段4: 控制命令 (Day 11-12)
  └─ 任务4.1 → 任务4.2 → 任务4.3 → 任务4.4

阶段5: 告警机制 (Day 13-14)
  └─ 任务5.1 → 任务5.2 → 任务5.3

阶段6: 工作计划 (Day 15-16)
  └─ 任务6.1 → 任务6.2 → 任务6.3

阶段7: OTA适配 (Day 17-18)
  └─ 任务7.1 → 任务7.2

阶段8: 属性读写 (Day 19-20)
  └─ 任务8.1 → 任务8.2

总预计工期: 20个工作日
```

---

## 八、Verification（验证方案）

### 8.1 编译验证

- Keil编译无错误无警告
- 检查map文件确认固件大小<112KB
- 检查RAM使用<48KB

### 8.2 功能验证清单

1. **MQTT连接验证：**

   - 日志输出密码值，与预期一致
   - 连接成功响应 `+QMTCONN: 0,0,0`
   - 订阅成功响应 `+QMTSUB: 0,1,0,1`
2. **登录上报验证：**

   - 平台收到DevInfo数据
   - SN字段与IMEI一致
3. **调光控制验证：**

   - 平台下发亮度50%
   - 设备响应ER=0
   - 实际PWM输出变化
4. **告警上报验证：**

   - 模拟过压条件
   - 平台收到almId=10000消息
5. **OTA升级验证：**

   - 完整升级流程执行
   - 新固件正常运行

---

## 九、附录：关键代码位置索引

| 功能           | 文件                    | 行号范围 | 函数名                    |
| -------------- | ----------------------- | -------- | ------------------------- |
| MQTT服务器配置 | NbDriver.c              | 445      | _4G_configModule_machine  |
| ClientId生成   | NbDriver.c              | 453-459  | CONNECT_CONFIG_AT_QMTCONN |
| 订阅主题配置   | NbDriver.c              | 465-468  | CONNECT_CONFIG_AT_QMTSUB  |
| 发布消息       | NbDriver.c              | 567-576  | nbSendTcpData             |
| JSON入口解析   | Json_Protocol.c         | 738-827  | app_mqtt_rx               |
| 数据上报       | App.c                   | -        | uploadAllDataPoint        |
| 电能采集       | sys_bl0942.c            | -        | sys_bl0942_process        |
| 调光控制       | sys_pwm.c               | -        | sys_pwm_output            |
| 温度保护       | sys_temp_over_protect.c | -        | temp_detect_is_over       |
| 漏电检测       | danger_current_check.c  | -        | danger_current_check      |
| 系统存储       | sys_data.c              | -        | sys_data_store/load       |
| OTA升级        | ota.c                   | -        | _4G_OTA_machine           |

---

## 十、【重要】安全约束与风险评估补充

> **⚠️ 此章节为重构的硬性约束，实施前必须确认所有检查项**

### 10.1 Flash分区大小与OTA升级影响

#### 10.1.1 Flash布局现状

```
STM32F103RC Flash布局 (256KB Total):
┌─────────────────────────────────────────────────────────────┐
│ 地址范围              │ 大小    │ 用途                      │
├─────────────────────────────────────────────────────────────┤
│ 0x8000000-0x8005000  │ 20KB   │ Bootloader区 (不可修改)    │
│                       │        │ - 系统启动代码             │
│                       │        │ - OTA跳转逻辑             │
│                       │        │ - 固件校验                │
├─────────────────────────────────────────────────────────────┤
│ 0x8005000-0x8006800  │ 6KB    │ 系统参数主区              │
│                       │        │ DATAROM_STARTADDR         │
├─────────────────────────────────────────────────────────────┤
│ 0x8006800-0x8008000  │ 6KB    │ 系统参数备份区            │
│                       │        │ BAKDATAROM_STARTADDR      │
├─────────────────────────────────────────────────────────────┤
│ 0x8008000-0x8024000  │ 112KB  │ APP主程序区               │
│                       │        │ (MDK IROM Size 0x1C000)   │
├─────────────────────────────────────────────────────────────┤
│ 0x8024000-0x8040000  │ 112KB  │ OTA备份区                 │
│                       │        │ OTABAKROM_STARTADDR       │
│                       │        │ 新固件下载存储位置        │
└─────────────────────────────────────────────────────────────┘

⚠️ APP分区上限: 112KB (0x8008000 - 0x8024000)
⚠️ OTA备份区大小: 112KB (必须能完整存储新固件)
```

#### 10.1.2 当前固件大小

| 类型                | 大小 (字节)      | 大小 (KB)         | 来源    |
| ------------------- | ---------------- | ----------------- | ------- |
| Code                | 49,966           | 48.8 KB           | map文件 |
| RO-data             | 1,546            | 1.5 KB            | map文件 |
| RW-data             | 1,440            | 1.4 KB            | map文件 |
| ZI-data             | 24,656           | 24.1 KB           | map文件 |
| **Total ROM** | **51,756** | **50.5 KB** | map文件 |
| **Total RAM** | **26,096** | **25.5 KB** | map文件 |

#### 10.1.3 新增代码预估

| 新增模块                    | 预估ROM (KB)        | 预估RAM (字节)   |
| --------------------------- | ------------------- | ---------------- |
| ZK_Protocol.c (密码/核心)   | 2 KB                | 64 (密码缓存)    |
| ZK_JsonHandler.c (JSON处理) | 8 KB                | 512 (JSON缓冲区) |
| ZK_Alarm.c (告警处理)       | 3 KB                | 128 (告警状态)   |
| ZK_WorkPlan.c (工作计划)    | 4 KB                | 256 (计划缓存)   |
| ZK_OTA.c (OTA适配)          | 2 KB                | 64               |
| sys_data扩展字段            | 0.5 KB              | 约100字节        |
| **合计新增**          | **约19.5 KB** | **约1 KB** |

#### 10.1.4 固件大小安全计算

```
当前固件: 50.5 KB
新增代码: ~19.5 KB
预估总大小: ~70 KB

安全边界验证:
├─ APP分区上限: 112 KB
├─ 预估固件大小: 70 KB
├─ 剩余空间: 42 KB (约37%余量)
│
├─ OTA备份区大小: 112 KB
├─ 新固件大小: 70 KB
├─ OTA存储余量: 42 KB
│
✅ 结论: ROM空间充足，OTA升级不会越界
```

#### 10.1.5 OTA升级安全阈值

| 条件               | 阈值          | 预估值        | 状态      |
| ------------------ | ------------- | ------------- | --------- |
| APP固件大小        | ≤ 100KB      | 70KB          | ✅ 安全   |
| OTA备份区容量      | ≥ 新固件大小 | 112KB ≥ 70KB | ✅ 安全   |
| Bootloader跳转地址 | 固定0x8008000 | 不修改        | ✅ 无影响 |

---

### 10.2 底层影响分析

#### 10.2.1 关键底层约束（不可修改）

| 项目                           | 当前值          | 修改? | 影响说明                  |
| ------------------------------ | --------------- | ----- | ------------------------- |
| **Bootloader跳转地址**   | 0x8008000       | ❌    | Bootloader固定跳转此地址  |
| **中断向量表偏移(VTOR)** | 0x8008000       | ❌    | main.c:144 设置，不可改动 |
| **sys_data存储地址**     | 0x8005000/0x8006800 | ❌    | Flash布局固定             |
| **固件校验位置**         | 0x8008200       | ❌    | checksum字段位置固定      |
| **device_type标识**      | 固定值          | ❌    | Bootloader校验依赖        |
| **APP起始地址**          | APROM_STARTADDR | ❌    | 定义在sys_data.h          |

#### 10.2.2 VTOR设置验证

**代码位置:** `Core/Src/main.c:144`

```c
SCB->VTOR = APROM_OFFSET_ADDR;  // = 0x08008000
```

**定义位置:** `Core/Src/common.h:35`

```c
#define APROM_OFFSET_ADDR  0x08000000  // 或 0x08008000 (取决于是否有Bootloader)
```

⚠️ **安全约束:** 协议层修改不得影响VTOR设置，否则中断向量表错误导致系统崩溃。

#### 10.2.3 协议修改对底层影响矩阵

| 修改内容     | 涉及底层            | 影响程度 | 安全性            |
| ------------ | ------------------- | -------- | ----------------- |
| MQTT连接参数 | NbDriver.c (AT指令) | 仅连接层 | ✅ 不影响启动     |
| JSON协议解析 | Json_Protocol.c     | 纯应用层 | ✅ 不影响底层     |
| 数据结构扩展 | sys_data.h/c        | 存储格式 | ✅ 向后兼容       |
| 告警检测映射 | sys_Vo_Io.c         | 检测逻辑 | ✅ 仅调用现有接口 |
| 调光控制     | sys_pwm.c           | 不修改   | ✅ 仅调用现有接口 |

---

### 10.3 栈和堆内存风险分析

#### 10.3.1 当前栈/堆配置

**文件:** `MDK-ARM-8008000/startup_stm32f103xe.s`

```assembly
第33行: Stack_Size  EQU  0x400    ; 1024字节 = 1KB  ⚠️ 当前值偏小
第43行: Heap_Size   EQU  0x2800   ; 10240字节 = 10KB
```

#### 10.3.2 栈使用分析

| 调用层级                                 | 预估栈消耗              |
| ---------------------------------------- | ----------------------- |
| main() → app_mqtt_rx() → cJSON_Parse() | 约300字节               |
| cJSON_Parse() 内部递归                   | 约200-400字节           |
| 局部变量 (JSON缓冲区指针)                | 约50字节                |
| 中断嵌套预留                             | 约200字节               |
| **最大栈消耗预估**                 | **约650-850字节** |

#### 10.3.3 栈溢出风险评估

```
当前栈大小: 1KB (1024字节)
最大预估消耗: 850字节
安全余量: 174字节 (约17%)

⚠️ 风险等级: 中等偏高

原因: cJSON_Parse处理复杂JSON时可能递归较深，
      加上中断打断时栈消耗叠加，存在溢出风险。
```

#### 10.3.4 【必须修改】栈空间配置

**修改文件:** `MDK-ARM-8008000/startup_stm32f103xe.s`

```assembly
// 第33行，必须修改为:
Stack_Size  EQU  0x800    ; 2048字节 = 2KB (推荐)
// 或更保守:
Stack_Size  EQU  0x1000   ; 4096字节 = 4KB (保守配置)
```

⚠️ **硬性约束:** 栈大小必须 ≥ 2KB，否则cJSON解析可能栈溢出导致系统崩溃。

---

### 10.4 RAM使用详细分析

#### 10.4.1 RAM约束

```
STM32F103RC RAM: 48KB (0x20000000-0x2000C000)

当前使用:
├─ RW-data: 1.4KB (初始化数据)
├─ ZI-data: 24.1KB (未初始化数据，含栈/堆)
├─ Stack: 1KB (startup定义) → 建议增大到2KB
├─ Heap: 10KB (startup定义)
├─ Total RAM: 26.1KB

剩余RAM: 48KB - 26.1KB = 约22KB
```

#### 10.4.2 新增RAM消耗预估

| 新增项目           | 消耗              |
| ------------------ | ----------------- |
| JSON静态缓冲区     | 512字节           |
| 告警状态缓存       | 128字节           |
| 工作计划缓存       | 256字节           |
| 密码/主题字符串    | 128字节           |
| 协议解析临时变量   | 约256字节         |
| **合计新增** | **约1.3KB** |

#### 10.4.3 栈增大后RAM使用

```
栈增大到2KB后:
├─ Stack增量: +1KB
├─ 新增协议消耗: +1.3KB
├─ Total RAM: 26.1KB + 1KB + 1.3KB = 28.4KB
├─ 剩余: 48KB - 28.4KB = 约19.6KB

✅ RAM空间充足 (剩余约40%)
```

---

### 10.5 边界检查要求

#### 10.5.1 必须实现的边界检查

| 检查项                   | 阈值       | 检查位置                    | 失败处理           |
| ------------------------ | ---------- | --------------------------- | ------------------ |
| **JSON字符串长度** | ≤512字节  | ZK_JsonHandler.c            | 截断或返回错误     |
| **属性数组索引**   | ≤回路数   | ZK_HandlePropertyRead/Write | 返回ER=6(数组越界) |
| **告警数组索引**   | ≤10       | ZK_HandleAlarmConfig        | 返回ER=6           |
| **工作计划数量**   | ≤7        | ZK_HandleWorkPlan           | 返回ER=6           |
| **Flash写入地址**  | <0x8024000 | hw_flash.c                  | 保持现有检查       |
| **固件下载大小**   | ≤100KB    | ota.c                       | 保持现有检查       |

#### 10.5.2 边界检查代码模板

```c
// ZK_Protocol.h 定义常量
#define ZK_JSON_BUF_MAX_LEN     512   // JSON缓冲区最大长度
#define ZK_MAX_PLAN_COUNT       7     // 工作计划最大数量
#define ZK_MAX_ALARM_COUNT      10    // 告警类型数量
#define ZK_MAX_PROPERTY_GROUPS  15    // 属性组数量
#define ZK_MAX_TIME_POINTS      10    // 每计划时间点数

// ZK_JsonHandler.c 实现检查
int ZK_BuildMessage(char *buf, u16 buf_len) {
    u16 json_len = strlen(buf);
    if (json_len >= buf_len || json_len >= ZK_JSON_BUF_MAX_LEN) {
        return -1;  // 缓冲区溢出或消息过长
    }
    return 0;
}

// Flash写入地址检查 (sys_data.c:86 已有)
if(size > 6*1024 || addr_main+size > APROM_STARTADDR) {
    return BOOL_FALSE;  // 防止写入超过APP起始地址
}
```

---

### 10.6 编译和调试方法

#### 10.6.1 编译验证步骤

| 步骤 | 操作            | 验证内容           | 检查文件                 |
| ---- | --------------- | ------------------ | ------------------------ |
| 1    | Keil编译        | 无错误无警告       | Build Output             |
| 2    | 查看map文件     | ROM < 100KB        | cat1.map末尾             |
| 3    | 查看map文件     | RAM < 35KB         | cat1.map末尾             |
| 4    | 检查栈配置      | Stack ≥ 2KB       | startup_stm32f103xe.s:33 |
| 5    | 检查scatter文件 | 起始地址=0x8008000 | *.sct文件                |

#### 10.6.2 map文件关键检查项

```
打开 MDK-ARM-8008000/out/cat1.map，检查末尾:

Total RW  Size (RW Data + ZI Data)  <  35KB   (当前26KB) ✅
Total ROM Size (Code + RO Data + RW Data) < 100KB  (当前50KB) ✅

搜索 "Stack_Mem": 确认栈大小 ≥ 2KB
搜索 "Heap_Mem": 确认堆大小 = 10KB
```

#### 10.6.3 调试输出建议

```c
// ZK_Protocol.h 定义调试宏
#define ZK_DEBUG_ENABLE  1

#if ZK_DEBUG_ENABLE
    #define ZK_LOG(fmt, ...) printf("[ZK] " fmt "\r\n", ##__VA_ARGS__)
#else
    #define ZK_LOG(fmt, ...)
#endif

// 关键调试输出位置:
ZK_LOG("Password: %s", zk_password);      // 验证密码生成
ZK_LOG("Topic: %s", pub_topic);           // 验证主题格式
ZK_LOG("JSON len: %d", strlen(json_buf)); // 验证消息长度
```

---

### 10.7 烧录后启动验证

#### 10.7.1 启动流程分析

```
启动流程:
1. CPU复位 → PC=0x8000000 (Bootloader入口)
2. Bootloader执行:
   ├─ 检查APP固件校验 (checksum)
   ├─ 检查固件长度
   ├─ 检查device_type匹配
   └─ 校验通过 → 跳转 0x8008000
3. APP启动:
   ├─ Reset_Handler执行
   ├─ SystemInit() 初始化时钟
   ├─ main() 开始:
   │   ├─ SCB->VTOR = 0x8008000 ⚠️ 关键
   │   ├─ sys_data_load() 加载配置
   │   └─ 进入主循环
4. MQTT连接:
   ├─ NbDriver状态机执行
   ├─ 密码生成
   ├─ 连接服务器
   └─ 订阅主题
```

#### 10.7.2 启动失败场景分析

| 失败场景         | 可能原因     | 验证方法           |
| ---------------- | ------------ | ------------------ |
| Bootloader不跳转 | 固件校验失败 | 检查checksum字段   |
| APP启动崩溃      | VTOR未设置   | 确认main.c:144执行 |
| 中断不响应       | VTOR地址错误 | 确认=0x8008000     |
| MQTT连接失败     | 密码错误     | 输出密码对比预期值 |
| 订阅失败         | 主题格式错误 | 输出主题字符串     |
| JSON解析崩溃     | 栈溢出       | 检查栈大小≥2KB    |

#### 10.7.3 首次烧录验证清单

```
烧录前检查:
├─ [ ] 确认固件大小 < 100KB (查看map文件)
├─ [ ] 确认栈大小 ≥ 2KB (startup文件)
├─ [ ] 确认scatter文件起始地址正确
├─ [ ] 确认VTOR设置代码存在 (main.c:144)

烧录后检查:
├─ [ ] 观察串口启动日志
├─ [ ] 确认Bootloader跳转成功
├─ [ ] 确认APP进入main循环
├─ [ ] 确认MQTT连接成功 (密码正确)
├─ [ ] 确认订阅主题成功
├─ [ ] 测试平台下发命令响应

失败处理:
├─ 栈溢出 → 增大Stack_Size到4KB
├─ MQTT连接失败 → 核对密码算法和输出
├─ 固件不启动 → 检查scatter文件和VTOR
```

---

### 10.8 修改前必须确认的安全措施

#### 10.8.1 准备工作清单

| 序号 | 准备工作                     | 操作                     |
| ---- | ---------------------------- | ------------------------ |
| 1    | **备份当前工程**       | 复制整个项目目录         |
| 2    | **记录当前固件大小**   | 记录map文件数据 (50.5KB) |
| 3    | **确认Bootloader版本** | 确保device_type兼容      |
| 4    | **准备调试串口**       | 确保能观察启动日志       |
| 5    | **准备测试服务器**     | 使用中科测试环境         |

#### 10.8.2 安全修改顺序

```
阶段0: 【最先执行】安全配置修改
├─ 修改 Stack_Size = 0x800 (增大栈到2KB)
├─ 编译验证栈增大后的RAM使用 (< 35KB)
└─ 确认编译成功后再继续后续阶段

阶段1: 基础架构
├─ 新增 ZK_Protocol.h/c (不影响现有代码)
├─ 扩展 sys_data.h (添加字段到结构体末尾)
└─ 编译验证新增代码大小

阶段2: MQTT连接参数修改
├─ 修改 NbDriver.c 连接参数
├─ 单独编译测试连接
└─ 验证连接成功后再继续

阶段3-8: 功能实现
└─ 各功能模块逐步添加，每阶段编译验证大小
```

#### 10.8.3 每次编译后必须检查

```
map文件末尾检查:
Total RW  Size  <  35KB
Total ROM Size  <  100KB

栈使用检查:
栈最大消耗预估 < Stack_Size
```

---

### 10.9 必须修改的安全项总结

#### 10.9.1 高优先级（必须修改）

| 序号 | 修改项                   | 原值        | 修改值      | 文件位置                 | 原因                |
| ---- | ------------------------ | ----------- | ----------- | ------------------------ | ------------------- |
| 1    | **栈大小**         | 0x400 (1KB) | 0x800 (2KB) | startup_stm32f103xe.s:33 | cJSON解析栈溢出风险 |
| 2    | **JSON缓冲区限制** | 512B        | 保持        | ZK_JsonHandler.c         | 消息过长风险        |
| 3    | **数组边界检查**   | 无          | 新增        | 各ZK_*.c文件             | 数组越界风险        |

#### 10.9.2 不修改项（保持不变）

| 项目               | 原值      | 位置           | 原因           |
| ------------------ | --------- | -------------- | -------------- |
| Bootloader跳转地址 | 0x8008000 | Bootloader代码 | 固件校验依赖   |
| VTOR设置           | 0x8008000 | main.c:144     | 中断向量表位置 |
| sys_data存储地址   | 0x8005000/0x8006800 | sys_data.h     | Flash布局固定  |
| OTA备份地址        | 0x8024000 | sys_data.h     | 新固件存储位置 |
| device_type标识    | 固定值    | for_iap.c      | Bootloader校验 |
| APROM_STARTADDR    | 0x8008000 | sys_data.h:88  | APP分区起始    |

---

### 10.10 快速参考表

#### A. 当前资源使用

```
ROM: 50.5KB / 112KB  (剩余61.5KB, 余量54%) ✅
RAM: 26.1KB / 48KB   (剩余21.9KB, 余量46%) ✅
Stack: 1KB           ⚠️ 必须增大到2KB
Heap: 10KB           ✅ 足够
```

#### B. 新增后预估

```
ROM: 70KB / 112KB    (剩余42KB, 余量37%) ✅
RAM: 28.4KB / 48KB   (剩余19.6KB, 余量41%) ✅
Stack: 2KB           ✅ 推荐
Heap: 10KB           ✅ 足够
```

#### C. 安全阈值

```
固件大小上限: 100KB (留12KB缓冲)
RAM使用上限: 35KB (留13KB缓冲)
栈使用上限: Stack_Size的70%
JSON消息上限: 512字节
```

---

**文档状态: 完整（包含风险评估补充）**
**更新时间: 2024-04-18**
