# 第一轮完成度审计

更新时间：2026-07-10 09:35 Asia/Shanghai

本审计按原始任务要求逐项判定，不把“已有防误操作保护”或“外部条件缺失”写成完整闭环。详细验收矩阵见 `docs/第一轮验收矩阵.md`，实机日志摘要见 `docs/实机验证记录.md`，当前优化镜像 OTA 交接清单见 `docs/当前优化镜像OTA闭环交接清单.md`。

## 总体结论

第一轮体积优化、旧链路裁剪、Debug/Release-MinSize 双配置、map 对比、J-Link 下载、reset-pin 启动、串口/MQTT 基础功能闭环已完成并达标。Release-MinSize Total ROM Size 从 93212 bytes 降到 71648 bytes，APP 起始地址保持 `0x08008000`，Boot/OTA/CRC/backup/copy 校验链路未被删除。

当前第一轮主目标已完成并通过可执行审计。仍作为后续策略或量产增强项记录的项目有 2 个：

1. 原生 type=2 日出日落定时触发：PARTIAL，Release-MinSize 按第一轮体积目标关闭设备端天文计算；平台换算为普通具体时间点后的计划执行已实测通过。
2. 当前镜像 live OTA 后 J-Link 只读逐字节 verify：FOLLOW-UP，本次 MQTT OTA 已通过，后续 `jlink_verify.ps1` 尝试因探针 USB 连接失败未完成；不影响既有 Release J-Link 烧录/verify 证据。

## 逐项审计

| 原始要求 | 状态 | 证据 | 说明 |
|---|---|---|---|
| 建立 baseline 并保存 map/bin/log | PASS | `baseline_map_summary.md`, `output/baseline_cat1.map`, `output/baseline_cat1.bin` | Baseline `program` 构建通过，记录 Code/RO/RW/ZI/ROM。 |
| 创建 Debug 与 Release-MinSize 双配置 | PASS | `MDK-ARM-8008000/project.uvprojx`, `output/debug_final_build.log`, `output/release_minsize_final_build.log` | Debug 保留日志，Release-MinSize 加入目标宏并构建 0 errors/0 warnings。 |
| Release-MinSize 体积控制到 85-90 KB 以下 | PASS | `docs/map_size_compare.md`, `output/release_minsize_final_cat1.map` | Total ROM Size `93212 -> 71648` bytes，约 69.97 KB。 |
| Boot/APP/APROM/OTABAKROM 地址不改 | PASS | `output/release_minsize_final_cat1.map` | APP `ER_IROM1` exec/load base 仍为 `0x08008000`。 |
| 禁止删除固件头、CRC、backup、APROM copy、Boot 校验 | PASS | `output/release_minsize_final_cat1.bin`, `tools/ota_test/logs/mqtt_ota_20260710_032918.jsonl`, `output/jlink_verify_old_ota_aprom_20260710_033010.log` | 当前包头校验通过；raw TCP OTA 主链路用服务器旧镜像跑通并 verify APROM。 |
| 禁止全片擦除或修改读保护 | PASS | `docs/实机验证记录.md`, J-Link flash logs | 记录为按地址下载/verify，未执行全片擦除，未改 RDP。 |
| Release-MinSize 关闭 UART3 日志链路 | PASS | `output/release_minsize_final_cat1.map`, `docs/map_size_compare.md` | `hw_uart3.o` Release 贡献 0，`main.o` 不再分配 `hw_uart3_init/hw_uart3_process/dma_printf`。 |
| Debug Target 保留 UART3 日志能力 | PASS | `output/debug_final_resetpin_serial_20260710_025029.txt` | 捕获 APP build date、4G、IMEI/ICCID、MQTT ACK、RTC sync 等日志。 |
| 清理旧数据点协议链路 | PASS | `output/release_minsize_final_cat1.map`, `docs/map_size_compare.md` | `app.o=0`, `protocol.o=0`，旧 `sendTcpData` 仅在 removing/cross-reference 中出现。 |
| 裁剪 OTA 调试日志和旧 QHTTP/UFS 路径 | PASS | `Core/Src/LampProtocolLib/ota_config.h`, `output/release_minsize_final_cat1.map` | Release 保留 raw TCP 状态机，关闭 QHTTPREADFILE/UFS 诊断和 raw hex/debug-only 路径。 |
| 移除浮点数学库依赖 | PASS | `output/release_minsize_final_cat1.map` | `powf/sqrtf/sinf/cosf/tanf/acosf/asinf/fmodf/floorf` 未进入最终镜像。 |
| cJSON 本轮只统计不大改 | PASS | `docs/固件体积优化_第一轮报告.md` | 已记录发送/接收 JSON 依赖和下一轮轻量 writer 方案。 |
| `scanf_fp.o` 尽量移除 | PARTIAL | `output/release_minsize_final_cat1.map` | 仍由 cJSON 数字逻辑引入；符合本轮“不直接重构 mqtt_zk_protocol.c / 不删除 cJSON”约束。 |
| RAM buffer 检查，不盲目缩小 | PASS | `docs/固件体积优化_第一轮报告.md` | 已记录 MQTT、OTA、portable、flash、nbdriver 的主要 RW/ZI，占位到下一轮错峰复用评估。 |
| J-Link 下载、verify、reset-pin APP handoff | PASS | `output/release_minsize_final_jlink_flash.log`, `tools/ota_test/logs/jlink_reset_20260710_080330.log` | `RSetType 2` 后 PC 位于 APP，VTOR=`0x08008000`，fault=0，`RCC_CSR=0`。 |
| 串口 Boot 启动日志 | PASS | `output/release_minsize_resetpin_serial_20260710_024056.txt` | reset-pin 后出现 `from power on or reset` 和 `checksum ok`。 |
| 4G 初始化、IMEI/ICCID、MQTT 登录/心跳/订阅 | PASS | `tools/ota_test/logs/mqtt_release_monitor_20260710_081125.jsonl`, `tools/ota_test/logs/mqtt_release_minsize_soak_20260710_025645.jsonl` | 登录含 IMEI/ICCID，12 min soak heartbeat=11、offline=0。 |
| 平台开关、调光、查询/巡检 | PASS | `tools/ota_test/logs/mqtt_release_minsize_control_switch_20260710_024853.jsonl`, `tools/ota_test/logs/mqtt_release_minsize_control_dim_20260710_024557.jsonl`, `tools/ota_test/logs/mqtt_release_minsize_control_query_20260710_024341.jsonl` | 控制 ACK 均 `ER=0`，运行状态上报匹配。 |
| BL0942 采集与上报链路 | PASS | `tools/ota_test/logs/mqtt_release_minsize_bl0942_telemetry_20260710_032121.jsonl`, `tools/ota_test/logs/mqtt_release_minsize_bl0942_telemetry_20260710_090815.jsonl` | 多次巡检返回合法数字 `EleInfo`。 |
| BL0942 带载遥测 | PASS | `tools/ota_test/logs/mqtt_release_minsize_bl0942_telemetry_20260710_143039.jsonl` | 电子负载接入后 6 次采样 v/c/p 均非零并落在当前协议原始缩放值范围内，offline=0。 |
| NTC 温度采集与过温保护 | PASS | `tools/ota_test/logs/mqtt_release_minsize_ntc_threshold_20260710_031702.jsonl` | 临时降低阈值触发保护和告警，恢复阈值后告警恢复。 |
| 参数保存与重启恢复 | PASS | `tools/ota_test/logs/mqtt_release_minsize_param_persist_20260710_025449.jsonl` | `Sense.sBri` 写入 70 后 reset-pin 仍保持，随后恢复 80。 |
| RTC 功能 | PASS | `tools/ota_test/logs/mqtt_release_minsize_rtc_plan_20260710_085552.jsonl` | RTC 读/写/读回均 `ER=0`，读回 delta 约 0.5 s。 |
| 普通计划工作定时调光 | PASS | `tools/ota_test/logs/mqtt_release_minsize_plan_exec_20260710_090547.jsonl`, `tools/ota_test/logs/mqtt_release_minsize_rtc_plan_20260710_090802.jsonl` | 临时 plan id 8 到点执行 `bri=70`，随后删除，`nid=[]`。 |
| 日出日落模式定时调光 | PARTIAL | `output/release_minsize_final_cat1.map`, `docs/第一轮验收矩阵.md` | 设备端天文计算按 Release-MinSize 目标关闭；平台换算为普通计划的执行链路已验证，原生 type=2 未实测触发。 |
| 告警上报 | PASS | `tools/ota_test/logs/mqtt_release_minsize_ntc_threshold_20260710_031702.jsonl`, `tools/ota_test/logs/mqtt_release_monitor_20260710_081125.jsonl` | NTC 告警和启动后告警均有 MQTT 证据。 |
| 看门狗不误复位 | PASS | `tools/ota_test/logs/mqtt_release_minsize_soak_20260710_025645.jsonl` | 720 s 内 login=1、heartbeat=11、offline=0、ota_error=0。 |
| raw TCP OTA 不被破坏 | PASS | `tools/ota_test/logs/mqtt_ota_20260710_032918.jsonl`, `output/jlink_verify_old_ota_aprom_20260710_033010.log` | 用现有服务器旧镜像完成 OTA，progress=100，APROM 与下载镜像一致。 |
| 当前优化镜像 live OTA | PASS | `tools/ota_test/logs/mqtt_guarded_ota_20260710_142754.jsonl`; `tools/ota_test/logs/mqtt_guarded_ota_20260710_142819.jsonl` | 新 URL 与当前 71648-byte 镜像 size/checksum/SHA256 完全一致；live OTA ACK `ER=0`，progress=100，设备重启后 `sver=22` 重新登录，offline=0。 |
| 输出第一轮报告、map 对比、实机记录、output artifacts | PASS | `docs/固件体积优化_第一轮报告.md`, `docs/map_size_compare.md`, `docs/实机验证记录.md`, `output/` | 主要交付文件已生成。 |

## 最小收尾条件

第一轮主目标已经从“外部阻塞”推进到“可执行审计通过”。后续增强项：

1. 明确平台是否接受“日出日落平台换算为普通计划”策略；若必须支持原生 type=2，则补做原生 type=2 下发、触发、恢复实测。
2. 如需 OTA 后逐字节读回确认，恢复 J-Link USB 后运行 `tools/ota_test/jlink_verify.ps1 -Image output\release_minsize_final_cat1.bin -Address 0x08008000 -ResetMode ResetPin -ProbeAfterRunMs 12000`。
3. 如需量产级计量结论，用参考表计进一步收窄 `mqtt_bl0942_telemetry_check.py --expect-range` 的 v/c/p 范围。

## 可执行验收审计

新增可重复执行的验收脚本：

```powershell
python tools\ota_test\release_minsize_acceptance_audit.py
```

最新输出：

```text
tools/ota_test/logs/release_minsize_acceptance_audit_20260710_143812.json
```

该脚本当前结论为 `summary=pass`：Release-MinSize 宏、最终 map、镜像头、必要文档、基线/Release/Debug 构建产物、构建日志、串口日志、J-Link flash 日志、当前镜像 live OTA、带载 BL0942 和代表性实机日志语义检查均为 PASS。

## 本轮追加核验

2026-07-10 09:18 追加核验：

1. 重新运行 guarded OTA preflight，日志 `tools/ota_test/logs/mqtt_guarded_ota_20260710_091835.jsonl`。结果仍为 safe abort：服务器 URL 返回 93212-byte 旧镜像，当前 Release-MinSize 期望为 71648-byte，未发布 MQTT OTA。
2. 检查 `MDK-ARM-8008000/project.uvprojx` 的 `Release-MinSize` target，所有原始任务要求的 Release 宏均存在，缺失项为空。
3. 对 `output/release_minsize_final_cat1.map` 从 `Image Symbol Table` 之后做 section-aware 检查，`hw_uart3_init/hw_uart3_process/dma_printf`、旧数据点入口和 `powf/sqrtf/sinf/cosf/tanf/acosf/asinf/fmodf/floorf` 均未命中 allocated symbol/map 区域。
4. 复核 `output/release_minsize_final_cat1.bin` 镜像头：size `71648`，checksum `0x0070A0EE`，device type `0x0003`，`valid=true`。
5. 2026-07-10 14:38 运行 `tools/ota_test/release_minsize_acceptance_audit.py` 并通过 `py_compile`；脚本输出 `release_minsize_acceptance_audit_20260710_143812.json`，汇总状态为 `pass`。该版本解析基线/Release/Debug 构建产物、构建、串口、当前镜像 OTA、带载 BL0942、MQTT/J-Link 日志中的实际 pass 信号、ACK、上报、RTC/计划/NTC/OTA 状态和 reset-pin VTOR/fault/RCC 证据。
