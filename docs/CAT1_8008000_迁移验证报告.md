# CAT.1 8008000 干净工程迁移验证报告

## 迁移边界

- 目标目录：`/Users/mac/Desktop/CAT1_Keil_Project`
- 唯一 Keil App 入口：`MDK-ARM-8008000/project.uvprojx`
- App 起始地址：`0x08008000`
- App 安全结束地址：`0x08024000`
- 源代码来源：`/Users/mac/Desktop/keil project/source_code`

## 保留范围

- Keil 工程实际引用的 84 个 `.c/.s` 文件。
- 中科协议链路：`Json_Protocol.c`、`mqtt_zk_protocol.c`、`zk_work_plan.c`、`NbDriver.c`、`TcpClient.c`。
- CAT.1 AT 状态机、OTA、PWM、电参采集、掉电检测、温度保护、Flash 参数和 cJSON。
- 协议文档、进度计划、重构方案、测试用例和验证工具。

## 剥离范围

- 旧 `MDK-ARM-8000000` 工程。
- Keil 历史输出：`out/`、`Objects/`、`Listings/`。
- 未编译 DALI/485 协议转换代码、USB Device middleware、CMSIS NN/Core_A/RTOS 示例包。
- 旧 `http_active 不用.c`、`Task scheduler.c`、macOS `.DS_Store`。

## 保留但需说明的历史命名

- `hw_uart2`：当前用于 BL0942 电参采集，不启用 485 业务协议。
- `hw_gateway`：当前 CAT.1 AT 接收、发送和状态切换仍依赖该模块。
- `app_active/http_active`：旧 HTTP 激活主流程已停用，但定时器和 HTTP 配置状态仍被现有状态机兼容调用。

## 验证命令

```bash
python3 -m unittest discover -s tests -v
bash tests/run_mqtt_protocol_tests.sh
python3 tools/check_keil_app_image.py --project MDK-ARM-8008000/project.uvprojx --skip-freshness
```

`--skip-freshness` 仅用于迁移阶段检查源码、工程和 Flash 契约。正式烧录前必须在 Keil 中重新 Rebuild，并执行：

```bash
python3 tools/check_keil_app_image.py --project MDK-ARM-8008000/project.uvprojx
```

通过标准：

- `file_refs=84`
- `missing_refs=0`
- `outside_target_refs=0`
- 工程不引用旧 `MDK-ARM-8000000` 路径
- 重新 Rebuild 后 `cat1.bin` 写入范围必须小于 `0x08024000`
