# 量产校准固件实现证据与待审核清单

状态：`待审核`。本分支只完成可独立验证的基线、只读快照和不可用门禁；没有把缺失的协议、Keil/MAP 或实机结果填成通过。

## 实现范围

- 分支：`codex/cat1-calibration-20260804-6c6a`
- 工作树：`/Users/mac/.codex/worktrees/6c6a/CAT1_Keil_Project`
- 基线 HEAD：`63f68f41ac8234554d7f37d628849220cd0cfbeb`
- F0 提交：`0899256`，集中 Flash 所有权、临时 A/B 候选、协议/安全/擦写门禁。
- F1 提交：`66b7ced`，接入唯一 BL0942 读取链，增加独立 BL0942、ADC、PWM 原始快照、无撕裂读取、帧校验和安全不可用服务框架。
- 当前量产边界：50W `MID=1`、`890mA`、`36–56V`、`RS3=120mΩ`、11 点 `0/20/.../200`；100W 仅记录 `RS3=50mΩ`，未启用。

## 已通过的主机/源码门禁

| 门禁 | 结果 | 证据 |
|---|---|---|
| 快照、服务门禁、BL0942 帧向量主机测试 | PASS | `bash tools/calibration/run_host_tests.sh` |
| ARM GNU C 语法检查 | PASS | `bash tools/calibration/run_arm_syntax_check.sh` |
| `sys_bl0942.c` 集成语法检查 | PASS | 上述脚本中的 `clang -target arm-none-eabi -fshort-enums` |
| Keil 工程 XML 结构检查 | PASS | `xmllint --noout MDK-ARM-8008000/project.uvprojx` |
| 提交前空白检查 | PASS | 两次提交前均执行 `git diff --cached --check` |

主机向量覆盖独立 seq/tick/age/skew、32 位 tick 回绕、ADC/PWM/BL0942 原始值、BL0942 负 WATT、保留字节、校验覆盖 byte 21、载荷变异拒绝，以及协议/codec/Flash commit/非零输出均不可用时的安全返回。

## 未完成且不得放行的门禁

| 看板 | 状态 | 缺失证据/下一步 |
|---|---|---|
| F2 协议与 MQTT | BLOCKED | 缺少权威校准协议原件、黄金 `.cal.bin` 和字段表；因此没有编造 198 字节/11×18、没有实现 `SV=cal` wire codec、Raw/暂存/临时应用/提交，也没有 MQTT/tc 联调。补齐后必须以同一 fixture/manifest 做 encode/decode、ACK+匹配回读和幂等 seq 测试。 |
| F3 Flash A/B | BLOCKED | `0x08005C00` 与 `0x08007400` 仍是共享 2KiB 页后半 1KiB 的 candidate；没有当前 HEAD 的新鲜 Keil MAP/HEX、所有者确认、共页邻居保护和掉电故障注入，不得冻结地址或启用 commit。 |
| F4 输出安全与联锁 | BLOCKED | 已确认正常启动 softstar 存在复位后 0→100% 风险；当前输出过流/过功率保护未证明实际关断；PWM=1000、`HWMAX_OUTCUR=1680` 不是硬件安全上限；boot-inhibit/工站硬联锁契约与 HIL 均缺失。校准服务在本分支保持非零输出关闭。 |
| Keil/MAP | PENDING | 当前环境未运行 Keil，未生成或验证新鲜 MAP/BIN/HEX。 |
| MQTT/tc | PENDING | 没有校准协议原件和可互通 payload，未执行设备 ACK/回读或 tc 工作台联调。 |
| 实机 HIL | PENDING | 未执行加电、限流、急停、复位/看门狗/棕断、保护触发、掉电 A/B 或仪器精度验证。 |

## 提交边界

只精确暂存了 F0/F1 文件；用户原有未跟踪任务书文档未暂存，旧 BIN/MAP/pyc 和本地残留测试未作为证据进入提交。当前停止在 `待审核`，不推送、不合并；收到独立审核的 P0/P1 反馈后再追加修复提交和测试。
