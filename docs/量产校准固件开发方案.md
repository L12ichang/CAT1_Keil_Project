# 量产校准固件开发方案（历史索引）

> 本文件仅保留旧路径兼容，不再定义当前校准算法、参数、协议或量产门禁。

当前 `fix/v3-real-calibration-20260831` 的唯一固件实施依据：

```text
docs/V3六型号真实校准最终设计.md
```

上位机对应依据：

```text
tc-desktop-client/docs/V3六型号真实校准上位机实施.md
```

当前冻结摘要：

- Output：11点真实物理采集 + inverse PWL，得到 `TargetCurrent -> CorrectPWM`；
- OCO：0% Offset + 90% Gain 两点线性采样校准；
- 正式校准电压：36/40/44/48/52/56V，默认36V；
- BUILD：Settling Delay + 固定采样次数 + MEDIAN，只做安全/数据有效性检查；
- APPLY 后 QUICK/FULL：完整验证并生成误差报告，非零误差不提前终止校准；
- COMMIT/READ：只受安全、曲线合法性、Payload/CRC/Flash/Readback硬门禁约束；
- 当前设备 SET/HWMAX 来自 CAP/Factory，Product Default 只做参考；I-V Limit 不得写回 HWMAX。

历史规划、旧功率专项参数、旧 V2/198B 业务表、旧精度百分比门禁、旧复杂工艺参数均不得从 Git 历史重新带回当前实现。
