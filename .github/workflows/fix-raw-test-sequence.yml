name: fix-raw-test-sequence
on:
  push:
    branches: [feat/legacy-11point-calibration-20260903]
    paths:
      - tools/calibration/fix-raw-test-sequence.py
      - .github/workflows/fix-raw-test-sequence.yml
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/legacy-11point-calibration-20260903
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tools/calibration/fix-raw-test-sequence.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "test(calibration): advance post-RAW rejection sequence"
          git push origin HEAD:feat/legacy-11point-calibration-20260903
