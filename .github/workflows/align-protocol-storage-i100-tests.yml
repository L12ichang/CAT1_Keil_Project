name: Align protocol storage I100 tests

on:
  push:
    branches:
      - feat/legacy-11point-calibration-20260903
    paths:
      - .github/workflows/align-protocol-storage-i100-tests.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/legacy-11point-calibration-20260903
          fetch-depth: 0
      - name: Align protocol/storage tests
        run: python3 .github/scripts/align-protocol-storage-i100-tests.py
      - name: Commit test update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/calibration/test_protocol_gates.c
          git commit -m "test(calibration): align protocol storage I100 fixtures"
          git push origin HEAD:feat/legacy-11point-calibration-20260903
      - name: Run legacy calibration host tests
        run: bash tools/calibration/run_host_tests.sh
